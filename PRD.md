# Gloss 产品需求文档 v1.3

**产品名**：Gloss（暂定）｜ **载体**：网页 ｜ **周期**：4 周
**文档负责人**：Clara ｜ **更新日期**：2026-09-14

---

## 0. 变更日志

| 版本 | 日期 | 主要变更 |
|---|---|---|
| v0.1–v0.3 | 09-13/14 | 确立核心功能、五条设计决策、反向指标；功能拆为两路；交互定为 click + inline 撑开；**否定"杜绝翻错"** |
| v1.0 | 09-14 | 重构为交付级规格：异常情况、验收标准、数据规则、非功能需求 |
| v1.1 | 09-14 | 定位升级为长期阅读器；功能二升为第二核心；输出回到三类；新增编辑白话；交互模型定稿 |
| v1.2 | 09-14 | ① **功能二的定义修正：整句一次，术语只是引导信号**；② 听不懂改为条件显示；③ 新增「已用」置灰态与结果常驻；④ 操作行收敛；⑤ 锁定取消按钮；⑥ 术语 nowrap；⑦ 新增 3.10 视觉规范；⑧ 新增 D13 |
| **v1.3** | **09-14** | **① 新增 F20 侧栏收起（沉浸阅读）——收起时中栏保持宽度居中，measure 不漂移**；② **主题系统：三套完整 token，不是换一个背景色**（羊皮纸 / 月白 / 护眼绿）；③ **侧栏底色比中栏深一档**，确立「中栏最亮、侧栏退后」的层次；④ 夜读模式明确划入 v2；⑤ 新增 D15、D16；⑥ **标注 W3 超载风险与可推迟项** |

---

# 第一部分：需求背景

## 1.1 谁，在什么场景，遇到什么问题

**谁**：读人文社科 / 哲学类长文本的学生（本科高年级、研究生、自学者）。

**场景**：在电脑上认真读一本难书，遇到长句、倒装句、术语堆叠的句子。

**问题**：读不懂，而现有路径太长——

> 复制整段甚至整章 → 切换到 AI → 粘贴 → 打字提问 → 等待 → 切回原文 → **找回刚才读到哪儿**

七步，最后一步最伤：注意力已经断了。

## 1.2 当前链路的损失点

| 环节 | 损失 | 证据 |
|---|---|---|
| 遇到难句 | — | 实测：56 句 / 平均 54.8 字 / 最长 160 字 / **9 句超 80 字**【强】 |
| 决定是否求助 | **大量难句被直接跳过** | 用户自述，比例随文本难度变化【中】 |
| 切换工具 | 七步操作，注意力中断 | 用户自述【中】 |
| 读完之后 | 理解不留存，复习要重来 | **用户已在 docx 正文里手打白话批注**【强】 |

## 1.3 核心洞察

> **问题不是"AI 做不到"，是"摩擦高到你不会真的去做"。**

产品的全部价值，是把七步压缩成一次点击。这同时回答了"跟直接问 AI 有什么区别"：**不是能力差异，是你会不会真的去做的差异。**

## 1.4 证据强度总表

| 证据 | 强度 |
|---|---|
| 用户已在用土办法解决（docx 手打批注） | **【强】** |
| 文本客观难度（句长实测） | **【强】** |
| 白话质量可被专业读者接受 | 【中】 |
| 保存动机是复习 | 【中】 |
| 打断导致体验崩坏（费曼 App 经历） | 【中】 |
| **其他读者是否也如此** | **【弱】——仅 1 个样本，本文档最大风险** |
| **长期阅读器定位是否成立** | **【弱】——零验证** |

---

# 第二部分：价值判断

## 2.1 产品定位

> **Gloss 不是"读完就走"的转换工具，而是一个长期使用的阅读空间。**

用户把难读的书放进来，在这里读、在这里问、在这里留下自己的理解，下次继续从这里读。

**产品的统一原则：一切服务于阅读的连贯性。** 所有功能与非功能的取舍都可以回到这一句来检验。

## 2.2 需求目标

| # | 目标 | 度量 | 成功 | 挑战 |
|---|---|---|---|---|
| G1 | 白话真的被读完 | 点击后读完白话的比例 | ≥60% | ≥75% |
| G2 | 理解被留下来 | 完读文档中白话保存率 | ≥30% | ≥50% |
| G3 | 用户会回来 | 7 日内二次打开同一文档 | ≥25% | ≥40% |
| G4 | 不打断阅读 | 首字延迟 P90 | ≤2.5s | ≤1.5s |
| G5 | 成本结构成立 | 缓存命中率（≥100 用户后） | ≥30% | ≥50% |

## 2.3 两个核心功能

| | **功能一：白话** | **功能二：听不懂** |
|---|---|---|
| 做什么 | **减法**——同一个意思，换个说法 | **加法**——结合上下文，对**这一句**做阅读理解 |
| 触发 | 单击句子 | 见 3.6 的条件显示规则 |
| 频次 | 每句可反复查看（结果固定不变） | **每句只能用一次** |
| 长度 | ≤150 字 | ≤250 字 |
| 模型 | 快模型 | 强模型 |
| 定位 | 主路径 | **第二核心功能，不是逃生口** |

### ⚠️ 功能二的定义（v1.2 修正，此前的措辞会导致实现错误）

> **功能二对「整句」生效，不是对「术语」生效。**
>
> 它做的是**结合上下文的整句阅读理解**，**每句只能用一次**。
>
> 一个被标记的术语**只是一个信号**——它说明这句可能难，所以按钮才浮现出来引导用户。**它不是一个"点它查它"的入口。**
>
> **一句话里有几个术语，与功能二无关——永远只有一次。**

**实现纪律**：任何文案、注解、变量命名都不得出现「解释这个术语」「术语深层展开」这类表述。正确表述是「整句理解」。

### 功能二的差异化（三条缺一不可，即其定义本身）

1. **自动携带上下文** —— 用户不必交代这是哪本书、哪一章、前面说了什么
2. **只针对这一句** —— 不解释整段、整章、整本书
3. **不可追问、不延伸** —— 一次性回答，无输入框，无对话历史。**一旦能追问，它就是聊天框**

## 2.4 竞品与差异化

| | 微信读书 AI 问书 | ChatPDF 类 | 划词翻译类 | **Gloss** |
|---|---|---|---|---|
| 阅读体验 | 好 | 差（不是阅读器） | — | 好 |
| **能读你自己的书** | **不能**（限其书库） | 能 | 能 | **能** |
| 交互 | 长按就地问 | 离开正文去聊天 | 划词 | 点句撑开 |
| 输出 | 解释+背景，**故意不直接给答案** | 问答 | 外语翻译 | **白话改写 + 就地整句理解** |
| 术语边界 | 无 | 无 | 无 | **不翻，给入口** |
| 回原文核查 | 可以 | **困难（头号抱怨）** | — | **原句始终并排** |
| 读完留下什么 | 无 | 无 | 无 | **文档留在这里，理解也留在这里** |

> ⚠️ "微信读书不覆盖冷门书"为推断，**上线前应抽样验证**（取 10 本目标用户实际在读的书查覆盖率）。

## 2.5 非目标

| 非目标 | 状态 | 为什么 |
|---|---|---|
| 问答框 / 对话界面 | **永不做** | ChatPDF 形态，会让阅读割裂 |
| 功能二支持追问 | **永不做** | 一旦能追问就变成聊天框，定位崩塌 |
| 全文摘要 / 读书笔记生成 | **永不做** | 摘要是"替你读"，本产品是"帮你读" |
| 社交 / 分享 / 评论 | **永不做** | 与个人阅读空间定位冲突 |
| 古籍文言断句 | **永不做** | 难度级别不同，用户面更窄 |
| 登录注册 | **MVP 不做** → v2 | 注册摩擦；长期阅读器最终需要它（跨设备） |
| 笔记系统 | **MVP 不做** → v2 | **UI 阶段出静态原型** |
| 扫描件 OCR + 纠错 | **MVP 不做** → v2 | 链路长，工时占 4 周的 1/3。**UI 阶段出静态原型** |
| 分页阅读模式 | **MVP 不做** → v2 | 需重做排版引擎 |
| 我的句子总览 / 收藏 | **MVP 不做** → v2 | 需先有留存 |
| 全文搜索 | **MVP 不做** → v2 | MVP 阶段用户最多几本书 |
| 支付 | **MVP 不做** | 个人身份无法接入国内正规支付；只设计不实现 |

---

# 第三部分：具体方案

## 3.1 用户角色

| 角色 | 权限 |
|---|---|
| **访客（唯一角色）** | 全部功能，无需注册。数据仅存本地，仅自己可见 |

## 3.2 使用场景

| # | 场景 | 期望结果 |
|---|---|---|
| S1 | 首次到访 | 10 秒内体验到核心价值，无需上传 |
| S2 | 精读一本难书 | 遇到难句点一下就懂，不中断阅读 |
| S3 | 遇到术语 | 知道这里不翻是有意的，且知道可以深入 |
| S4 | 白话之外还想懂整句 | 一次结合上下文的解释，不超出这一句 |
| S5 | AI 说得不合我意 | 改成自己的话 |
| S6 | 过几天继续读 | 书架里找到它，从上次位置继续 |
| S7 | 想带走 | 导出原文 + 白话对照 |
| S8 | 发现翻错了 | 右键一键报错，不打断阅读 |

## 3.3 功能范围

| ID | 功能 | 优先级 | 周次 |
|---|---|---|---|
| F1 | 文件上传与解析（docx / txt / 粘贴 / 文字层 PDF） | **P0** | W1 |
| F2 | 自动切句（含无标点文本的 AI 断句） | **P0** | W1 |
| F3 | 阅读器（正文渲染 + 阅读位置记忆） | **P0** | W1 |
| F4 | **功能一**：点句 → 撑开出白话 | **P0** | W1 |
| F5 | 三类输出区分（句法简化 / 指代还原 / 术语标注） | **P0** | W2 |
| F6 | 跨会话句子级缓存 | **P0** | W2 |
| F7 | 保存白话（形态可设置） | **P0** | W2 |
| F8 | **功能二**：听不懂（含已用置灰态） | **P0** | W2 |
| F9 | 「翻错了」反馈（右键） | **P0** | W2 |
| F10 | 书架 / 最近文档 | **P0** | W2 |
| F11 | 导出 docx（形态可设置） | **P0** | W3 |
| F12 | 全量埋点 | **P0** | W3 |
| F13 | 内置示例文档 + 首次引导 | **P0** | W3 |
| F14 | 编辑白话（双击） | P1 | W3 |
| F15 | Ctrl 锁定撑开 | P1 | W3 |
| F16 | 覆盖模式 + 查看上下文原文（右键） | P1 | W3 |
| F17 | 导出 PDF | P1 | W3 |
| F18 | 视图切换（书架 ⇄ 列表） | P1 | W3 |
| **F20** | **侧栏收起（沉浸阅读）** | **P1** | **W3** |
| **F21** | **阅读主题（三套浅色）** | **P1** | **W3** |
| F19 | bionic 阅读 | P2 (stretch) | — |

**v2（不在本周期）**：hover 预览已缓存句 ｜ **扫描件 OCR 识别** ｜ **上下文纠错 + 纠错处视觉提示 + 原图悬停** ｜ 笔记系统 ｜ 登录与跨设备同步 ｜ 分页阅读模式 ｜ 我的句子总览 ｜ 全文搜索 ｜ **夜读模式** ｜ **侧栏收起后的边缘唤出**

> **纪律**：任何新增 P0 必须同时砍掉一个等量项，或延长周期。

## 3.4 业务流程

```
打开网页
   ├─ 有最近文档 → 书架 → 选一本 → 恢复上次位置
   └─ 无         → 上传 / 粘贴 / 试试示例
                        ↓
                 解析 → 校验（扫描件？超限？）→ 异常提示（3.9）
                        ↓
                 切句 + 结构提取（无标点 → AI 断句）
                        ↓
                 ┌───── 阅读器 ─────┐
                 │                  │
            点击句子                 │
                 ↓                  │
          缓存命中？                 │
          ├ 是 → 秒出                │
          └ 否 → 功能一（快模型，流式）│
                 ↓                  │
          撑开（气泡样式，推动下文）    │
                 ↓                  │
     ┌────┬──────┼──────┬────┬─────┤
   保存  编辑  听不懂  右键  收起 ────┘
     │          （条件显示）  │
     │             ↓        ├ 翻错了
     ↓        功能二（强模型）├ 查看上下文原文
  常驻 inline    每句一次     └ 听不懂
     ↓        用后按钮置灰
  导出 docx/PDF  结果常驻不失
```

## 3.5 页面流程

| 页面 | 路由 | 说明 |
|---|---|---|
| P1 书架 | `/` | 书脊排列 + 空态虚线书（上传入口）+ 示例书 |
| P2 处理中 | 页内状态 | 进度条 + 阶段文案 |
| P3 阅读器 | `/read/:docId` | 三栏。撑开态在此页内 |
| M1 设置面板 | 右栏常驻 | 呈现模式、保存形态、导出形态、字号、行距、纸面底色 |
| M2 右键菜单 | 浮层 | 听不懂 / 翻错了 / 查看上下文原文 |

**首次访问**：`/`（空书架）→ 点示例书 → `/read/sample`

## 3.6 功能详细说明

### F4 功能一：点句出白话

**触发**：单击句子
**输出**：该句下方**撑开**（占据布局空间、推动下文），视觉上是气泡样式，**不遮挡任何内容**。

| 约束 | 规则 |
|---|---|
| 长度 | 原句的 50%–80%；**绝对上限 150 字** |
| 不加信息 | 不补背景、不做评论、不引导思考、不延伸 |
| 术语不翻 | 保留原词 + 视觉标记。**不做"为什么不翻"的文字说明** |
| 原句可见 | 撑开区与原句同屏 |
| **稳定性** | **同一句在同一会话内白话固定不变，重复点击不重新生成、不重复计费** |
| **术语不折行** | 术语整词 `white-space: nowrap` |

**三类输出**：

| 类型 | 判定 | 处理 |
|---|---|---|
| 句法简化 | 长句、倒装、嵌套 | 拆短理顺 |
| 指代还原 | 含代词或回指 | 补上所指 |
| **术语标注** | 含专有概念 | **保留原词 + 灰蓝标记**，不翻，不解释为什么不翻 |

> v1.1 曾有第四类「知识缺口标注」，**v1.2 已砍**——产品无法判断用户是否具备某项前置知识，那是替用户做假设，且必然经常判断错。由用户点「听不懂」来表达。

### F8 功能二：听不懂

**语义见 2.3 的定义框。以下是交互规则。**

**条件显示（v1.2 新增）**：

| 情况 | 操作行 | 右键菜单 |
|---|---|---|
| **白话中含术语标记** | **显示「听不懂」** | 也有 |
| 白话中无术语 | **不显示** | 有 |

> **设计说明**：不常驻、不硬藏——**在最可能需要的那一刻出现**。术语的存在是"这句可能难"的信号。
>
> **副作用与缓解**：功能二的发现率因此挂在术语出现频率上。缓解办法在 F13——**内置示例文档的第一个难句必须含术语**，让用户第一次使用时就建立认知，之后即便隐藏也知道右键里有。

**按钮标签固定为「听不懂」三字**，不带术语名、不带箭头。

**已用状态（v1.2 新增）**：

| | 规则 |
|---|---|
| 一句用过一次后 | 「听不懂」按钮**置灰禁用**，可见但不可点 |
| 结果 | **常驻在撑开区内** |
| 收起后再打开 | **结果仍在** |
| 重新生成 | **永不**。不重复调用、不重复计费 |

**内容约束（即功能二的定义）**：

| 约束 | 规则 |
|---|---|
| 长度 | **≤250 字** |
| 携带上下文 | 自动带前后文与全书结构摘要 |
| 范围 | 只解释这一句 |
| 禁止延伸 | 不得出现"此外""值得一提的是""这让我们想到"等扩展句式 |
| **不可追问** | 一次性回答，**无输入框、无对话历史** |

### F7 保存 / F14 编辑

| 能力 | 行为 |
|---|---|
| 保存 | 撑开态转为常驻。形态可设置：① 气泡常驻 ② 更小更浅的 inline 小字（默认） |
| 取消保存 | 再次点击保存 |
| **编辑（P1）** | **双击撑开区**进入编辑；保存后以编辑版本为准 |

> **编辑的价值**：用自己的话重述就是理解发生的那一刻。`gloss_edit` 是比「翻错了」细腻得多的质量信号——编辑率和编辑幅度说明 AI 的白话离用户自己的理解有多远。

### F15 锁定撑开（v1.2 修订）

| | 规则 |
|---|---|
| 触发 | **仅 Ctrl + 单击**。界面上**没有任何可点的锁定控件** |
| 解除 | **再次 Ctrl + 单击同一句** |
| **唯一视觉信号** | **撑开区外边框轻微加深**。无图钉、无徽章、无图标、无文字标签 |
| 发现性 | 操作行末尾一行极淡的灰字提示「Ctrl + 单击锁定」——**是提示不是按钮** |
| 行为 | 点击别处 / 该句滚出视口 均不收起；**多句锁定可并存** |

### F11/F17 导出

**形态可设置**（纸面上不存在"浮窗"，故提供四种纸面形态）：

| 形态 | 样子 |
|---|---|
| **行下小字**（默认） | 白话紧跟原句，更小更浅 |
| 批注框 | 白话置于带框的批注块中 |
| 脚注 / 尾注 | 原句加角标，白话统一放页脚或文末 |
| 双栏对照 | 左栏原文、右栏白话 |

**分页规则**：**带白话的句子必须保证整句 + 其白话不跨页**。

### F10 书架

书脊横向排列，宽窄错落。选中的书凸出为完整封面，**页面背景色跟随该书**。

| | 规则 |
|---|---|
| 书脊颜色 | 默认随机，**右键可自定义** |
| 书脊宽度 | 仅为错落美观服务，**不编码任何信息** |
| 阅读进度 | **不显示** |
| 空态 | **一本浅灰虚线书 = 上传入口** + 若干**示例书**（明确标记为示例） |
| 视图切换 | 书架 ⇄ 列表（P1） |

> **示例书 ≠ 推荐书。** 推荐 = 平台在给你内容（与"不做书库"冲突）；示例 = 产品在教你怎么用。

### F20 侧栏收起 · 沉浸阅读（v1.3 新增）

| | 规则 |
|---|---|
| 范围 | **左右两栏可独立收起**——有人想留目录不要设置，有人反过来 |
| 触发 | 快捷键 `Cmd/Ctrl + \`；栏边缘一个**极轻的折叠把手**，hover 才显现 |
| **中栏宽度** | **保持不变，仅居中。两侧留白增加** |
| 状态 | 记住（localStorage），下次打开保持 |
| v2 | 收起后鼠标移到屏幕边缘 → 侧栏临时滑出 |

> ### ⚠️ 收起侧栏时中栏不得变宽
>
> 若中栏跟随变宽，行长会从约 38 字涨到 60+ 字。**长行在中文里显著更难读**——眼睛回行时容易串行。
> 收起侧栏换来的是**视觉清净**，不是**更宽的文字**。
>
> measure 是排版的核心参数，不因布局变化而漂移。

### F21 阅读主题（v1.3 新增）

三套**浅色**主题，默认「羊皮纸」。**夜读模式划入 v2**。

> ### ⚠️ 每个主题是一整套 token，不是换一个背景色
>
> 背景变了，正文墨色、白话色、术语色、分隔线**全部要跟着换**。
> 若只替换 `--paper`，绿色底上那套暖褐色的分隔线与术语底会显脏。
>
> 完整 token 表见 **3.10**。

### F13 内置示例文档 + 首次引导

**不做 tooltip 教程。** 做法：示例文本中**第一个难句自带轻微呼吸/高亮动效**，用户本能点击 → 一次点击理解全部交互。

**⚠️ 该难句必须含术语标记**（见 F8 的副作用缓解）。

**版权方案**（按稳妥度）：

1. **最稳**：取公有领域外文原文，**自行生成新中译**作为示例
2. 较稳：译者去世超 50 年的旧译本（多为文言，与定位不完全贴合）
3. 有风险：现行译本节选，依"合理使用"

> ⚠️ 马克思原文在公有领域，但**中文译本有译者/编译机构著作权**，不可直接使用。以上非法律意见，落地前需自行核实。
> **原型与设计稿中的「版本：中共中央编译局本」等署名文案不得带入产品。**

## 3.7 交互规则

### 核心交互

```
hover 句子  → 轻微变色（可点提示）。不触发生成、不推动内容
单击句子    → 该句下方撑开气泡，白话流式填充（3–5 字块），下方内容平滑下移
```

> **两条技术要求，W1 必须验证**
> ① **撑开时锚定视口**——点击行位置不变，只有下方内容下移
> ② **点击时自动微调滚动**，保证整句 + 撑开区完整可见

### 操作清单

| 操作 | 行为 |
|---|---|
| hover 句子 | 轻微变色 |
| 单击句子 | 撑开 / 再次单击收起 |
| 点击页面其他位置 | 收起 |
| 该句滚出视口 | 收起 |
| 单击另一句 | 前一句收起（**同时只允许一个非锁定撑开态**） |
| **Ctrl + 单击** | 锁定 / 解锁 |
| **双击撑开区** | 进入编辑（P1） |
| **右键** | 菜单：听不懂 / 翻错了 / 查看上下文原文（后者仅覆盖模式下出现） |
| Esc | 收起；编辑中则退出编辑 |

### 操作行内容（v1.2 收敛）

| 位置 | 内容 |
|---|---|
| **撑开区操作行** | 保存 · 编辑 · **听不懂（仅当有术语）** · 收起 |
| **右键菜单** | 听不懂 · 翻错了 · 查看上下文原文 |

> **视觉权重**：操作行内**没有主按钮**，四项权重相当。这是阅读工具，操作行不应与正文争夺注意力。

### 呈现模式（设置项）

| 模式 | 说明 | 优先级 |
|---|---|---|
| **撑开**（默认） | 气泡在行下方，不遮挡 | P0 |
| 覆盖 | 白话替换原句位置，白底区分；**hover 到白话上临时显示原句**；右键可「查看上下文原文」 | P1 |
| hover 预览 | 已缓存句 hover 显示白话前 15–20 字 | P2 (v2) |

## 3.8 数据规则

| 数据 | 位置 | 内容 | 保留 |
|---|---|---|---|
| 上传的原文 | **浏览器内存 + localStorage** | 全文 | **服务端不持久化原文** |
| 已保存白话 | localStorage | 句 hash → 白话（含编辑版本） | 用户清除即消失 |
| 功能二结果 | localStorage | 句 hash → 整句理解 | 同上，**永不重新生成** |
| 阅读位置 | localStorage | doc_id → 句序号 | 同上 |
| **句子缓存** | 服务端 KV | **句 hash + 上下文指纹 → AI 原始白话**。不含原文明文 | 生成即写入，永不主动删 |
| 埋点 | 服务端 | 见 4.1 | — |
| 「翻错了」上报 | 服务端 | 原句 + 白话 + 句 hash | 用于 prompt 迭代 |

> **关键规则：用户编辑过的白话只存本地，绝不写入共享缓存。** 编辑版本是个人理解，不是通用改写。

### 缓存原则（用户已确认）

> **不共享用户数据、不共享文档、不可检索他人内容；只复用对相同文本片段的处理结果。**

命中条件：句子 hash **完全一致** 且 上下文指纹一致。

> **定位修正**：这是**跨会话缓存**，跨用户命中是附带收益。主要价值是同一用户重读同一本书时的瞬时响应与零成本——而重读正是本产品的核心场景。**论证不依赖用户规模。**

### 额度规则（W1 实测成本后校准）

| 限制 | 阈值 | 超出后 |
|---|---|---|
| 单文档字数 | 50,000 字 | 拒绝，提示分卷 |
| 每日点击（功能一） | 100 次 | 提示明日恢复；**已缓存句不计入** |
| 每日点击（功能二） | 20 次 | 同上 |
| 单文件大小 | 20 MB | 拒绝 |

## 3.9 异常情况

### A. 文件与解析

| # | 情况 | 处理 |
|---|---|---|
| A1 | 格式不支持 | 提示支持清单 + 「改用粘贴文本」入口 |
| A2 | 文件 >20 MB | 提示上限，建议分卷 |
| A3 | 文件损坏 | 提示"无法读取" + 粘贴入口 |
| A4 | 加密 PDF | 提示需先解除密码 |
| A5 | **扫描件 PDF** | **明确含"扫描件"三字** + 原因 + v2 计划。不可显示乱码 |
| A6 | **混合型 PDF** | 提取可提取部分，**横幅告知"第 X–Y 页为扫描页，已跳过"** |
| A7 | 空文件 | 提示"未检测到文本内容" |
| A8 | 超字数上限 | 拒绝，提示分卷 |
| A9 | 非中文文本 | **允许但提示"针对中文优化"**，不阻断 |
| A10 | 含表格/公式/脚注 | 剔除表格与公式块；脚注单独归类不参与切句 |

### B. 切句

| # | 情况 | 处理 |
|---|---|---|
| B1 | **无标点 / 乱换行** | **触发 AI 断句**（仅此类文本）。不按字数硬切。横幅提示"已自动断句，可能不准确" |
| B2 | 超长句（>250 字） | 在句中标点处切分。**实测 0 句，属安全网** |
| B3 | 引号内含句号 | 识别成对引号/书名号，不在其内部切分 |
| B4 | 省略号、破折号、分号 | 分号视为句末；省略号与破折号不是 |
| B5 | 空白段落 | 归一化，不产生空句 |
| B6 | AI 断句失败 | 回退到规则切分并提示 |

### C. AI 调用

| # | 情况 | 处理 |
|---|---|---|
| C1 | 超时（>15s 无首字） | "生成超时" + 「重试」。**不自动重试** |
| C2 | API 错误 | "暂时无法生成" + 「重试」。**详情入埋点，不给用户看** |
| C3 | 限流 | "请求有点多，稍后再试"，5 秒后可重试 |
| C4 | 返回为空 | 同 C2 |
| C5 | **返回超长** | **服务端截断 + 埋点** `gloss_overlength`。连续超长 = prompt 退化信号 |
| C6 | 模型拒答 | "这一句无法处理" + 右键「翻错了」入口 |
| C7 | **幻觉** | **无法自动检测。** 依靠 ① 原句并排 ② 人工上报 ③ 20 句评测集回归 |
| C8 | 返回含 markdown | 前端清洗，只渲染纯文本与预定义标记 |

### D. 网络与会话

| # | 情况 | 处理 |
|---|---|---|
| D1 | 断网 | 顶部持久提示；已缓存内容可看；点击新句时提示不可用 |
| D2 | 弱网流式中断 | 保留已接收部分 + 「继续」 |
| D3 | 生成中收起 | **中止请求**，不计费不缓存；埋点 `gloss_abort` |
| D4 | 生成中离开页面 | 同 D3 |
| D5 | 页面刷新 | 恢复文档、已保存白话、**功能二结果**、阅读位置；临时撑开态不恢复 |
| D6 | 多标签页 | 以最后写入为准；不做冲突解决 |

### E. 本地存储

| # | 情况 | 处理 |
|---|---|---|
| E1 | localStorage 已满 | 提示 + 导出入口 + 删除旧文档入口 |
| E2 | localStorage 被禁用 | 提示"当前模式下无法保存，请及时导出"，**功能不阻断** |
| E3 | 用户清空数据 | 数据丢失。读完时主动提示导出 |

### F. 额度

| # | 情况 | 处理 |
|---|---|---|
| F1 | 超出每日额度 | "明天恢复"；**已缓存句不计入，仍可点** |
| F2 | 剩余 ≤10 | 顶部轻提示，不打断 |

### G. 交互边界

| # | 情况 | 处理 |
|---|---|---|
| G1 | 快速连点同一句 | 300ms 去抖 |
| G2 | 点击已撑开的句子 | 收起 |
| G3 | 撑开中滚动 | **仅当该句滚出视口才收起** |
| G4 | 极窄屏 / 移动端 | 撑开区全宽；hover 降级为无；左右栏收进抽屉 |
| G5 | 用户选中文本 | **不触发撑开**。仅无选区时的 click 才触发 |
| G6 | 句子被视口截断 | **点击时自动微调滚动** |
| G7 | 编辑中点击别处 | 提示未保存，提供保存/放弃 |
| G8 | 编辑后内容为空 | 视为删除该白话 |
| G9 | 多个锁定态 | 允许并存；非锁定态仍只允许一个 |
| **G10** | **点击已置灰的「听不懂」** | 无反应。**但记录埋点**——用户想再要一次是个真实信号 |
| **G11** | **术语位于行末** | `white-space: nowrap`，整词移至下一行，不折断 |

### H. 导出

| # | 情况 | 处理 |
|---|---|---|
| H1 | 无已保存白话时导出 | 提示，询问是否仅导原文 |
| H2 | 导出失败 | 提示重试；大文档提供分段导出 |
| H3 | 超大文档超时 | 分块生成 + 进度提示 |
| H4 | 分页时白话跨页 | 整句 + 白话作不可分割块，必要时整体移至下一页 |

## 3.10 视觉规范（v1.3 定稿）

### 色彩 · 三套主题

**每个主题是一整套 token。切换主题 = 切换整张表，不是只换 `--paper-main`。**

| Token | 用途 | **羊皮纸**（默认） | **月白** | **护眼绿** |
|---|---|---|---|---|
| `--paper-main` | **中栏底色** | `#F8F6F0` | `#F5F5F7` | `#EDF2EA` |
| `--paper-side` | **侧栏底色** | `#F1EEE6` | `#EAEAEE` | `#E3E9DF` |
| `--ink` | 正文墨色 | `#212121` | `#212121` | `#223027` |
| `--gloss` | 白话（同字号仅色浅） | `#5D5750` | `#5A5A61` | `#4F5D50` |
| `--rule` | 分隔线、边框 | `#E5DFC8` | `#E2E2E8` | `#D5DFD0` |
| `--term` | 术语（降饱和冷调） | `#3F4B53` | `#3F4B53` | `#37505A` |

> **层次关系：中栏最亮，侧栏退后一档。**
> 中栏是主角，**不得压暗中栏来制造对比**。三套主题的中栏/侧栏明度差为 6.6% / 8.9% / 7.5%。
> 不给中栏加阴影——在这种克制的设计里，阴影会显得突兀。

**对比度校验（全部通过 WCAG AA 4.5:1）**

| 主题 | 正文/中栏 | 白话/中栏 | 术语/中栏 | 正文/侧栏 |
|---|---|---|---|---|
| 羊皮纸 | 14.9:1 | 6.6:1 | 8.3:1 | 13.9:1 |
| 月白 | 14.8:1 | 6.3:1 | 8.2:1 | 13.4:1 |
| 护眼绿 | 12.2:1 | 6.1:1 | 7.5:1 | 11.2:1 |

> **白话是对比度最低的一档（6.1:1）**，任何进一步调浅都必须重新校验。

### 字体

| 角色 | 字体 |
|---|---|
| 正文 | **Noto Serif SC** |
| 展示 / 字标（仅拉丁） | Cinzel |
| 工具 / 参数 / 元信息 | JetBrains Mono |

### 排版

| 项 | 值 |
|---|---|
| 正文字号 | **16.5px**（可调 15–19） |
| 行高 | **2.05**（可调 1.8–2.4） |
| 字距 | 0.025em |
| 对齐 | **`text-align: justify` + `text-justify: inter-ideograph`** |
| 中栏宽度 | ≤750px |

### 组件规则

| 组件 | 规则 |
|---|---|
| **原句（激活态）** | **不改变任何属性**——不加底色、不加边框、不改字重、不改位置 |
| 白话块 | 不加框、不加底色。**仅一条左竖线** |
| 术语标记 | 冷调、`nowrap`、与正文同字号 |
| 操作行 | 全描边按钮，**无主按钮**，标签 `nowrap` |
| 锁定态 | **仅外边框加深** |
| 流式填充 | **3–5 字块**逐步吐出，不逐字 |
| **侧栏收起** | **中栏宽度不变，仅居中**；折叠把手 hover 才显现 |
| **主题切换** | 切换整张 token 表；过渡用 `transition` 而非闪变 |

### 实现注意（来自设计稿的已知问题）

- 左栏最小宽度需容纳「复习态」等标签**单行显示**
- 已保存条目摘要保持**两行**（`line-clamp-2`）
- 两端对齐易产生**句末孤字**，可用 `text-wrap: pretty` 或微调 measure 缓解
- 设计稿使用 Tailwind Play CDN，**不可用于生产**，W1 需替换为真实构建或手写 CSS

---

# 第四部分：埋点与验证

## 4.1 埋点清单

| 事件 | 参数 |
|---|---|
| `page_view` | 路由、是否首访 |
| `shelf_book_click` | doc_id、距上次打开天数 ← **G3 回访度量** |
| `sample_doc_enter` | — |
| `doc_upload_attempt` | 格式、大小 |
| `doc_upload_reject` | **拒绝原因（A1–A9 分类）** ← 异常统计核心 |
| `doc_parse_complete` | 字数、句数、耗时、是否触发 AI 断句 |
| `reader_enter` | doc_id、来源、是否恢复位置 |
| `sentence_hover` | 句序号（采样） |
| `sentence_click` | 句序号、句长、**是否命中缓存** |
| `gloss_first_token` | **首字延迟 ms** ← G4 |
| `gloss_complete` | 耗时、字数、**输出类型（三类）** |
| `gloss_overlength` | 实际字数 ← prompt 退化预警 |
| `gloss_read_complete` | 停留时长、是否滚动到底 ← **G1 北极星** |
| `gloss_dismiss_early` | 停留 <2s |
| `gloss_abort` | 中止阶段 |
| `gloss_fail` | **失败类型（C1–C6）** ← 异常统计核心 |
| `sentence_reclick` | 第 N 次、**是否已锁定** ← 交互摩擦信号 |
| `deep_explain_click` | **触发来源（术语行内 / 右键）** |
| **`deep_explain_blocked`** | **点击已置灰按钮** ← G10，"还想再要一次"的信号 |
| `report_error_click` | 句 hash + 原句 + 白话 |
| `context_view_click` | — |
| `gloss_edit` | **编辑前后字数、差异度** ← 最细腻的质量信号 |
| `gloss_save` | 句序号、是否为编辑版本 |
| `lock_expand` | 锁定 / 解锁 |
| `doc_export` | 格式、形态、白话条数 |
| `quota_exceeded` | 类型 |
| `session_end` | 时长、点击总数、阅读进度 |

## 4.2 核心指标

### 领先指标

| 指标 | 目标 |
|---|---|
| 示例转化率（首访 → 进入阅读器） | ≥70% |
| 首句点击率 | ≥60% |
| **白话读完率（北极星）** | **≥60%** |
| 首字延迟 P90 | ≤2.5s |
| 上传成功率 | ≥85% |

### 滞后指标

| 指标 | 目标 |
|---|---|
| 白话保存率 | ≥30% |
| **7 日回访率** | **≥25%** |
| 缓存命中率 | ≥30% |
| 单本完读率 | ≥25% |
| 文档导出率 | **观察项，不设阈值** |

### 中性指标（只观察）

| 指标 | 说明 |
|---|---|
| **功能二使用率** | 用户读的书术语多，使用率自然高。**不是好坏信号** |
| 编辑率 | 高可能是 AI 不合口味，也可能是深度参与。需结合定性访谈 |
| `deep_explain_blocked` 频次 | 反映"一句一次"这条限制是否合理 |

### 反向指标（变好 = 产品变坏）

| 反向指标 | 阈值 | 为什么 |
|---|---|---|
| **单句重复点击率** | >20% | **交互摩擦信号，不是质量信号**（白话固定不变）。须与 `lock_expand` 一起判读 |
| **白话平均字数** | 环比 +15% | 越写越长 = 退化成"解释" |
| `gloss_overlength` 率 | >5% | prompt 退化 |
| `gloss_dismiss_early` 率 | >30% | 白话没被读 |

## 4.3 AB 实验

**MVP 用户量（10–50 人）不足以跑统计显著的 AB。**

- W4 采用**前后对比 + 定性访谈**
- 唯一轻量实验：白话长度（原句 50% vs 80%），分桶比较读完率
- Case Study 中**如实说明样本量不支持显著性结论**

> 明确写出"我知道这个样本量得不出显著结论"，比编一个 p 值更专业。

## 4.4 验收标准

### F4 功能一

- [ ] **Given** 在阅读器中，**When** 单击任意句子，**Then** 该句下方 300ms 内出现撑开区，且**点击行的视口位置不变**
- [ ] **Given** 句子部分超出视口，**When** 单击，**Then** 自动微调滚动，整句 + 撑开区完整可见
- [ ] **Given** 白话已生成，**Then** **原句的底色、边框、字重、位置均未发生任何改变**
- [ ] **Given** 未命中缓存，**Then** 首字 P90 ≤2.5s，以 3–5 字块流式填充
- [ ] **Given** 已命中缓存，**Then** 300ms 内完整显示
- [ ] **Given** 生成的白话，**Then** ≤150 字且 ≤原句 80%
- [ ] **Given** 同一句被多次点击，**Then** 白话**完全相同**且**不产生新的 AI 调用**
- [ ] **Given** 术语位于行末，**Then** 整词移至下一行，**不折断**
- [ ] **Given** 用户已选中文本，**When** 松开鼠标，**Then** **不触发**撑开

### F8 功能二

- [ ] **Given** 白话中**有**术语标记，**Then** 操作行**显示**「听不懂」
- [ ] **Given** 白话中**无**术语标记，**Then** 操作行**不显示**「听不懂」，但右键菜单中**有**
- [ ] **Given** 用户第 2 次点击同一句，**Then** **不**出现任何额外的功能二入口
- [ ] **Given** 触发功能二，**Then** 输出 ≤250 字，**不含**延伸句式，界面中**不存在**任何输入框
- [ ] **Given** 某句已用过功能二，**Then** 该句的「听不懂」**置灰不可点**
- [ ] **Given** 收起该句撑开区后重新打开，**Then** 功能二结果**仍在**，且**未产生新的 AI 调用**

### F15 锁定

- [ ] **Given** 撑开区打开，**Then** 界面中**不存在**任何锁定/解锁按钮或图钉
- [ ] **Given** Ctrl + 单击，**Then** 撑开区**外边框加深**，除此之外无任何视觉变化
- [ ] **Given** 已锁定，**When** 点击别处 / 该句滚出视口，**Then** **不收起**
- [ ] **Given** 已锁定，**When** 再次 Ctrl + 单击同一句，**Then** 恢复普通态
- [ ] **Given** 多句被锁定，**Then** 可并存

### F14 编辑

- [ ] **Given** 双击撑开区，**Then** 进入可编辑状态
- [ ] **Given** 编辑并保存，**Then** 后续显示编辑版本，且**编辑版本不写入服务端共享缓存**
- [ ] **Given** 编辑后为空，**Then** 视为删除该白话

### F11 导出

- [ ] **Given** N 条已保存白话，**When** 导出 docx，**Then** 含完整原文，白话按设置形态呈现于对应位置
- [ ] **Given** 分页格式，**Then** 带白话的句子与其白话**不跨页**

### 异常路径

- [ ] 3.9 中 **A1–A10、B1–B6、C1–C6、D1–D5、E1–E3、G1–G11** 每条均有用户可见提示，**无任何静默失败**
- [ ] 上传扫描件时提示明确包含"扫描件"与 v2 计划
- [ ] 断网后点击句子，出现明确提示而非无限加载

## 4.5 非功能需求

| 类别 | 要求 |
|---|---|
| **性能** | 首屏 ≤2s；切句 5 万字 ≤3s；撑开动画 ≥50fps |
| **延迟** | 首字 P50 ≤1.5s / P90 ≤2.5s；缓存命中 ≤300ms |
| **兼容性** | Chrome / Edge / Safari 最近两个大版本 |
| **响应式** | ≥1024px 主场景；768–1024px 可用；<768px 降级 |
| **可访问性** | 全键盘可操作（Tab 选句、Enter 撑开、Esc 收起）；白话与原文对比度 ≥4.5:1 |
| **隐私** | 服务端不持久化原文；缓存只存白话与 hash；**编辑版本不上传**；「翻错了」含原句需明示告知 |
| **成本** | 功能一单次 ≤¥0.01；功能二 ≤¥0.05（W1 实测后校准） |
| **构建** | **不得使用 Tailwind Play CDN**（设计稿用的是它，生产必须替换） |

---

# 第五部分：版本计划

## 5.1 开发排期

| 周 | 开发目标 | 可验证交付 |
|---|---|---|
| **W1** | F1–F4 | 上传 → 切句 → 点击 → 撑开出白话端到端可用；**撑开锚定视口 + 自动微调滚动已验证** |
| **W2** | F5–F10 | 三类输出可区分；缓存可观测；保存、功能二（含置灰态）、书架可用 |
| **W3** | F11–F13（P0）+ F14–F21（P1） | 导出可用；埋点全量；示例文档与引导完成 |
| **W4** | **不写新功能** | ≥10 人试用；数据复盘；**一轮有数据支撑的迭代** |

### ⚠️ W3 超载风险（v1.3 标注）

W3 现有 **3 个 P0 + 8 个 P1**，共 11 项。**大概率做不完。**

**P0 必须完成**（F11 导出 docx / F12 全量埋点 / F13 示例文档与引导）——少了任何一个，W4 的数据复盘就无法进行。

**可推迟的 P1，按推迟优先级排序：**

| 顺序 | 项 | 推迟代价 |
|---|---|---|
| 1 | F17 导出 PDF | 低。docx 已覆盖主要场景 |
| 2 | F18 视图切换（书架⇄列表） | 低。MVP 阶段书少，书架够用 |
| 3 | F16 覆盖模式 | 中。默认撑开模式已完整可用 |
| 4 | F21 阅读主题 | 中。默认羊皮纸可用，但这是易感知的加分项 |
| 5 | F20 侧栏收起 | 中。同上 |
| 6 | F15 锁定 / F14 编辑 | **较高。编辑是"理解发生"的核心动作，锁定影响长白话的可读性** |

> **纪律：W4 不允许被挤占。** 进度落后时从上表第 1 项开始砍，不动 W4。

## 5.2 设计产出（并行，不占开发工时）

| 产出 | 状态 |
|---|---|
| 低保真线框（全状态矩阵） | ✅ 已完成 |
| 阅读页高保真视觉 | ✅ 已完成（3.10 即其产物） |
| 书架页高保真视觉 | 待做 |
| 扫描件纠错静态原型 | 待做 |
| 笔记系统静态原型 | 待做 |

> ⚠️ 作品集中静态原型必须标注「设计原型，未实现」。

## 5.3 作品集交付节点

| 时间点 | 可用于简历/作品集 |
|---|---|
| **现在** | PRD + 竞品分析 + 中译中样例 + **全状态线框 + 高保真视觉** |
| **W1 末** | **可交互 demo 链接** |
| W2 末 | 完整 MVP |
| W3 末 | 完整体验 + 设计原型 |
| W4 末 | **数据驱动迭代叙事 + Case Study** |

> **W4 不允许被 W1–W3 的延期挤占。进度落后时砍 W3 的 P1 项，不砍 W4。**

---

## 6. 开放问题

### 已解决

- ✅ 白话质量能否被专业读者接受 → 能
- ✅ 保存动机 → 复习
- ✅ 交互模型 → click + 撑开
- ✅ 缓存原则 → 已确认
- ✅ 扫描件 → v2 + 静态原型
- ✅ 载体 → 网页
- ✅ 功能二语义 → **整句一次，术语只是引导**
- ✅ 听不懂显示规则 → **有术语行内显示，否则只在右键**
- ✅ 功能二已用状态 → **置灰，结果常驻**
- ✅ 锁定视觉 → **仅边框加深，无按钮**
- ✅ 中文字体 → **Noto Serif SC**
- ✅ 视觉规范 → 见 3.10
- ✅ 侧栏收起 → **做，P1；中栏宽度不变仅居中**
- ✅ 阅读主题 → **三套浅色，夜读划入 v2**
- ✅ 侧栏与中栏层次 → **侧栏退后一档，不压暗中栏**

### 待决策

| # | 问题 | 归属 |
|---|---|---|
| 1 | **白话语气：中性客观 vs 更口语** | 产品（**阻塞 AI prompt 编写**） |
| 2 | 内置示例文档的具体文本与版权路径 | 产品 / 自行核实 |
| 3 | 术语第二次出现是否仍标记 | 产品 |
| 4 | 点击粒度是否支持半句 | 产品（MVP 建议仅整句） |
| 5 | 中文产品名 | 产品 |
| 6 | 额度阈值最终数值 | W1 实测成本后定 |

### 待用户产出

| # | 事项 |
|---|---|
| 7 | **20 句人工评测集** —— 覆盖不同难度类型，是 F5 验收的前提。**没有第二个人能做** |

### 待访谈验证

| # | 问题 | 影响 |
|---|---|---|
| 8 | 其他读者是否也在用"复制整章给 AI"的土办法 | 1.2 的核心论证 |
| 9 | 他们保存白话的动机是否也是复习 | F7/F11 优先级 |
| 10 | **微信读书书库对目标用户在读书籍的覆盖率** | **2.4 竞品结论的验证** |
| 11 | 他们能接受多长的白话 | 3.6 长度约束 |
| 12 | **他们会不会回来** | **2.1 定位的验证** |

---

## 7. 关键设计决策（备查）

| # | 决策 | 理由 | 代价 |
|---|---|---|---|
| D1 | 结构预处理 + 单句按需生成 | 多数句子无人点击 | 首次点击有延迟 |
| D2 | **不要求用户等待** | **阅读连贯性 > 即时性。** 费曼 App 失败的根因不是 AI 慢，是产品要求用户停下来等 | — |
| D3 | 跨会话缓存 | 重读同一本书命中率接近 100%，而重读正是核心场景。**论证不依赖用户规模** | 跨用户命中率低 |
| D4 | 小请求上下文窗口 | 更快更便宜；顺手解决用户手动复制整章的原始痛点 | 上下文精度略降 |
| D5 | **假定 AI 会出错，把验证成本降到接近零** | **LLM 一定会出错，"杜绝错误"无法验收、必然被证伪** | 需原句并排、报错入口 |
| D6 | MVP 用导出替代账号体系 | 避免注册摩擦；省下账号工时 | 跨设备无法同步 |
| D7 | 模型分层（一快二强） | 改写是受约束任务；同时省钱提速 | 两套调用链 |
| D8 | 内置示例兼首次引导 | 10 秒体验到核心价值；对作品集可达性是决定性的 | 需处理译本版权 |
| D9 | 撑开而非浮窗，视觉为气泡 | 浮窗会遮住上一行；撑开占据布局空间 | 需锚定视口防跳动 |
| D10 | **功能二是第二核心，不是逃生口** | 读难书时遇到术语是常态不是异常 | 必须靠三条硬约束守住与通用 AI 的差异 |
| D11 | **不猜用户的知识缺口** | 产品无法判断用户懂不懂某项前置知识，猜必然经常错 | 少一类输出 |
| D12 | **用户编辑版本不入共享缓存** | 编辑是个人理解，不是通用改写 | 编辑内容无法跨设备 |
| **D13** | **状态由"新增的东西"承担，不由"改变已有的东西"承担** | 点击后原句若加底色/加粗，视觉上会被**抬离正文**——与"不打断阅读"直接冲突。正确的表达是原句一动不动，只有下方多出一块白话 | 状态的可感知性略弱，靠撑开本身的存在感补偿 |
| **D14** | **功能二条件显示（有术语才在行内出现）** | 不常驻、不硬藏，在最可能需要的那一刻出现 | 发现率挂钩术语频率，靠示例文档首句含术语缓解 |
| **D15** | **收起侧栏时中栏宽度不变，仅居中** | measure 是排版的核心参数。跟随变宽会让行长涨到 60+ 字，**中文长行显著更难读**——为专注而做的功能反而伤了阅读 | 沉浸感稍弱于"文字铺满屏幕"，但那本来就是错的方向 |
| **D16** | **主题是整套 token，且中栏最亮侧栏退后** | 只换背景色会让其余暖色元素在绿底上显脏；而压暗中栏来制造对比是本末倒置——**中栏是主角** | 三套主题各需一张完整 token 表，维护成本上升 |

---

## 8. 自查

**老板视角 — 看得懂为什么值得做吗？**
✅ 1.2 量化损失点，2.4 论证竞品缺口，2.2 给出可度量目标。
⚠️ 如实标注：**用户样本仅 1 人**；竞品覆盖率待验证；**长期阅读器定位零数据支持**。

**研发视角 — 各种情况都处理完了吗？**
✅ 3.9 列出 8 类 45 条异常；3.10 给出完整视觉 token；4.4 要求"无任何静默失败"。
⚠️ C7（幻觉）明确承认无法自动检测，依靠三道人工防线。

**算法/数据视角 — 上线后能验证效果吗？**
✅ 4.1 覆盖全漏斗与全部失败原因分类；4.2 定义领先/滞后/中性/反向四类指标。
⚠️ 4.3 如实说明样本量不支持显著性 AB。
⚠️ `sentence_reclick` 无法区分"没看懂"和"不会锁定"，**必须结合 `lock_expand` 判读，单独看会误导**。
