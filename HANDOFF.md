# Gloss 交接文档（给 Codex）

> 写于 2026-09-18，由 Claude Code 根据仓库、KANBAN.md 与本轮会话记录整理。
> 只写事实。拿不准的地方标「不确定」并说明原因。
> 数字都注明是**实测**还是**估算**，以及时间。
> **本文件与 KANBAN.md 冲突时，以 KANBAN.md 为准**（见第 7 节第 1 条）。
>
> 本文件提交在分支 `docs/handoff` 上，这个分支从 main（`4781e5c`）拉出。
> 第 3–5 节提到的 G-08 代码（术语标记、`--hover`、gloss-v9）**只在未合并的分支 `feat/g08-output-types` 上**，
> main 上没有。

---

## 1. 目标与进度

### 1.1 Gloss 是什么

- **产品**：读难懂的中文哲学、理论书时，点一下句子，这句话的大白话改写（中译中）就在段落下方就地展开。
  定位不是翻译器，而是长期的阅读空间：书架、读到哪、存下的白话都留着。书架、存白话尚未实现。
- **给谁用**：见 PRD.md 开头。
  - 读人文社科、哲学长文本的学生：本科高年级、研究生、自学者。
  - 第一位用户是项目负责人 Clara（哲学系）。Gloss 既是她自己要用的工具，也是她的作品集。
- **统一原则**：**一切服务于阅读的连贯性。**
  - 核心洞察（PRD）：把读者卡住时要做的七步，压缩成一次点击。
- **最高约束**：产品不变量，见第 4.1 节。

### 1.2 Issue 状态（2026-09-18 读 KANBAN.md 与 git log 核对）

#### 已完成（均已合并进 main）

| Issue | 内容 | commit |
|---|---|---|
| G-01 | 脚手架与 token 系统 | `488c7c1` |
| G-02 | docx / txt / 粘贴解析 | `d36d547` |
| G-03 | 规则切句 | `049383e` |
| G-04 | 阅读器三栏与排版 | `0fb3ceb` |
| G-05 | 点击撑开与视口锚定 | `18ba1f2` |
| G-06 | 功能一 API 与流式返回 | `21e8fdf` |
| G-07 | 端到端串联与上线 | `b7bfe89` |
| G-02B | PDF 文字层解析 | `6d18967`（实现）、`5ffed98`（Preview 验收记录）、`75ff3ea`（生产验收） |
| G-24 | docx 剔除表格 / 公式时给提示 | `f32c89c`（实现）、`dfc97a8`（验收记录） |
| G-25 | 左栏常驻解析统计、无标题时的文案 | `3e010f4`（实现）、`4781e5c`（验收记录） |

关于上表，有三处需要说明：

- **G-05 的日期不确定。** KANBAN 的 Done 条目写的是「完成 2026-09-15」，git 里的提交时间是 2026-09-16 04:15（+0800）。
  可能是跨过午夜才提交，没有核实过。
- **G-06 的 commit 号不在 KANBAN 里。** KANBAN 的 Done 条目没写 commit 号，`21e8fdf` 是从 git log 按提交说明对上的。
- **G-07 部分验收项没有打勾。** 它仍列在 Done 里，但有几项被挪到 G-15 或挂起：
  - Safari 验证：挂起。
  - 线上首字延迟：挪到 G-15。
  - 离开页面时中止请求：挪到 G-15。
  - 线上单次调用成本：挪到 G-15。

#### 进行中

**G-08 · 三类输出与术语标记**，分支 `feat/g08-output-types`，**未合并**。

- 已打勾：a、b、d、e、f 和「提示词示例不出自评测集」，另有术语色、悬停色两项。
- 未打勾的三项：
  - **c**：标得够不够、有没有标错，由 Clara 在第二批 10 句上判断。**阻塞于第二批 10 句。**
  - 原文含混的地方保持含混。
  - 术语按承重与否分别处理。
- 详见第 6 节第 1 条。

#### 未开始

| 批次 | Issue |
|---|---|
| W2 · P0 | G-09 跨会话缓存、G-10 保存白话、G-11 功能二（听不懂）、G-12 右键菜单与「翻错了」、G-13 书架 |
| W2 · P2 | G-26 UI 精修清单、G-27 PDF 目录识别 |
| W3 | G-14 导出 docx、G-15 全量埋点、G-16 示例文档与首次引导、G-17 编辑白话、G-18 Ctrl 锁定、G-19 阅读主题切换、G-20 侧栏收起、G-21 覆盖模式、G-22 视图切换、G-23 导出 PDF |
| W4 | 不写新功能：招募用户、数据复盘、做一轮迭代 |

### 1.3 线上与分支

- **线上地址**：https://gloss-jet.vercel.app
  - 由 Vercel 的 GitHub 集成部署 main，每个分支自动生成 Preview。
  - 线上跑的是 main（`4781e5c`）。本轮会话最后一次确认时，生产部署对应 main。
    之后没有再查部署状态，所以现在是否仍是这个版本「不确定」。
- **仓库**：https://github.com/clarachan1123/Gloss
- **main** = origin/main = `4781e5c`（docs(G-25): Preview 验收通过，移进 Done）。
- **未合并的分支**：

| 分支 | 状态 |
|---|---|
| `feat/g08-output-types` | 比 main 多 6 个 commit：`600dc05` → `aaa2568` → `29f99c6` → `d4bcbfa` → `02a80df` → `551e53a`。已推送，origin 与本地一致。**等 Clara 看完再定是否合并，不得擅自合并。** |
| `docs/handoff` | 本文件所在分支，从 main 拉出，只含 HANDOFF.md。**未推送。** |

- 其余分支（g01–g07、g02b、g24、g25 与几个 docs 分支）都已合并进 main。

---

## 2. 技术栈、目录、关键文件

### 2.1 技术栈（版本号取自 package.json 与本机，2026-09-18）

- **框架**：Next.js 16.3.5（App Router，dev 用 Turbopack）+ React 19.3.0 + TypeScript 5.9.3。
  - **这版 Next 与训练数据里的不同。** AGENTS.md 要求写代码前先读 `node_modules/next/dist/docs/` 里的相关文档。
- **样式**：原生 CSS + CSS 变量。
  - 主题 token 在 `styles/tokens.css`，阅读器样式在 `styles/reader.css`，上传页和提示框用 CSS Modules。
  - **不用 Tailwind。** 禁止引入 `cdn.tailwindcss.com`。设计稿用的是 Tailwind Play CDN，生产环境不可用。
- **文档解析**：
  - mammoth 1.12.3（docx）。
  - pdfjs-dist **6.3.289（锁定版本）**，用于 PDF 文字层；worker 和 cMap 都由本站托管。
- **模型**：DeepSeek `deepseek-flash`（DeepSeek-V4.1-Flash），用于功能一和结构摘要。
  - 功能二（G-11，未做）按 KANBAN 用 `deepseek-v4-pro`。
- **测试**：vitest 5.0.0，外加 happy-dom 20.14.5。
  - 配置文件是 **`vitest.config.mts`**，其中配了 `@/` 路径别名，route 测试需要它。
  - 用 `.mts` 的原因：package.json **没有声明 `"type": "module"`**，`.ts` 配置会按 CommonJS 加载，`.mts` 强制按 ESM 处理。
    这是 G-06 时的决定，记在 KANBAN G-06「会改」一栏。
- **运行环境**：
  - 本机实测：Node v24.16.0、npm 11.13.0、Windows 10。
  - 部署：Vercel Hobby 套餐。

### 2.2 目录结构（两级，只列被 git 跟踪的文件与关键的被忽略目录）

```
gloss/
├── AGENTS.md / CLAUDE.md     # 执行纪律摘要；CLAUDE.md 只引用 AGENTS.md。next dev 会自动重写 AGENTS.md 顶部一段
├── KANBAN.md                 # 唯一事实源：纪律、不变量、安全底线、每个 issue 的验收清单
├── PRD.md                    # 产品需求 v1.3。不改 PRD（见第 7 节）
├── README.md                 # 本地运行、主题 token 说明（token 数已过时，见第 5.1 节）
├── HANDOFF.md                # 本文件
├── package.json              # predev / prebuild 先跑 copy-pdfjs-assets.mjs
├── vitest.config.mts         # @/ 别名；.mts 原因见 2.1
├── next.config.ts            # 空配置
├── app/
│   ├── layout.tsx            # 字体（Cinzel、JetBrains Mono preload:false）、主题属性
│   ├── page.tsx              # 首页：目前只有上传组件（书架是 G-13）
│   ├── read/[docId]/         # 阅读器路由 page.tsx
│   └── api/
│       ├── gloss/route.ts    # 功能一：目标句 + 前后各 ≤2 句 + 结构摘要 → 流式白话
│       └── structure/route.ts# 全书结构摘要（≤300 字），每份文档只算一次
├── components/
│   ├── Upload.tsx(.module.css)  # 上传 / 粘贴入口，客户端解析，提示分阻断型和横幅型
│   ├── Notice.tsx(.module.css)  # 统一的异常提示组件
│   └── reader/
│       ├── Reader.tsx        # 三栏、段落 memo、行尾拆分、视口锚定、结构摘要请求、会话内缓存
│       ├── Sentence.tsx      # 可点击句子
│       ├── GlossPanel.tsx    # 撑开区：流式文字、失败 / 限流 / 断流界面、术语渲染
│       └── TermMark.tsx      # <span class="gloss-term">（仅 G-08 分支）
├── lib/
│   ├── parse/{docx,pdf,txt,validate}.ts  # 客户端解析；validate 负责 A1–A10 异常与文档组装
│   ├── segment.ts            # 规则切句（序言样本 61 句）
│   ├── context.ts            # 请求体校验与提示词拼接，前缀顺序固定
│   ├── deepseek.ts           # 模型调用、thinking 关闭、价格表注释、C1–C6 错误分类
│   ├── gloss-client.ts       # 前端流式读取、403 / 429 / 断流分类、结构摘要请求
│   ├── output.ts             # 输出清洗、150 字上限、术语切分与本地校验
│   ├── storage.ts            # localStorage 封装（文档、阅读位置、结构摘要）
│   ├── prompts/gloss.ts      # 生效的提示词 + 历史版本存档
│   ├── prompts/structure.ts  # structure-v1
│   └── *.test.ts             # 共 9 个测试文件，见 3.4
├── scripts/
│   ├── copy-pdfjs-assets.mjs # 把 pdfjs 的 cMap 复制到 public/pdfjs/<版本>/cmaps/
│   └── eval-gloss.mjs        # 评测脚本（跑一轮 / --compare 多版盲测），会调用真实模型
├── styles/{tokens,reader}.css
├── docs/stitch-书架页-修订prompt-r2.md  # 书架页视觉稿的 prompt（G-13 用）
├── public/pdfjs/             # 【gitignore】构建时生成
├── test-fixtures/            # 【gitignore】样本与评测集，有版权，不进仓库
└── Claude outputs/           # 【gitignore】给 Clara 看的对照页、候选页，永不提交
```

- KANBAN「技术骨架」一节的目录结构是**规划**，不是现状。
  其中 `explain/`、`segment/` 路由，`shelf/`、`settings/` 组件，以及 `cache.ts`、`analytics.ts` 都还不存在。

### 2.3 解析在客户端：信任边界

- **这是 PRD 的承诺，不是实现细节。** 相关条目：
  - PRD 第 441 行：上传的原文存放在「浏览器内存 + localStorage」，「服务端不持久化原文」。
  - PRD 第 774 行（隐私）同样写明服务端不持久化原文。
  - KANBAN G-02、G-02B 的验收项「解析全部在客户端完成，原文不出浏览器」。
- **「原文不出浏览器」的准确范围**：只指**文件解析和存储**。文件本身从不上传，docx / PDF 在浏览器里解析，结果存在 localStorage。
- **原文的文字会发给服务端**（再转给 DeepSeek），发生在两个时刻：
  1. **开书时**，`Reader.tsx` 约第 420 行向 `/api/structure` POST `{ title, headings, paragraphs: doc.paragraphs }`，也就是**全书正文**，用来生成结构摘要。
     路由注释写的上限是正文 ≤5 万字（PRD 3.8）。
  2. **每次点句时**，向 `/api/gloss` 发目标句、前后各 ≤2 句，以及结构摘要。
- 服务端日志只记长度、错误类型、token 数，不记正文。
- **改动任何会把原文送出浏览器、或让服务端落盘的代码之前，先停下来问 Clara。**

---

## 3. 安装、运行、测试

### 3.1 安装与运行

```bash
npm install
```

```bash
npm run dev
```

```bash
npm test
```

```bash
npm run build
```

- 打开 http://localhost:3000。
- `predev` 和 `prebuild` 会先跑 `node scripts/copy-pdfjs-assets.mjs`，把 cMap 复制进 `public/pdfjs/6.3.289/cmaps/`。
  - 2026-09-17 在 Vercel 构建日志里实测：复制了 169 个文件。
- 不跑这一步的话，`pdf-real.pdf` 这类 GBK 编码的 PDF 会每页解析出 0 字（2026-09-17 实测）。

### 3.2 Windows 注意事项

- 本机有两个 shell：PowerShell 5.1 和 Git Bash。
- **PowerShell 5.1 的写法：**
  - 不支持 `&&`，改用 `A; if ($?) { B }`。
  - 设环境变量写 `$env:NAME="值"`。
- **Git Bash：** 本机的 Git Bash 里 `gh` 不在 PATH 上，本轮会话遇到过。
  - 查 GitHub 状态时改用 curl，或用 Node 脚本调 GitHub API。
- **中文路径：** 仓库里有中文文件名（`test-fixtures/政治经济学批判-序言.docx`、`Claude outputs/` 下的文件）。
  - git 默认会把非 ASCII 路径转义显示，所以 key 扫描要加 `-c core.quotepath=false`（见第 7.4 节）。
- **换行符：** 本机 `core.autocrlf` 的值是 `input`（2026-09-18 实测）。

### 3.3 本地起服务：一律用无效 key

PowerShell 写法：

```powershell
$env:DEEPSEEK_API_KEY="local-test-invalid"; npm run dev
```

生产构建同理，在同一行里先设环境变量，再跑 `npm run build` 和 `npm run start`。

- **为什么：**
  - Next 加载 `.env.local` 时**不覆盖启动前已存在的环境变量**，所以这个无效值优先，本地服务调不通模型。
  - 本轮项目里发生过两次意外的真实调用，见第 5.4 节。
- **无效值不以 `sk-` 开头**，免得被 key 扫描命中。
- **启动后先确认拦截生效，再开始测：**
  - 发 1 个会调模型的请求。最简单的是在浏览器里打开任意一份文档，阅读器会自动请求一次 `/api/structure`。
  - 确认服务端日志里出现 `HTTP 401`（认证失败），而不是正常结果。
- **只有明确需要真实生成时才用真 key**，并且事先在报告里说明、事后报实际花费。

### 3.4 测试

2026-09-18 19:51 在 `feat/g08-output-types` 上实测：9 个文件、**276 条全部通过**，耗时 1.91s。
main 在 G-25 完成时是 214 条（当时实测）。

| 文件 | 条数 | 覆盖内容 |
|---|---|---|
| `lib/context.test.ts` | 39 | 请求体校验（前后文 ≤2 句等）、提示词拼接顺序、结构摘要请求校验 |
| `lib/deepseek.test.ts` | 32 | 用模拟 fetch 逐条覆盖 C1–C6 错误分类、流式切块、thinking 关闭等请求参数 |
| `lib/output.test.ts` | 45 | markdown 清洗、150 字上限（定界符不计）、术语切分与配对失败降级、`limitTerms` 四道检查 |
| `lib/segment.test.ts` | 49 | 切句规则 B2–B5、闭合标点并入、降级段落；序言 61 句硬验收 |
| `lib/storage.test.ts` | 33 | 文档、阅读位置、结构摘要的存取；旧记录（无 `meta.skipped`）照常打开；存储满或被禁用 |
| `lib/theme-contrast.test.ts` | 19 | 读 tokens.css：术语色、悬停色、墨色、白话色 vs 底色 ≥4.5；悬停 vs 术语 ΔE00 ≥10；`.gloss-term` 只有两条声明 |
| `lib/parse/docx.test.ts` | 13 | 表格与公式检测、计数口径（G-24） |
| `lib/parse/pdf.test.ts` | 29 | 扫描页判定、页眉页脚去除、与 docx 逐字对照、加密、混合型 |
| `lib/parse/validate.test.ts` | 17 | A1–A10 提示、`skippedSummary` 文案 |

- `theme-contrast.test.ts` 在 main 上没有这个文件；上表的 output 条数也是 G-08 分支上的。
- **依赖样本的测试会被跳过。** 样本在 `test-fixtures/`，已被 gitignore，新 clone 下来没有。以下文件用 `describe.skipIf` 跳过，**不会报错**：
  - `segment.test.ts` 的序言用例；
  - `docx.test.ts`；
  - `pdf.test.ts`。
- **新 clone 能跑到多少条不确定**：没有在无样本的环境里实测过。样本要向 Clara 要。
- **样本清单：**
  - `政治经济学批判-序言.docx`、`docx-table-formula.docx`；
  - `pdf-text.pdf`、`pdf-encrypted.pdf`（密码 123456）、`pdf-scan.pdf`、`pdf-mixed.pdf`、`pdf-real.pdf`、`pdf-header.pdf`；
  - `eval20.json`（评测集）。

---

## 4. 近期改动与重要设计决策（附原因）

### 4.1 产品不变量

> **正在读的内容，永远不因为别处的变化而移动。**

- 判据：改动发生在视口**上方**时，可见内容位移必须为 0。只有用户自己滚动，页面才可以动。
- 任何改变文档高度的操作都必须先记下视口参照物的位置，改完补偿回去。这类操作包括：
  - 撑开、收起、自动收起；
  - 保存后转常驻；
  - 插入功能二结果、编辑白话；
  - 侧栏收起；
  - 字号、行距变化。
- 涉及的 issue：G-05、G-10、G-11、G-17、G-19、G-20，每一个都要单独验这条。
- G-07 的推论：
  - 流式生成期间，撑开区本身及其上方的位移为 0。
  - 撑开期间高度**只增不减**，收起时才缩回（决议 B）。

### 4.2 G-05 行尾拆分；为什么去掉 `text-wrap: pretty`

- **做法：**
  - 点击时量出被点句最后一个字所在的行，在这一行的行尾把段落拆成三块：上半段 / 撑开区 / 下半段。
  - 上半段加 `text-align-last: justify`，最后一行照样两端对齐。
- **证据**（G-05 实测，Chrome 152，1440 宽，45 次段中拆分，逐字坐标比对）：
  - 加 `text-align-last`：偏移 0px。
  - 不加：45 次全部移位，最大 33.7px。
- **去掉 `text-wrap: pretty`**，原因写在 `styles/reader.css` 第 192 行附近的注释里：
  - pretty 按「段落最后几行」重新平衡断行。
  - 段落一拆，「最后几行」指的就换了一批，断行随之改变，和行尾拆分的原理冲突。
  - 原句位置不变（D13）是硬约束，避免孤字只是观感优化，冲突时前者优先。**不要加回来**，哪怕是为了缓解句末孤字。
- **切换句子时前一句直接收起、不播动画**：两个动画同时播，锚定基准就不唯一了。
- **Safari 未验证**，见第 5.2 节。

### 4.3 段落 memo、性能基线、左栏不随滚动变化

- `Reader.tsx` 里每个段落组件都用了 memo。点一句时只有被点的那 1 段重新渲染。
- 给 Paragraph 传的 props 必须**按值可比**。G-08 传原句用的是字符串，不是对象。
- **基线**（G-07，2026-09-17 实测）：
  - 样本：本地生产构建，序言段落重复 30 次，共 450 段 / 1830 句；40 次点击。
  - 结果：点击 → DOM 更新中位数 6.5ms、p90 10.6ms、最长 17.2ms。
- **复测**（2026-09-18 实测，均以 main 为对照）：

| 轮次 | main 中位数 | 本分支中位数 |
|---|---|---|
| G-25 | 6.9ms | 6.6–6.7ms |
| G-08 接线前 | 7.3–7.5ms | 7.1–7.4ms |
| G-08 接线后 | 7.2ms | 7.7ms（冷）/ 7.3ms |

  三轮都没有回归；每次点击仍只有 1 段重新渲染。
- **测量脚本不在仓库里**：它们在会话的临时目录中，本次交接没有带过来。方法是：
  1. 本地 `npm run build` 加 `npm run start`，用无效 key 启动。
  2. 在浏览器里开上述 450 段样本。
  3. 用脚本逐次点击句子，记录点击到 DOM 更新的耗时，取中位数、p90、最长值。
  4. 重渲染检查：读 React fiber 的 `memoizedProps`，比较点击前后每个段落的属性对象是否是同一个引用。
  5. 每轮第一次测量偏高（热身），要跑 2–3 轮。
  - Codex 如果要复测，需要自己重写这个脚本。
- **左栏不做任何随滚动变化的内容**（G-25 验收项）：
  - 滚动时如果左栏的 state 或 effect 变化，会引起整棵阅读器树重新渲染，破坏上面的性能前提。
  - 左栏全部是静态内容，不新增 state 或 effect。

### 4.4 结构摘要缓存与 docId

- `docId` = `paragraphs.join("\n")` 的 SHA-256 前 8 位 hex。
  - 同一份文档重复上传得到同一个 id，书架上不会出现两本一样的书。
- localStorage 的三个前缀（`lib/storage.ts`）：
  - `gloss:doc:<docId>`：只存 paragraphs、headings、footnotes、meta。text 和 sentences 每次重算，免得在大书上逼近 localStorage 上限。
  - `gloss:pos:<docId>`：句序号，不存像素位置。
  - `gloss:structure:<docId>`：结构摘要，连同生成它的提示词版本一起存。读取时版本对不上就作废。
- `SCHEMA_VERSION = 1`。G-25 新增的 `meta.skipped` 是**可选字段**，所以没有升版本，旧记录照常打开（有测试）。
- **结构摘要的流程：**
  - 开书时后台请求一次，按 docId 缓存。
  - 算好之前的点击**不带摘要照常发**，不让第一次点击等待。
  - `/api/gloss` 从不调用 `/api/structure`，结构上保证不会每次点句都算一次摘要。
- **与 G-09 的关系：** 同一本书里，开头几次点击的输入没有摘要，后面的有。
  所以 G-09 的缓存指纹必须包含「本次请求是否带了结构摘要」，写在 G-09 验收项里。

### 4.5 WAF 限流

- **规则**：Clara 在 Vercel 控制台配置。
  - 路径前缀 `/api/`，按 IP 计数，10 分钟 30 次，超出返回 **403**。
  - Hobby 套餐每个项目只能配 1 条。
  - 2026-09-16 实测：POST 连发 35 次，前 30 次返回 400（请求体非法，但请求确实到达了路由），之后返回 403。
- **为什么用 WAF，不在应用里计数**：Vercel 的函数是多实例的，应用层计数每个实例各记各的，挡不住刷量。WAF 在边缘拦截，请求到不了路由。
- **为什么是 403 而不是 429**：
  - 上游 DeepSeek 忙时，我们的路由会原样返回 **429**（C3，几秒后就能重试）。
  - 如果 WAF 也返回 429，前端就分不清「点太快被拦了」和「上游忙」。
  - 我们自己的路由**从不返回 403**，所以 403 只可能是 WAF 拦截。
- **前端怎么区分**（`lib/gloss-client.ts`）：
  - **只看状态码**，不看响应头和正文，因为真实的 WAF 响应内容没见过。
  - 403：显示「点得太快了，过几分钟再试」，**不给重试按钮**；结果不进会话缓存；结构摘要被拦时不提示，本次会话也不再重试。
  - 429：上游限流，可以重试。
  - 403 发生在流开始之前，和读流中途断开（D2）是两条路径。
- **安全底线第 6 条**：先限流，后上 key。生产环境的 key 是在 WAF 验证生效之后才配置的。
  Preview 和 Development 环境没有配 key。

### 4.6 cMap 本地托管

- `pdf-real.pdf` 这类扫描书的文字层是 GBK 编码，没有 cMap 就解析出 0 字。
- pdf.js 默认从 CDN 拉 cMap，这样会有第三方请求。
- 做法：
  - `scripts/copy-pdfjs-assets.mjs` 在 predev 和 prebuild 时，把 `node_modules/pdfjs-dist` 的 cMap 复制到 `public/pdfjs/<版本>/cmaps/`。这个目录已被 gitignore。
  - worker 由本站打包。
- **路径里带版本号**，所以升级 pdfjs-dist 时必须同步检查这条路径。版本是锁定的（6.3.289）。
- **验证**（Clara 在 Preview 和生产上亲验）：`GBK-EUC-H.bcmap`、`Adobe-GB1-UCS2.bcmap` 都返回 200 且来自本站，没有 cdn、jsdelivr、unpkg 的请求。
- **未托管**的是标准字体和 wasm 解码器。JPX / JBIG2 图片可能识别不出来，见 KANBAN 的已知盲区。

### 4.7 PDF 规则与阈值来源（`lib/parse/pdf.ts`，G-02B）

逐页数据见 KANBAN G-02B「样本逐页数据」，2026-09-17 实测。

- **扫描页判定**：本页字数 < 20，**且**最大图片面积 ≥ 30%。
  - `pdf-scan.pdf`：1 页，0 字，图片 62.7%。
  - `pdf-mixed.pdf` 第 2 页：0 字，图片 44.5%。
  - `pdf-real.pdf`：每页 420–672 字，图片 96.9%，所以不能只看图片面积，否则整本书都会被判成扫描页。
- **兜底**：没有扫描页，但全书平均每页 < 20 字 → A5（扫描件）。
  - 用来接住图片面积算不出来的情况（JPX / JBIG2）。
- **乱码阈值 5%**：**未校准**。样本的乱码比例全部是 0%（KANBAN 盲区表写「5 份样本」，逐页数据表列了 6 份，都是 0%）。
- **页眉页脚**：
  - 只看每页最上面、最下面各一行。
  - 规则：同一行文字在 ≥3 页、相近的 y 坐标上重复出现，就去掉。
    比对时把数字归一化，并去掉两端的字母和数字。
  - 纯数字的行按页码去掉。
  - 来源：`pdf-header.pdf`（奇数页「政治经济学批判」、偶数页「序言」、底部页码）。
    去除后与 `pdf-text.pdf` 逐字、逐段一致。
- **段落拼接**：看行宽、缩进、行间距。
  - 按字号分段被放弃了，因为 `pdf-real.pdf` 的 OCR 文字层字号在 10–20 之间乱跳。
- **归一化**：合并汉字与汉字之间的半角空白，全角空格不合并。
  - 误伤率抽查（实测）：序言 docx 0 处，`pdf-text.pdf` 10 处（都是标题字间的空格），评测文本 169 处，抽查未见误伤。
- **已知限制**（本卡不处理）：
  - 脚注会混进正文；
  - 表格文字会被读进正文；
  - 英文长单词换行会被误判为段末；
  - 页眉下面紧跟的第二行即使也重复，也不会被去掉。

### 4.8 术语标记（G-08，只在未合并的分支上）

- **含义**（2026-09-18 Clara 定）：灰蓝色标记表示「**这个词没给你改写**」，即白话里原样保留了原句的词。
  - 不再表示「这是本书的承重概念」。承重与否是主观判断，模型三轮都判不准；「是不是照抄原句」是可核对的事实。
- **流程**：模型提名 + 本地校验 + 每句上限。
  - **模型提名**：模型在白话里用 `⟦⟧` 把保留的原词圈起来。
    这对定界符在 23 份文本、共 60895 字符里实测出现 0 次，所以不会和正文冲突。
    `「」` 在正文里出现过，`【】` 已被用户消息的分节标题占用，所以都没选。
  - **本地校验**：前端 `lib/output.ts` 的 `splitTerms` 先切出候选，再由 `limitTerms` 按顺序做四道检查：
    1. 必须逐字出现在原句里，比对时忽略空白。
    2. 长度 ≤ 6 字（`MAX_TERM_CHARS`）。
    3. 同一个词只标第一次出现。
    4. 每句最多 3 个不同的词（`MAX_TERMS_PER_GLOSS`）。
    没通过的按普通文字显示，也不占名额。
- **容错：**
  - 定界符配对失败（只有左半边、只有右半边、被数据块截断）→ 降级成普通文字，定界符不出现在读者眼前。
  - 定界符不计入 150 字上限（`CharLimiter`）。
- **原句的来源：** `Reader.tsx` 把被点的原句以字符串形式经 Paragraph 传给 `GlossPanel`（prop 名 `source`）。
  这是 2026-09-18 经批准做的改动，按值比较，不破坏 memo。
- **视觉：只有颜色**，另加 `white-space: nowrap`，让整个词在行末一起移行。
  - 不加底色、下划线、边框、图标。
  - 原文一侧不做任何标记。
  - `theme-contrast.test.ts` 断言 `.gloss-term` 只有这两条声明，多一条就失败。
- **提示词**：生效的是 gloss-v9；v4–v8 存档在 `lib/prompts/gloss.ts`。
  - 正例：市民社会、生产关系、交换价值、物化、拜物教。
  - 「日常词也是概念」的例子：劳动、商品。
  - 反例：泛词（东西、关系、问题、方面、情况）、短语、已经改写过的词。
  - 「必须是名词性的概念词」这条本地检查不了，只能靠提示词约束。
- **已知代价**：流式输出时，重复词的前一两个字可能先按术语显示，写完后才判定为重复。至多闪一次，这是基于放字节奏（每 45ms 放 4 个字）的推算。

### 4.9 `--term` 与 `--hover` 的取值和原因（`styles/tokens.css`，只在 G-08 分支上）

**`--term`**：原来的颜色在三套主题下「完全看不出来」（Clara 的原话）。
当时术语色 vs 白话色的对比度只有 1.23–1.31:1。Clara 从候选页挑定了下面的值：

| 主题 | 选中档 | `--term` | vs 底色 | vs 白话色 | ΔE00 |
|---|---|---|---|---|---|
| 羊皮纸 | A（深、低饱和） | `#233448` | 11.73 | 1.78 | 20.0 |
| 月白 | A（深、低饱和） | `#1F3341` | 11.99 | 1.91 | 16.4 |
| 护眼绿 | B（中、中饱和） | `#326095` | 5.70 | 1.08 | 26.8 |

- 月白的 ΔE 最低，因为月白的白话色偏冷灰，灰蓝色很难和它拉开。

**`--hover`**：原来原句悬停也共用 `--term`。Clara 判断悬停「太不显眼」，要稍微显眼一点点，但只用来提示这句能点，不能抢注意力。
所以把悬停这一处拆成独立变量，三套主题都选了 **C 档**：

| 主题 | `--hover` | vs 底色 | 与墨色 ΔE00 | 与术语色 ΔE00 |
|---|---|---|---|---|
| 羊皮纸 | `#1F4E75` | 8.08 | 21.6 | 10.4 |
| 月白 | `#1F4E75` | 8.02 | 21.6 | 11.8 |
| 护眼绿 | `#14446B` | 8.94 | 23.3 | 10.1 |

- **约束**（`theme-contrast.test.ts` 锁住）：
  - 悬停色 vs 底色 ≥ 4.5（按正文标准：16.5px 属于正文，不能用大字号的 3:1）。
  - 悬停色与术语色的 ΔE00 ≥ 10，悬停的原句不能看起来像被标了术语。
  - 悬停色与墨色的色差要大于换色前的悬停色。
- **回退预案**：如果 Clara 连读时觉得悬停太跳，降到 **B 档**，只改色值：
  羊皮纸 `#2E5270`、月白 `#2C506D`、护眼绿 `#204665`。这组值同样满足上面三条测试。
- **`--term` 仍被另外两处共用，没有拆**：
  - 白话面板的「重试」链接；
  - 上传页**阻断型**提示框的左边线（`components/Notice.module.css` 的 `.block`）。
- 两个表里的数字都是 2026-09-18 计算的（对比度按 WCAG 2.1 公式，色差按 CIEDE2000），都没有线上肉眼确认。
  KANBAN 写着「线上肉眼确认待 Clara」。

### 4.10 其他生效参数（G-06 定，取自代码）

- 功能一温度 0.2，max_tokens 400。
- 上下文：前后各 ≤2 句 + 结构摘要，**不传整章**。
- 输出 ≤150 字，超长时由服务端截断，并记录日志事件 `gloss_overlength`。
- **DeepSeek 的思考模式关闭**：`lib/deepseek.ts:141`，`thinking: { type: "disabled" }`。
  - 为什么关闭：本文件没有找到记录下来的理由。根据延迟和成本推测是为了速度和费用，这一点**不确定**。
- 结构摘要：structure-v1，≤300 字，max_tokens 800。
- **价格**：`lib/deepseek.ts` 注释，2026-09-16 按官方文档核对。flash 每百万 token 的价格：

| 计费项 | 空闲 | 高峰 |
|---|---|---|
| 缓存命中 | ¥0.02 | ¥0.04 |
| 未命中 | ¥1 | ¥2 |
| 输出 | ¥4 | ¥8 |

  高峰时段为北京时间工作日 9–12 点和 14–18 点。**价格可能已经变了，用之前重新核对。**

---

## 5. 已知问题、未完成项、教训

### 5.1 KANBAN 的盲区、风险与过时之处

- **已知盲区**：共 10 行，见 KANBAN「🔍 已知盲区」：
  - Safari；
  - 被中止的请求是否计入 WAF；
  - WAF 403 的响应内容；
  - PDF 字符映射错位但输出的仍是正常汉字；
  - 乱码阈值 5% 未校准；
  - JPX / JBIG2 图片；
  - 大插图、字很少的页；
  - 只有一两页的 PDF；
  - docx 公式检测依赖 mammoth 的警告文本；
  - PDF 规则换一本书是否有效。
- **已知风险**：WAF 按 IP 计数。
  - 共用出口 IP 的读者（校园网、公司网络）共享 10 分钟 30 次的额度。
  - 暂时不改，G-15 时重新评估。
- **KANBAN 里已过时、尚未修正的地方**（本文件只记录，没有改 KANBAN）：
  - G-08 验收项 e 写着「gloss-v8 已写入」，实际生效的是 v9。
  - G-08「术语识别实测」表里，v7 那一行标着「（生效）」，实际生效的是 v9。
  - G-08「接线后复测」下面，「重渲染：抽 3 个位置…」这句话重复写了两次。
  - 「安全底线」第 2 条的两条命令**没有带 `-c core.quotepath=false`**，只在后面的正文里提了一句。
    第 7.4 节给出的是实际使用的完整命令。
  - README 写「每套 6 个颜色变量」，G-08 分支上是 7 个（多了 `--hover`）。main 上仍是 6 个，所以合并 G-08 时要同步改 README。
  - 「技术骨架」里的目录是规划，不是现状（见 2.2）。

### 5.2 必须知道的几件事

1. **eval20 的「术语_来源不明_非标准答案」字段**
   - `test-fixtures/eval20.json` 原来的字段名是「术语」：20 句里 17 句有值，共 53 个词。
   - 2026-09-18 **改了名，值没有动**。
   - 这个字段的来源查不清：
     - 文件创建于 2026-09-16 04:51（本地时间），3 分钟后 Clara 在会话里说「评测集我已经放好了」。
     - 所有会话记录里都没有写入过这个文件。
     - Clara 本人不记得是手标的还是用工具生成的。
     - 53 个词都逐字出自原句，但 14 组多词列表里只有 4 组是按句中顺序排的。
   - 结论：不能当标准答案。建立在它上面的「准确率 ≥85%」和 53 的分母**已作废**。
   - **不要用这个字段打分，也不要从这个字段、评测集原句、Clara 在读的书里取提示词示例**（见第 7.6 节）。
2. **术语密度问题：原因不明**
   - 2026-09-18 在 20 句评测集上实测，口径统一为「按每次出现计数」：

| 版本 | 模型标出 | 本地检查后显示 | 平均每句 | 每句显示 0 / 1 / 2 / 3 个的句数 |
|---|---|---|---|---|
| v7 | 9 | 6 | 0.30 | 15 / 4 / 1 / 0 |
| v9 | 2 | 2（两个都是「样式」，id 158、166） | 0.10 | 18 / 2 / 0 / 0 |

   - v9 和 v7 的白话平均都是 77 字。v9 的本地检查一个都没拦下（原始输出里只有 2 对定界符）。
   - **v9 为什么标得比 v7 少这么多，原因不明**：
     - 提示词 v8、v9 都改过措辞和示例；
     - 模型本身有随机性；
     - 20 句的样本量很小。
     这三者分不开，没有做归因实验。
   - 标 2 个是太少还是正好，**由 Clara 判断**，还没有定论。
   - 反过来的极端：离线对齐实验用 v7 的白话重算，本地对齐平均每句会标 10.6 个，而且会出现跨词边界的碎片。所以没有改用纯本地对齐。
3. **提示词迭代出现过退步（G-06 的 v2、v3）**
   - v2 按「重述而非改写」执行（禁止破折号、分号、多层从句，指代词必须指名道姓），盲测显示比 v1 差：本来就好的句子被过度改写。v3 同样比 v1 差。
   - 两版都已废弃，原文存档在 `lib/prompts/gloss.ts`。
   - 教训：改提示词必须做多版盲测对照（`node scripts/eval-gloss.mjs --compare …`），不能只看单版。
     D 类问题（偏离原意）在单版阅读里是看不出来的：第一轮单版没有发现，三版并排后 20 句里暴露出 7 句。
4. **人工判定的噪声底（G-06）**
   - 实验：v1 同配置跑两遍、v1 降温到 0.2、v3 换 pro 模型，共四版盲测。
   - 结果：20 句里有 19 句没有明确的赢家；同配置跑两遍，逐句判定的分歧率约 64%。
   - 结论：提示词之间的差异小于人工判定能分辨的程度，全量评测到此为止。
   - 以后评估提示词改动时，**小样本上的差异不能当结论**。
5. **Safari 未验证**
   - G-05 的行尾拆分方案只在 Chrome 152 上做过逐字比对。
   - Safari 对 `text-align-last` 和 `text-justify: inter-ideograph` 的行为未知，方案可能失效。
   - 没有 Mac，所以挂起。在验证之前**不宣称支持 Safari**。
6. **375px 宽时三栏布局塌掉**
   - 2026-09-18 做 G-08 行末移行测试时实测：左栏 184 + 右栏 140 已经占满，中栏只剩 18px，正文逐字换行。
   - PRD 主场景的下限是 1024px，手机不在 MVP 范围内。已记入 G-26，要求至少有一个可读的降级（比如某个宽度以下自动收起侧栏）。

### 5.3 其他未完成项

- **G-08**：三项未勾（见 1.2），线上的术语色和悬停色都还没经 Clara 肉眼确认。
- **G-07 挪到 G-15 的项目**：
  - 线上首字延迟 P90 ≤2.5s。本地实测约 0.7–0.8s；G-06 实测 P90 682ms。
  - 离开页面时中止请求，且中止后不计费。
  - 线上单次调用成本。本地日志估算：高峰价、固定前缀命中缓存时每次约 ¥0.0009，完全不命中时约 ¥0.0021，空闲时段减半。
- **PRD 1.2 的句数**：PRD 写 56 句，实际是 61 句。是否同步修订 PRD，待 Clara 决定。
- **favicon.ico 返回 404**（G-26）。
- **G-27**：PDF 目录一律为空。PDF 路径传的是 `headings: []`。

### 5.4 两次意外的真实模型调用（教训）

1. **G-07 性能测试**（2026-09-17）：拦截没有生效，真实调用了 11 次，约 ¥0.009（按日志 token 数和价格表估算）。
2. **G-02B 浏览器实测**（2026-09-17）：打开文档后，阅读器自动请求了一次结构摘要，真实调用 1 次，约 ¥0.001（估算）。

- 由此定了规则：本地起服务**一律用无效 key**，启动后先确认返回 401（执行纪律第 6 条，第 3.3 节）。
- 这类事故的共同原因：打开文档就会**自动**调一次 `/api/structure`，不需要点任何东西。

### 5.5 评测花费记录（均为按日志 token 数 × 价格表估算，不是账单）

| 时间 | 内容 | 花费 |
|---|---|---|
| G-06（2026-09-16） | 6 轮人工评测 | 约 ¥0.23 |
| G-08 v5–v7（2026-09-18） | 60 次调用 | 约 ¥0.035 |
| G-08 v9（2026-09-18 19:20，空闲时段） | 20 次调用：缓存命中 17152 + 未命中 8488 + 输出 922 token | 约 ¥0.0125 |
| 账户余额（2026-09-17，Clara 查看） | DeepSeek 是预付费，余额本身就是硬上限，常态保持 ≤¥20 | ¥17.07 |

---

## 6. 下一步（按优先级）

排序依据 KANBAN 的 P 级和依赖关系。**最终顺序由 Clara 定**；这里的排序是我根据 KANBAN 推出来的，不是她确认过的。

1. **G-08 收尾**（P0，分支 `feat/g08-output-types`）
   - **目标**：把剩下三项做完或由 Clara 定去留，Preview 亲验后合并。
   - **阻塞，等 Clara：**
     - 看 `Claude outputs/g08-v9-标记对照-8句.html`，判断术语密度（v9 每句平均 0.10 个）是否可以接受。
     - 交出**第二批 10 句难句**（标准：第一遍没懂、回头又读了一遍的句子），用于验收 c。
       判定要留痕：哪一句、她认为该标哪几个、模型标了哪几个。
     - 在 Preview 上肉眼确认术语色和悬停色。
   - **可能改动的文件**：
     - `lib/prompts/gloss.ts`：版本号加一，旧版存档。
     - 如果只调规则：`lib/output.ts`、`lib/output.test.ts`。
     - `KANBAN.md`。
   - 「原文含混保持含混」「术语按承重与否分别处理」这两项都要改提示词，要按第 7.6 节的规矩做多版盲测。
   - **完成标准**：验收全部打勾，Clara 在 Preview 上亲验，**她说合并才合并**。
2. **G-09 跨会话缓存**（P0，依赖 G-06，已满足；分支 `feat/g09-cache`）
   - **目标**：同一句点第二次，300ms 内出现，且没有新的模型调用。
   - **缓存 key**：句 hash + 上下文指纹。指纹必须包含提示词版本、temperature、本次请求是否带了结构摘要。
   - **value 只存白话**，不存原文明文。
   - **依赖外部资源**：Vercel KV 或 Upstash Redis。**由谁开通不确定**，按以往惯例控制台操作由 Clara 做，但没有确认过。
   - **会改的文件**：`lib/cache.ts`（新建）、`app/api/gloss/route.ts`。
   - **不改**：`components/**`、`lib/prompts/**`。
   - **建议在 G-08 提示词定稿之后再做**，否则提示词版本一变，缓存就整批失效。这条是我的推断，KANBAN 里没有写。
   - **完成标准**：KANBAN G-09 的 6 项验收全部通过；埋点 `sentence_click.cache_hit` 可以观测。
3. **G-10 保存白话**（P0，依赖 G-08）
   - **会改**：`lib/storage.ts`、`components/reader/GlossPanel.tsx`、`components/settings/SettingsPanel.tsx`（新建）。
   - **要点**：
     - 刷新后恢复；
     - 阅读模式和复习模式之间显式切换；
     - 同一段有多个撑开区时，拆分点要基于原始排版一次算出；
     - 存储满或被禁用时，提示并给出导出入口，不阻断使用；
     - **产品不变量**。
   - **完成标准**：KANBAN G-10 的 9 项验收全部通过。
4. **G-11 功能二「听不懂」**（P0，依赖 G-08）
   - **会改**：`app/api/explain/route.ts`、`lib/prompts/explain.ts`、`components/reader/ActionRow.tsx`、`lib/storage.ts`。
   - **要点**：
     - 用强模型 `deepseek-v4-pro`，和功能一分开计费；
     - 输出 ≤250 字；
     - 每句只能用一次，结果常驻，永不重新生成；
     - 句子有术语时才在操作行显示「听不懂」按钮。
   - 「有术语」怎么判断，依赖 G-08 最终的标记语义，**不确定**：现在的标记表示「没改写的词」，和 PRD D14 原意里的「术语」是否一致，需要问 Clara。
5. **G-12 右键菜单与「翻错了」**（P0，依赖 G-11）
   - **会改**：`components/reader/ContextMenu.tsx`、`app/api/report/route.ts`、`lib/analytics.ts`。
   - 上报会带原句明文，隐私说明里要写明。这一条碰到信任边界（2.3），开工前要复述给 Clara。
6. **G-13 书架**（P0，依赖 G-10）
   - **会改**：`components/shelf/{Shelf,Spine,EmptyShelf}.tsx`、`app/page.tsx`、`lib/storage.ts`。
   - **视觉稿阻塞于 Clara**（她要跑 Stitch，prompt 在 `docs/stitch-书架页-修订prompt-r2.md`），但这不阻塞开发起步。
   - 包含「添加新书」入口。
7. **W3 的 P0**：
   - G-14 导出 docx；
   - G-15 全量埋点，含从 G-07 挪来的三项线上测量；
   - G-16 示例文档，**阻塞于 Clara**：示例文本和版权路径还没定。
8. **W3 的 P1**：G-17、G-18、G-19、G-20、G-21、G-22、G-23。推迟顺序见 KANBAN，G-23 最先砍。
9. **P2**：
   - G-26 UI 精修：上传失败提示的颜色、favicon、窄视口降级。
   - G-27 PDF 目录：先读 PDF 自带的大纲；**明确排除调用模型生成目录**。

---

## 7. 必须遵守的规则

### 7.1 KANBAN 是唯一事实源

- 每次编辑 KANBAN.md 之前，**先从磁盘重新读一遍**，不要凭记忆或旧的上下文改。
- 每完成一项就勾选对应的验收项，全部打勾才能移进 Done。
- 验收未全绿不算完成。

### 7.2 每个 issue 开工前先复述

- 说清楚会改哪些文件、不会改哪些文件，和 KANBAN 条目核对，**等 Clara 确认**。
- 清单之外的文件一律不动。开工后确实需要追加文件时，先停下来说明，获批后再改，并把追加记进 KANBAN 的「会改」一栏。
- 一个 issue 一个分支。每轮要有一个可验证的增量。
- **不改 PRD**：发现 PRD 有问题就停下来说。

### 7.3 不许「顺手修一下」

- 发现别的问题，记进 KANBAN 对应的卡片（或新建卡片），不在当前分支里改。
- 本文件 5.1 节列出的 KANBAN 过时之处，也是只记录、没有改。

### 7.4 安全底线（另一个项目的 API key 被盗用过）

- **key 只存在两个地方**：本机 `.env.local`（已 gitignore）和 Vercel 环境变量。
  - 代码、注释、日志、报错信息、提交记录里**永远不能出现**。
  - 不要把 key 打印到任何输出里，也不要在报告里回显。
- **每次提交前跑两遍 key 扫描**，只数命中次数，不打印内容，**两次都必须是 0**。在 PowerShell 里运行：

```powershell
(git -c core.quotepath=false diff --cached | Select-String -Pattern "sk-[a-zA-Z0-9]{20,}" | Measure-Object).Count
```

```powershell
(git -c core.quotepath=false log -p --all | Select-String -Pattern "sk-[a-zA-Z0-9]{20,}" | Measure-Object).Count
```

  - 第一条查这次要提交的内容。这是唯一能在 key 进入历史之前拦住它的时机。
  - 第二条查全部历史，是事后确认：
    - 报警时如果还没推送，还能改写历史；
    - 如果已经推送，key 只能作废。
  - **`-c core.quotepath=false` 的原因**：KANBAN 写的是，扫描非 ASCII 路径时不关掉 git 的路径转义，「会得到一个假的 0」。
    我的理解是 quotepath 只影响路径的显示方式，是否真的影响内容行的匹配，**不确定**（没有做对照实验）。
    加上这个参数没有坏处，**照写**。
- **已泄露的 key 只能作废**，没有补救办法。
- **暂存必须写明路径**：`git add <路径>`，禁止 `git add -A` 和 `git add .`。
- `Claude outputs/` 永远不提交；测试样本只放在 `test-fixtures/`（已 gitignore）。
- **线上环境变量只在防护措施就位之后才配置**：先限流，后上 key。
- 每周看一次 DeepSeek 用量曲线，出现异常尖峰就立刻作废 key 并重建。这件事由 Clara 在控制台做。

### 7.5 本地服务与验收标注

- **本地起服务一律用无效 key**：`$env:DEEPSEEK_API_KEY="local-test-invalid"`。
  - 启动后先发 1 个请求，确认服务端日志里是 `HTTP 401`，再开始测。
  - 确实需要真实调用的评测，**事先说明预计花费，事后报实际花费**。
  - 批量调用之前，先用 1 个请求确认拦截生效。
- **验收标注分三种：**
  - **（Claude Code 自测）**：在本地开发环境或本地生产构建里用脚本测的。Codex 自己测的，照此标注（写成「Codex 自测」）。
  - **Clara 亲验（Preview 部署）/ Clara 亲验（线上 gloss-jet.vercel.app）**：Clara 亲手验过的。
  - **不打勾**：没验过，或只验了一部分。后面写明还差什么，以及挪到了哪个 issue。
- **分支的推送和合并：**
  - 推送分支要 Clara 同意。
  - **合并进 main 必须 Clara 明确说**，而且在她亲验 Preview 之后才合并。

### 7.6 技术约束

- **不用 Tailwind CDN**，样式用原生 CSS + CSS 变量。验证命令：`git grep -n "cdn\.tailwindcss\.com" -- ':!*.md'` 结果为空。
- **DeepSeek 的思考模式保持关闭**（`thinking: { type: "disabled" }`）。
- **提示词前缀的顺序固定为：系统提示词 → 全书结构摘要 → 目标句及其前后文。**
  - 这样前面不变的部分能命中 DeepSeek 的前缀缓存（命中价是未命中价的 1/50）。
  - 拼接逻辑在 `lib/context.ts`。**不要把会变的内容挪到前面。**
- **`overflow-anchor: none`**（`styles/reader.css:186`）不能删。
  它关掉了浏览器自带的滚动锚定，视口锚定全部由 Reader 手动完成；两套机制同时生效会互相打架。
- **不要加回 `text-wrap: pretty`**（原因见 4.2）。
- 改动文档高度的任何功能，都要单独验证产品不变量（4.1）。
- 段落组件的 props 要保持按值可比，不能破坏 memo（4.3）。
- **提示词的规矩：**
  - 措辞一改，**版本号就加一**（`GLOSS_PROMPT_VERSION`、`STRUCTURE_PROMPT_VERSION`）。
    旧版原文存进 `GLOSS_PROMPT_ARCHIVE`，不删。
  - 结构摘要提示词改了，功能一的上下文指纹也跟着变（G-09 缓存要考虑这一点）。
  - **示例词不得出自评测集**（原句和前后文），**也不得出自 Clara 正在读的书**（`pdf-real.pdf` 就是这本书的样本）。
    - 换示例词之前，先实测它在 `test-fixtures/eval20.json` 和 `pdf-real.pdf` 正文里出现 0 次。
    - 前车之鉴：「意识」从 v5 起就在提示词里，评测集里出现 5 次、pdf-real 里 35 次，一直在泄题。
  - 评估提示词改动要做**多版盲测对照**，不能只看单版；判断好坏以 Clara 本人的阅读为准。
  - 机械检查（破折号、分号、双重否定、指代词）只是定位工具，不是目标。
