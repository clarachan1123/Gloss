# Gloss · 开发看板

> **依据** PRD v1.3 ｜ **周期** 4 周 ｜ **更新** 2026-09-21
> 本文件是开发的唯一事实来源。放在仓库根目录，每完成一个 issue 就把它移到 `## ✅ Done` 并勾选验收项。

---

## 执行纪律（每个 issue 都适用）

1. **动手前先复述**：开工前先说清「这个 issue 会改哪些文件、不会改哪些文件」，与下方清单核对。**清单之外的文件一律不动。**
2. **一轮一个可验证增量**：每个 issue 结束时必须有一个能在浏览器里看到、或能跑测试验证的结果。做不到就说明 issue 拆得太大，先拆。
3. **一个 issue 一个分支一个 commit**：分支名见各条目。回滚 = `git revert` 或直接删分支。
   阅读器改动的性能复测一律使用 `scripts/perf/README.md` 所述同一脚本与样本。
4. **验收未全绿不算完成**：勾选框全打勾才能移进 Done。
5. **不改 PRD**：实现中发现 PRD 有问题，先停下来说，不在代码里偷偷改行为。
6. **批量调用真实模型前先确认拦截生效**：凡是可能调用真实模型的自动化测试，批量跑之前必须先发 1 个请求，
   并通过服务端日志的调用计数确认拦截真的生效了，才能开始批量。
   依据：G-07 性能检查时拦截失效，真实调用 11 次，约 ¥0.009。
   **本地起服务做测试默认就调不通**（`next dev` 和 `next start` 都算）：启动时用环境变量把 `DEEPSEEK_API_KEY`
   覆盖成无效值（PowerShell：`$env:DEEPSEEK_API_KEY="local-test-invalid"`；无效值不以 `sk-` 开头，免得被 key 扫描命中）。
   Next 加载 `.env.local` 时不覆盖启动前已存在的环境变量，所以这个值优先。启动后先发 1 个请求，
   确认返回的是认证失败（服务端日志里是 `HTTP 401`）而不是正常结果，再开始测。
   只有明确需要真实生成的测试才用真 key 启动，并且事先在报告里说明。
   依据：G-02B 浏览器实测时，文档打开后阅读器自动请求结构摘要，真实调用 1 次（第二次误触发）。

---

## 产品不变量（跨 issue，永远适用）

> **正在读的内容，永远不因为别处的变化而移动。**
>
> 这不是 G-05 的一条验收，是整个产品的地基。任何改变文档高度的操作——
> 撑开、收起、自动收起、保存白话后转常驻、功能二结果插入、编辑白话、
> 侧栏收起、字号行距变化——**都必须先记下视口参照物的位置，改完补偿回去**。
>
> 判据：改动发生在视口**上方**时，可见内容位移必须为 0。
> 只有用户自己滚动，页面才可以动。
>
> 涉及的 issue：G-05、G-10、G-11、G-17、G-19、G-20。每一个都要单独验这条。

---

## 安全底线（另一个项目的 API key 已被盗用过）

1. key 只存在两个地方：本机 `.env.local`（已 gitignore）、Vercel 环境变量。
   代码、注释、日志、报错信息、提交记录里永不出现。
2. 每次提交前查两遍，只数命中次数，不打印内容：
   `(git diff --cached | Select-String -Pattern "sk-[a-zA-Z0-9]{20,}" | Measure-Object).Count`
   `(git log -p --all  | Select-String -Pattern "sk-[a-zA-Z0-9]{20,}" | Measure-Object).Count`
   任一不为 0 即停止提交。
   第一条查「这次要提交的内容」——这是唯一能在 key 进入历史之前拦住它的时机。
   第二条查历史，是事后确认；等它报警时，只要还没推送尚可改写历史，
   推送了就只能作废 key。
   扫描非 ASCII 路径时必须关闭 git 的路径转义（`-c core.quotepath=false`），
   否则会得到一个假的 0。
3. G-07 的两条防护是硬阻塞，不得以「先上线再说」跳过：
   服务端 IP 级速率限制 + 供应商后台消费上限。
4. 上线后每周看一次 DeepSeek 用量曲线。异常尖峰 = 立刻作废 key 重建。
5. 已泄露的 key 只能作废，没有补救。
6. 线上环境变量只在防护措施就位之后才配置。顺序不能反：先限流，后上 key。

---

## 技术骨架

| 层 | 选型 | 说明 |
|---|---|---|
| 框架 | **Next.js (App Router) + React** | 需要服务端跑 AI 调用；部署 Vercel |
| 样式 | **原生 CSS + CSS 变量** | **不用 Tailwind Play CDN**（设计稿用的是它，生产不可用） |
| 文档解析 | mammoth.js (docx) / pdf.js (PDF 文字层) | 客户端解析，原文不出浏览器 |
| 缓存 | Vercel KV / Upstash Redis | key = 句 hash + 上下文指纹 |
| 本地存储 | localStorage 封装 | 原文、白话、功能二结果、阅读位置、设置 |
| 模型 | 功能一快模型 / 功能二强模型 | 见 PRD 3.8 |

### 目录结构

```
gloss/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                      # 书架
│   ├── read/[docId]/page.tsx         # 阅读器
│   └── api/
│       ├── gloss/route.ts            # 功能一
│       ├── explain/route.ts          # 功能二
│       ├── structure/route.ts        # 全书结构提取
│       └── segment/route.ts          # AI 断句（仅无标点文本）
├── components/
│   ├── reader/{Reader,Sentence,GlossPanel,ActionRow,TermMark,ContextMenu}.tsx
│   ├── shelf/{Shelf,Spine,EmptyShelf}.tsx
│   └── settings/SettingsPanel.tsx
├── lib/
│   ├── parse/{docx,txt,pdf}.ts
│   ├── segment.ts                    # 规则切句
│   ├── context.ts                    # 上下文窗口构造
│   ├── cache.ts                      # KV 读写 + hash
│   ├── storage.ts                    # localStorage 封装
│   ├── analytics.ts                  # 埋点
│   └── prompts/{gloss,explain}.ts
├── styles/
│   ├── tokens.css                    # 三套主题 token
│   └── reader.css
└── public/samples/                   # 内置示例文档
```

---

# 📋 Backlog · W1 核心链路

> **W1 目标**：上传 docx → 切句 → 点击 → 撑开出白话，端到端可用。
> **W1 结束时必须有一个能发给别人的链接。**
>
> **W1 已全部完成**：G-01～G-07、G-02B 均已移进 Done，链接为 gloss-jet.vercel.app。
> 更正记录：W1 在 F2 的 AI 断句未实现的情况下宣告完成，遗漏已补记为 G-28；这是漏排，不是有意延期。

---

# 📋 Backlog · W2 完整 MVP

---
### G-46 · 自动收起误收正在读的白话
**P0 · W2** ｜ 依赖 G-05、G-07 ｜ 分支待定

> 2026-09-25 查明：自动收起的 IntersectionObserver 只观察原句 `[data-index]`，
> 不观察撑开区。撑开区在原句下方，读者为了看长白话的后半段往下滚，
> 原句先离开视口顶边，收起即触发：生成中会中止请求，已完成也会把读者正在看的内容收掉。
> 实测证据：原句底边 −15px、面板底边 +85px 时收起触发，白话仅写到 40/148 字。
>
> **Clara 决定（2026-09-25）**：收起判据改为「原句与撑开区都不在视口内才收起」，
> 不按生成中 / 已完成分叉。理由：规则要能一句话讲清、时机要可预期；
> 按状态分叉会造出两种收起时机，读者摸不着规律。
> 采用候选修法 1（观察原句与撑开区的并集）；候选 2（滚动时几何检查）不采用，
> 理由是为同一结果多背一套滚动监听与时序状态。

**可验证增量**：点开一句长白话，往下滚到原句离开视口、白话仍在屏幕上，白话不收起、生成不中止。

**验收**
- [ ] 原句离开视口但撑开区仍有任意部分可见时，不自动收起、不中止请求
- [ ] 原句与撑开区都离开视口时，自动收起并按既有规则中止请求
- [ ] 生成中与已完成两种状态下，收起时机一致
- [ ] 面板挂载、卸载时 observer 正确重建，不泄漏、不重复触发
- [ ] 向上滚动与向下滚动都验：收起时机符合上面三条
- [ ] 收起本身满足产品不变量：收起发生在视口上方时，可见内容位移为 0
- [ ] 不改 G-05 的锚定实现

**会改**：待开工时复述并确认
**回滚**：`git revert`

---
### G-28 · 无标点文本的 AI 断句
**P1 · W2** ｜ 依赖 G-03 ｜ 分支 `feat/g28-ai-segment`

> 补漏（2026-09-18）：PRD F2 将此项列为 W1 的 P0，实际未实现；这是漏排，不是有意延期。
> 本轮决定降为 P1：Clara 实际读的是有标点的 docx 和有文字层的 PDF；无标点主要出现在古籍和 OCR 文本，不阻塞当前使用。
> 本卡不扩展到扫描件 OCR 或古籍文言断句产品能力。

**PRD 原文要求（3.9）**
- B1「无标点 / 乱换行」：**触发 AI 断句**（仅此类文本）。不按字数硬切。横幅提示“已自动断句，可能不准确”。
- B6「AI 断句失败」：回退到规则切分并提示。

**现状与差距（Codex 只读核对，未运行测试）**：阅读器直接调用 `segmentParagraphs`；无标点段落整段成一句。
没有识别与触发、没有 AI 断句调用、没有成功横幅、没有失败回退提示；`app/api/segment` 不存在。
现有规则不按字数硬切，超长且无可切点时保留原句并写警告；这不等于实现 B1 / B6。

**可验证增量**：无标点 / 乱换行样本触发 AI 断句并显示横幅；模拟 AI 失败后仍可按规则结果阅读，且有明确提示。

**验收（均未验）**
- [ ] 识别无标点 / 乱换行文本，仅对此类文本触发 AI 断句；触发判据与样本在开工前确认
- [ ] 不按字数硬切；成功后显示“已自动断句，可能不准确”横幅（B1）
- [ ] AI 断句失败时回退规则切分并提示，阅读不被阻断（B6）
- [ ] 正常有标点文本沿用规则切句，不新增 AI 断句调用

**会改**：待开工时复述并确认；原文外发范围、调用成本及结果存储方案也须先确认
**回滚**：`git revert`

---

### G-29 · 离页中止与请求生命周期
**P1 · W2** ｜ 依赖 G-07、G-11 ｜ 分支 feat/g29-page-leave-abort

> 从 G-15 拆出。G-15 只记录与对账，本卡才实现离页时的中止行为。

**验收**
- [ ] 离开阅读页或切换文档时，本地中止仍在进行的功能一、功能二请求；中止后不再发出新请求
- [ ] 服务端日志记录 abort 事件；已收到的流式文字按既有断流规则保留
- [ ] 不改每日额度计数或用量显示

**会改**：待开工时复述并确认
**回滚**：git revert

---

### G-30 · 每日额度计数
**P1 · W2** ｜ 依赖 G-15 ｜ 分支 feat/g30-daily-quota

> 从 G-15 拆出。额度用于读者用量感知，不承担防滥用；防滥用仍由服务端限流和供应商消费上限负责。

**验收**
- [ ] 按本地日期分别计数功能一 100 次、功能二 20 次；已缓存结果不计入
- [ ] 达到额度时提示“明天恢复”，不发出新的对应请求
- [ ] 跨日期自动进入新计数周期；不改变服务端限流策略

**会改**：待开工时复述并确认
**回滚**：git revert

---

### G-31 · 用量显示
**P1 · W2** ｜ 依赖 G-30 ｜ 分支 feat/g31-usage-display

> 从 G-15 拆出。向读者显示当天剩余额度，不把本地额度误表述为防滥用措施。

**验收**
- [ ] 阅读器显示功能一、功能二当天剩余次数
- [ ] 剩余 ≤10 时显示顶部轻提示，不打断阅读
- [ ] 数字与 G-30 的计数一致；不新增模型调用

**会改**：待开工时复述并确认
**回滚**：git revert

---

### G-32 · 解析器清除零宽字符
**P2 · W2** ｜ 依赖无 ｜ 分支待定

> 解析器的空白归一化应清除零宽字符（U+200B 等）。这些字符不可见、无语义，却会计入字数并进入 prompt。
> 本条来自 G-08 第二批难句入库：第 12 条原文中的 3 个 U+200B 本批保留原样并如实记录；这里只登记后续修复，不在当前评测中处理。

**验收**
- [ ] 解析后的原文不再包含 U+200B 等零宽字符
- [ ] 字数统计与发给 prompt 的文本均不受零宽字符影响

**会改**：待开工时复述并确认
**回滚**：git revert

---


---

### G-33 · 白话硬截断后半句残缺
**P1 · W2** ｜ 依赖 G-07 ｜ 分支待定

> 证据（2026-09-19，第二批评测第 6 条）：原句 184 字，白话写至 150 字上限后停在“需”，读者看到半截话，无法判断是未写完还是出错。
> 优先级 P1：读者直接看得到，且会影响对结果状态的判断。本卡只记录候选方案，不在本轮选择或实现。

**候选方案与代价**
1. 切到最后一个句末标点收尾：内容会少一点，但话完整。
2. 保留硬截断，在面板底部加灰字“原句较长，白话在此收住”：仍给读者半截内容。
3. 对超长原句放宽上限：与 PRD 3.7 的 150 字上限冲突，必须先改 PRD。
4. 超长原句在分号、句中标点处拆成两段，分两次生成、依次展开：两次调用、成本约翻倍；接缝处语气可能对不上，G-06 已实测断点续传接不上语气。

**验收**：待方案确认后细化
**会改**：待开工时复述并确认
**回滚**：git revert
---



### G-26 · UI 精修清单
**P2** ｜ 无依赖 ｜ 分支 `chore/g26-ui-polish`

> 逐条累积，攒够一批一起做。

**验收**
- [ ] 上传失败的提示颜色不够醒目（来源：Clara 在 Preview 上的实际使用反馈，2026-09-18）
- [ ] 补网站图标：`favicon.ico` 目前 404（G-02B 线上验收时发现）
- [ ] 窄视口下三栏布局塌掉：375px 宽时左 184 + 右 140 已占满，中栏只剩 18px，正文逐字换行
      （G-08 测行末移行时发现）。PRD 的主场景下限是 1024px，手机不在 MVP 范围内，
      但至少要有一个可读的降级：比如某个宽度以下自动收起侧栏
- [ ] `.gitignore` 加入 `tmp-*.log`
- [ ] `styles/reader.css` 的 `.action-row-notice` 使用了不存在的 `var(--paper)`，存储满提示背景透明；应改为 `var(--paper-main)`（G-10a 遗留，仅记录，不随 G-10b 修复）
- [ ] 撑开区操作行（保存按钮）在白话完整生成前不显示，但用 `visibility: hidden` 保留占位，避免生成结束时推动下文（违反“生成结束不是用户操作”的不变量）
- [ ] 操作行按钮文案改为未保存时「留下」、已保存时「取消」（替代 G-10c 的「已留下」）；具体用词以开工复述时再确认为准（来源：Clara 2026-09-20 G-10c 手测反馈）
- [ ] 白话显示形态为气泡时，操作行按钮圆角与气泡一致（现为按钮 0、`.gloss-panel-bubble` 6px），应复用同一数值而不是各写一遍；inline 形态保持直角（来源：Clara 2026-09-20 G-10c 手测反馈）
- [ ] 阅读模式的行末「白」字标记混在正文里太不显眼，需要更容易看见，但仍不能抢正文注意力；开工时给出 2–3 个候选让 Clara 挑（来源：Clara 2026-09-20 G-11 亲验反馈）
- [ ] 阅读页对损坏或缺字段的本地文档没有兜底：meta 缺 `charCount` 时 Reader.tsx 的 `documentStats` 抛 TypeError，整页崩溃而不是给提示
- [ ] 书架书脊顶部的导入年月字号 9px 偏小，原型为 10px

**会改**：待开工时复述
**回滚**：`git revert`

---

### G-27 · PDF 目录识别
**P2** ｜ 依赖 G-02B ｜ 分支 `feat/g27-pdf-outline`

> 来源：G-25 定「无标题时文案改为『这份文档没有标题层级』」时留下的后续。
> 现状：PDF 路径一律传 `headings: []`（`lib/parse/pdf.ts`），所以**每一本 PDF 的目录栏都是空的**。
> 优先级 P2 的理由：目录是导航便利，不在 PRD 的 MVP 验收项里（PRD 只在 F20「侧栏收起」里提到过目录），
> 不影响「上传→切句→点击→出白话」主链路；W2 的 P0 / P1 排完再做。

**样本实测**（2026-09-18，`getOutline()`，只读）

| 样本 | 自带大纲 | 内容 |
|---|---|---|
| `pdf-text.pdf` | 有：顶层 1 条 / 共 1 条 / 1 级 | L1「序言」 |
| `pdf-encrypted.pdf` | 有：顶层 1 条 / 共 1 条 / 1 级 | L1「序言」 |
| `pdf-header.pdf` | `null` | 同一份 docx 导出，但没勾「创建书签」 |
| `pdf-scan.pdf` | `null` | — |
| `pdf-mixed.pdf` | `null` | — |
| `pdf-real.pdf` | `null` | 扫描书，文字层是 OCR 的 |

结论：Word 勾选「创建书签」导出的 PDF 才有大纲，真实扫描书通常没有。

**两条可行路径（都在本地完成，不调模型）**
1. **读 PDF 自带大纲**：`doc.getOutline()` 直接给出层级和标题，还带跳转目标（需把目标解析成页码 / 段落下标）。
   零成本、零风险，但覆盖面有限（6 份样本里只有 2 份有）。
2. **按版式启发式识别标题**：字号明显大于正文、独立成行、长度短、居中或缩进异常。
   G-02B 已经拿到每行的字号、行首行末坐标（`PdfLine`），数据是现成的。
   覆盖面广，但会误判（`pdf-real.pdf` 的 OCR 文字层字号在 10–20 之间乱跳，见 G-02B 的已知盲区）。

**明确排除**：调模型生成目录。目录是结构信息，本地就能拿，没必要为此花钱、也没必要把原文再送一次出去。

**验收**
- [ ] 待细化：先做路径 1（自带大纲），再评估路径 2 是否值得
- [ ] 两条路径都拿不到时，左栏仍显示「这份文档没有标题层级」（G-25 定的文案）

**会改**：待开工时复述
**回滚**：`git revert`

---


### G-35 · 跨读者共享的服务端缓存
**阻塞 / 待触发** ｜ 依赖 G-09 ｜ 分支待定

> G-09 只做当前读者浏览器内的 IndexedDB 自动缓存。本卡不得提前实现或预设 Redis / KV 供应商。

**阻塞条件（缺一不可）**
- [ ] 出现多名真实读者，已能证明存在跨读者共享收益
- [ ] Clara 已在控制台设置消费上限

**开工前重新核对**：服务端命中仍消耗 WAF 额度、第三方持久化与隐私边界、供应商费用 / 消费上限、原文与白话外发范围。

**会改**：待开工时复述并确认
**回滚**：`git revert`

---

### G-36 · 修复 scripts/perf 持久化测量流程
**P2** ｜ 依赖 G-09 ｜ 分支待定

**可验证增量**：同一个持久化 Chrome profile 完成 dev 灌缓存后，在同端口 production 页面可读到同源 IndexedDB 条目并产出四组性能 JSON。

**待验证假设（未验证）**：灌缓存与只读检查可能分别使用了 `localhost` 和 `127.0.0.1`；浏览器将两者视为不同源，因而未读到彼此的 localStorage / IndexedDB。

**已知阻塞（仅记录）**：`scripts/perf/README.md` 第 1 步生成的 450 段样本约 9.1 万字，超过上传页 5 万字上限，无法从页面上传；手测时改为上传原序言后在 Console 复制 30 份写入 localStorage。

**会改**：待开工复述并确认
**不改**：本轮不修 `scripts/perf/**`、Reader 或缓存代码
**回滚**：`git revert`

---

### G-37 · 白话人称漂移与劝慰化
**P2** ｜ 依赖 G-08 ｜ 分支待定

> 在第一人称、带情绪的自述句上，白话可能把“我”改成“你”，并加入原文没有的判断和建议。本卡不记录任何触发原文，仓库公开可见。

**离线统计（2026-09-19，v9 评测输出 32 条）**：人称漂移 0 条；12 条第一人称原句中，11 条白话保留“我”。该现象目前只在评测集以外的第一人称情绪性自述中观察到。

**约束**：修复必须针对性处理；不得加入全局强制指令，不得靠多轮调提示词。提示词示例不得取自评测集或 Clara 正在读的书。

**验收**：待开工时复述并确认
**会改**：待开工时确认
**回滚**：`git revert`

---


---

### G-39 · 多句并行生成与繁忙提示
**P2** ｜ 依赖 G-18、G-07 ｜ 分支待定

**背景**：Clara 希望点多句时不必等上一句生成完；现行 PRD D3/D4 是点下一句即中止上一句请求；需与 G-18 多句锁定、G-07 服务端限流一起设计并发上限和「繁忙」提示。

**验收**：待开工时复述并确认
**会改**：待开工时复述并确认
**PRD 本轮不修改**。
**回滚**：`git revert`

---

### G-38 · 单句重新生成
**P1** ｜ 依赖 G-10a ｜ 分支待定

**待定事项**：重生成的新版本若未保存，下次打开会回到旧版，读者可能不知道；倾向方案是新版本标「新版本 · 未保存」并提供「用这一版」，点了才算保存。

---

### G-41 · 白话与整句理解分别折叠
**P2** ｜ 依赖 G-11 ｜ 分支待定

> 来源：Clara 2026-09-20 G-11 亲验反馈，“不急”。用过「听不懂」的句子，撑开区里同时有白话和整句理解，读者可能只想看其中一个。
**验收（开工复述时细化）**

- [ ] 白话框和整句理解框各有一个折叠图标，读者可以折叠其中一个，只看另一个
- [ ] 折叠和展开都满足视口不变量
- [ ] 只用一个图标，不加文字按钮

**会改**：待开工时复述
**回滚**：git revert

---

### G-12 · 右键菜单与「翻错了」
**P0** ｜ 依赖 G-11 ｜ 分支 `feat/g12-context-menu`

**可验证增量**：右键呼出菜单，点「翻错了」上报成功，**撑开区保持打开，阅读不被打断**。

**验收**
- [ ] 右键菜单含：听不懂 / 翻错了 / 查看上下文原文（后者仅覆盖模式下出现）
- [ ] 上报内容：句 hash + 原句 + 白话 + 输出类型 + 时间戳
- [ ] **上报后撑开区保持打开**
- [ ] 隐私说明中明示「翻错了」会携带原句明文

**会改**：`components/reader/ContextMenu.tsx`、`app/api/report/route.ts`、`lib/analytics.ts`
**不改**：`components/reader/GlossPanel.tsx`
**回滚**：`git revert`

---

---

# 📋 Backlog · W3 打磨与埋点

> ⚠️ **W3 超载：3 个 P0 + 8 个 P1，共 11 项，大概率做不完。**
> P0 必须完成（少了任何一个，W4 的数据复盘无法进行）。
> P1 按下方标注的**推迟顺序**砍，**W4 不允许被挤占**。

---

### G-14 · 导出 docx
**P0** ｜ 依赖 G-10 ｜ 分支 `feat/g14-export-docx`

**可验证增量**：导出一份 docx，用 Word 打开，原文完整，3 条白话按设置形态排在对应原句后。

**验收**
- [ ] 四种纸面形态可选：行下小字（默认）/ 批注框 / 脚注 / 双栏对照
- [ ] **带白话的句子与其白话不跨页**（H4）
- [ ] 无已保存白话时提示，询问是否仅导原文（H1）
- [ ] 大文档分段导出 + 进度提示（H2/H3）

**会改**：`lib/export/docx.ts`、`components/settings/SettingsPanel.tsx`
**不改**：`components/reader/**`、`app/api/**`
**回滚**：`git revert`

---

### G-15 · 全量埋点
**P0** ｜ 依赖 G-13 ｜ 分支 `feat/g15-analytics`

**可验证增量**：走一遍完整流程，后台能看到 PRD 4.1 表里的全部事件，且失败原因分类正确。

**验收**
- [ ] PRD 4.1 的全部事件均已接入
- [ ] **上传失败能按 A1–A9 分类统计**
- [ ] **生成失败能按 C1–C6 分类统计**
- [ ] 北极星（白话读完率）可计算
- [ ] `sentence_reclick` 与 `lock_expand` 可关联查询（否则前者会误导）
- [ ] 接入 G-09 留出的命中 / 未命中状态，发送 `sentence_click.cache_hit`；本项只接通埋点，不改变缓存行为
- [ ] 无 PII 上报
- [ ] **线上首字延迟 P90 ≤2.5s**（来自 G-07：G-07 只有本地实测）
- [ ] **线上单次调用成本**，用于校准 PRD 3.8 的额度阈值（来自 G-07：G-07 只有本地日志的数字）
- [ ] **中止用量核对**：以服务端 abort 事件对照 DeepSeek 用量；上游在中止前已处理的 token 可能仍计费，不承诺零费用
      中止行为的验收口径为：a. 中止后本地不再发出新请求；b. 服务端日志记录 abort 事件。
      G-15 只负责记录与对账，不实现离页中止行为；离页中止、每日额度计数、用量显示分别移交 G-29、G-30、G-31。
      本轮第 6 条触发中止时 usage 为 null；中止前已处理 token 的费用无法从日志还原，实际扣款可能高于已知的 ¥0.00454。这是中止场景下日志口径缺口，纳入本项对账。
- [ ] 接入 G-11 派发的 gloss:analytics 事件 deep_explain_blocked（点击已置灰的「听不懂」），计入 PRD 4.1 埋点（从 G-11 移交，2026-09-20）

**会改**：`lib/analytics.ts`、各组件埋点调用点
**不改**：任何业务逻辑。**本 issue 只加埋点，不改行为**
**回滚**：`git revert`

---

### G-16 · 内置示例文档与首次引导
**P0** ｜ 依赖 G-13 ｜ 分支 `feat/g16-sample`

**可验证增量**：首次访问 → 点示例书 → 第一个难句有呼吸动效 → 点一下就理解了全部交互。**全程无需上传任何文件。**

**验收**
- [ ] **不做 tooltip 教程**
- [ ] 示例文本第一个难句自带轻微呼吸/高亮动效
- [ ] **该难句必须含术语标记**（让用户第一次就见到功能二）
- [ ] 版权路径已落实：公有领域原文 + 自行生成的新中译
- [ ] **产品中不出现任何现行译本的署名**
- [ ] 示例书在书架上明确标记为「示例」

**会改**：`public/samples/**`、`components/shelf/EmptyShelf.tsx`、`components/reader/Sentence.tsx`
**不改**：`lib/**`、`app/api/**`
**回滚**：`git revert`

---

### G-17 · 编辑白话
**P1 · 最不该推迟** ｜ 依赖 G-10 ｜ 分支 `feat/g17-edit`

> 「用自己的话重述」就是理解发生的那一刻，这是产品最有价值的动作之一。

**可验证增量**：双击撑开区进入编辑，改写后保存，刷新仍是编辑版本。

**验收**
- [ ] 双击进入编辑；虚线边框区分编辑态
- [ ] 保存后以编辑版本为准
- [ ] **编辑版本绝不写入服务端共享缓存**（D12）
- [ ] 编辑中点击别处 → 提示未保存（G7）
- [ ] 编辑后为空 → 视为删除该白话（G8）
- [ ] 埋点 `gloss_edit` 记录编辑前后字数与差异度

**会改**：`components/reader/GlossPanel.tsx`、`lib/storage.ts`、`lib/analytics.ts`
**不改**：`lib/cache.ts`（**关键：不得让编辑版本流入缓存**）
**回滚**：`git revert`

---

### G-18 · Ctrl 锁定
**P1 · 最不该推迟** ｜ 依赖 G-05 ｜ 分支 `feat/g18-lock`

**可验证增量**：Ctrl+点击后，点别处、滚出视口都不收起；边框加深；再次 Ctrl+点击解锁。

**验收**
- [ ] **界面中不存在任何锁定/解锁按钮或图钉**
- [ ] 唯一视觉信号：**撑开区外边框加深**
- [ ] 再次 Ctrl+点击解锁
- [ ] 多句锁定可并存（G9）
- [ ] 操作行末尾有极淡灰字提示「Ctrl + 单击锁定」——**是提示不是按钮**
- [ ] 同一段有多个撑开区时，所有拆分点必须基于原始排版一次算出，
      不得在已拆分的 DOM 上逐个计算
- [ ] 两句在同一行结束时，两个撑开区按句子顺序上下相邻插入，不合并

**会改**：`components/reader/{GlossPanel,ActionRow}.tsx`、`styles/reader.css`
**不改**：`lib/**`
**回滚**：`git revert`

---

### G-19 · 阅读主题与字体
**P1 · 推迟顺序 4** ｜ 依赖 G-01 ｜ 分支 `feat/g19-themes`

**可验证增量**：右栏切换三套主题，**所有元素**（正文、白话、术语、分隔线、侧栏）同时变化，无任何一处留着上一套的颜色。

**验收**
- [ ] 三套完整 token：羊皮纸 / 月白 / 护眼绿
- [ ] **切换时全部 6 个变量一起换**，不是只换背景（D16）
- [ ] **中栏最亮、侧栏退后一档**，不压暗中栏
- [ ] 三套主题下白话对比度均 ≥4.5:1（实测值 6.6 / 6.3 / 6.1）
- [ ] 切换用 `transition`，不闪变
- [ ] 选择记住（localStorage）
- [ ] 可切换三套中文字体：思源宋体（默认）、霞鹜文楷、文津宋体，均采用 SIL OFL 1.1；后两套按 `unicode-range` 分包，切到哪套才加载哪套
- [ ] README 记录字体来源与许可；所有 `font-family` 通过 `styles/tokens.css` 变量引用

**会改**：`styles/tokens.css`、`components/settings/SettingsPanel.tsx`、`lib/storage.ts`
**不改**：任何组件的结构。**本 issue 只动 CSS 变量与切换逻辑**
**回滚**：`git revert`

---

### G-20 · 侧栏收起（沉浸阅读）
**P1 · 推迟顺序 5** ｜ 依赖 G-04 ｜ 分支 `feat/g20-collapse`

**可验证增量**：`Cmd/Ctrl + \` 收起两栏，**中栏宽度不变、只是居中**，两侧留白增加。

**验收**
- [ ] 左右两栏**可独立收起**
- [ ] 快捷键 `Cmd/Ctrl + \`；栏边缘有 hover 才显现的折叠把手
- [ ] **中栏宽度不变，仅居中。行长仍约 38 字**（D15）
- [ ] 状态记住（localStorage）
- [ ] 收起/展开有平滑过渡

**会改**：`components/reader/Reader.tsx`、`styles/reader.css`、`lib/storage.ts`
**不改**：`components/reader/{Sentence,GlossPanel}.tsx`
**回滚**：`git revert`

---

### G-21 · 覆盖模式
**P1 · 推迟顺序 3** ｜ 依赖 G-08 ｜ 分支 `feat/g21-overlay`

**验收**
- [ ] 白话替换原句位置显示，白底区分
- [ ] **hover 到白话上临时显示原句**（否则违反"验证成本接近零"）
- [ ] 右键可「查看上下文原文」
- [ ] 设置中可切回撑开模式

**会改**：`components/reader/GlossPanel.tsx`、`styles/reader.css`、`components/settings/SettingsPanel.tsx`
**不改**：`lib/**`
**回滚**：`git revert`

---

### G-22 · 视图切换（书架 ⇄ 列表）
**P1 · 推迟顺序 2** ｜ 依赖 G-13 ｜ 分支 `feat/g22-view-toggle`

**验收**
- [ ] 书架视图 ⇄ 列表视图可切换，选择记住
- [ ] 列表视图显示：标题、字数、已保存白话数、最后打开时间

**会改**：`components/shelf/**`、`app/page.tsx`
**不改**：`components/reader/**`
**回滚**：`git revert`

---

### G-23 · 导出 PDF
**P1 · 推迟顺序 1（最先砍）** ｜ 依赖 G-14 ｜ 分支 `feat/g23-export-pdf`

**验收**
- [ ] 四种纸面形态与 docx 一致
- [ ] 带白话的句子不跨页

**会改**：`lib/export/pdf.ts`
**不改**：`lib/export/docx.ts`
**回滚**：`git revert`

---

### G-42 · 阅读页视觉对齐
**P1** ｜ MVP 最后一步（W2 收尾，W4 用户测试前）｜ 依赖 `docs/stitch-reader/code.html`、`docs/stitch-reader/screen.png` ｜ 分支待定

**范围**：纯视觉对齐：字体、小标签的等宽质感、文章标题块、操作行样式、页脚收尾、左右栏排版。

**排除**：稿子里画但尚未实现的导出 docx/pdf（G-14/G-23）、覆盖模式（G-21）、编辑（G-17）、Ctrl 锁定（G-18）；不编造作者与年份、版本、断句方案、平均句长、P90 延迟、缓存命中率；不做多级章节目录（目录来自文档自带标题层级）。字体引入机制在本卡落地：默认思源宋体，用 `next/font` 自托管，所有 `font-family` 走 `styles/tokens.css` 变量。

**验收**
- [ ] 依照 Stitch 稿逐项完成上述视觉范围并由 Clara 浏览器验收

**回滚**：`git revert`

---

### G-43 · 本地校验流程修复
**P2** ｜ 分支待定

**验收**
- [ ] 解决 `DEEPSEEK_BASE_URL` 只在 development 生效的问题，使 `next start` 下也能用本地替身验证 401，并证明未调用真实上游

**回滚**：`git revert`

---

### G-44 · 展开的封面无法右键
**P1** ｜ 分支待定

> Clara 2026-09-21 实机发现：hover 展开后封面盖住书脊，右键落在封面上出现浏览器菜单，换色和移除入口不可达。

**验收**
- [ ] 展开的封面支持与书脊相同的右键菜单，仍可换色与移除，不弹浏览器默认菜单

**回滚**：`git revert`

---

### G-45 · 自定义域名上线
**P1 · 硬截止：W4 招募第一批用户之前** ｜ 依赖 G-07 ｜ 分支待定

> Clara 已购入 .com 域名。换域名必须在把链接发给真实用户之前完成：
> 一旦第一批用户停在 gloss-jet.vercel.app 上，之后再换域名等于丢掉这批样本，
> 而 7 日回访率是全项目零验证的最大假设，样本丢不起。

**开工前需要 Clara 提供**：域名本身、域名注册商后台的访问方式。域名不是密钥，可以写进仓库。

**开工时必须先核对的四件事**（复述阶段回答，不要边做边发现）
1. G-07 的服务端 IP 级速率限制绑在哪一层：Vercel 项目还是具体域名。换域名后是否自动跟随。
2. 代码、环境变量、埋点上报地址里有没有硬编码的 vercel.app 域名或绝对 URL。
3. 旧域名的处置：保留可访问还是 301 跳转。两种做法对已分享出去的链接各是什么后果。
4. 本地存储按来源隔离：换域名后读者在旧域名下存的书、白话、阅读位置在新域名上读不到。
  当前只有一个读者，影响不大，但必须写进报告。

**验收**
- [ ] 新域名可访问，HTTPS 证书就绪，不出安全警告
- [ ] G-07 的限流在新域名上确认仍然生效（发请求触发一次，看到拦截）
- [ ] 旧域名按上面第 3 点确定的方案处置，行为与决定一致
- [ ] 仓库里不存在硬编码的旧域名

---

# 📋 W4 · 不写新功能

> **W4 是整个作品集里最值钱的部分，不允许被 W1–W3 的延期挤占。**

| 天 | 事项 |
|---|---|
| 1–2 | 招募 ≥10 名真实用户试用（发布 demo 链接到求助场景社区，**不是读书博主聚集地**） |
| 3–4 | 收集数据 + 定性访谈 |
| 5 | 数据复盘：北极星、失败原因分布、`gloss_edit` 与 `report_error` 的句子特征 |
| 6–7 | **做一轮有数据支撑的迭代**，并记录前后对比 |
| — | 撰写 Case Study |

**W4 必须回答的三个问题：**
1. 白话读完率是多少？没达标的话，是太长、太慢、还是没说清？
2. 「翻错了」上报集中在哪类句式？prompt 该怎么改？
3. **7 日回访率是多少？「长期阅读器」这个定位到底成不成立？**（这是全项目零验证的最大假设）

---

## 🔍 已知盲区

> 盲区是「不知道」，缺陷是「知道坏了」。这里列的都没有验证过，不要当成 bug，也不要当成已经查过。

| 盲区 | 为什么不知道 | 处置 |
|---|---|---|
| G-05 的行尾拆分方案未在 Safari 验证 | 只在 Chrome 152 上逐字比对过；Safari 的 `text-align-last` 与 `text-justify: inter-ideograph` 行为未知，方案可能失效 | 上线前找一台 Mac 验一次；在此之前**不宣称支持 Safari** |
| 被中止的请求是否计入 WAF 限流计数 | 请求已到达边缘节点，但 Vercel 文档没有说明；没有实测 | 估算额度时**按计入**处理（收起、快速切换句子都算一次） |
| WAF 403 的响应头和正文 | 没见过真实响应的内容，只验证过状态码 | 前端只按状态码 403 判断，不依赖响应内容 |
| PDF 字符映射错位但输出的仍是正常汉字 | 乱码检查按字符类型判断（替换符、私用区、控制字符），错位成别的汉字时检测不出来 | 无样本，暂不处理（G-02B） |
| PDF 乱码阈值 5% | **未经样本校准**：5 份样本的乱码比例全部是 0% | 拿到文字层有问题的 PDF 后重新标定（G-02B） |
| PDF 扫描书的 JPX / JBIG2 图片 | 标准字体和 wasm 解码器没有托管，这类图片可能识别不出来，图片面积算不出，逐页判定失效 | 靠「全书平均每页少于 20 字 → A5」兜底；混合型书里这类扫描页会被当成字少的文字页，不出 A6（G-02B） |
| 有大插图、字很少的 PDF 页（封面、标题页） | 样本里没有这种页 | 会被判成扫描页，出 A6 横幅，这一页的几个字被跳过，不阻断（G-02B） |
| 只有一两页、字确实很少的 PDF | 样本里没有；兜底按全书平均每页字数判断 | 平均每页少于 20 字会被误报成扫描件（A5）（G-02B） |
| docx 公式检测依赖 mammoth 的警告文本 | 判断条件是 messages 里出现 `2006/math` 命名空间的 unrecognised element；升级 mammoth 后文本可能变化 | `lib/parse/docx.test.ts` 用 `docx-table-formula.docx` 与构造消息锁住，升级时跑测试就能发现（G-24） |
| PDF 页眉去除和段落拼接规则换一本书是否还有效 | 只在 1 本真实书（`pdf-real.pdf`，14 页）和 Word 导出的序言样本上调过；排版不同的书（双栏、竖排、页眉在侧边、段间无缩进）效果未知 | 拿到更多真实书样本后重新评估（G-02B） |

---

## ⚠️ 已知风险

> 风险是「知道可能出问题，暂时接受」。和盲区不同，这里的每一条都已经想清楚了后果。

| 风险 | 后果 | 处置 |
|---|---|---|
| WAF 限流按 IP 计数 | 共用出口 IP 的读者（校园网、公司网络、运营商共享地址）共享 10 分钟 30 次的额度，几个人同时读会很快被拦 | 暂不改（Hobby 只有一条规则，放宽会让单个 IP 的最坏花费同比例翻倍）。**G-15 做用量显示时重新评估** |

---

## ✅ Done

_（完成的 issue 移到这里，保留验收清单）_
### G-40 · 功能一流式增长时的视口上方补偿
**P1** ｜ 依赖 G-07 ｜ 分支 `codex/g40-stream-viewport-anchor` ｜ 完成：2026-09-26 ｜ 合并 commit：`88bddc9c32caf183e79674b98a6a552cea4a6b26`

> 2026-09-20 G-11 开工核查时发现：Reader 的 onText 直接更新白话，主锚定事务不依赖流式白话状态；GlossPanel 的 min-height 只防收缩。读者在生成期间往下滚，撑开区到了视口上方后继续长高，会推动可见内容，违反产品不变量。功能二已在 G-11 用“整块面板在视口上方才补偿”的规则修复，本卡让功能一采用同一规则。
**验收**

- [x] 功能一生成中，撑开区整块在视口上方时继续长高，可见内容位移为 0
      —— 2026-09-25 查明：面板在原句下方，面板整块到视口上方时原句已离开视口，
      自动收起已触发并中止生成，**该场景在当前产品流程中不可达**。
      G-46 改为「原句与撑开区都离开视口才收起」之后，该场景仍然不可达。
      补偿实现与隔离测试（G40_PROBE_ABOVE / G40_DISABLE_COMPENSATION）作为回归防线保留，
      供 G-29、G-39 改动自动收起时机后重新启用。
      反证实测：关闭补偿 36 次增高累计漂移 +1043.03px 且单向；开启补偿为 0px。
- [x] 视口顶边切过撑开区时，撑开区向下生长，区内文字不动
- [x] 撑开区在视口内时，保持 G-07 行为（撑开区及上方不动，下方被推开）

**实测记录**：真实面板宽度下 148 字白话约 5 次增高（估算）；表中的 36 次是把面板宽度压到 58px 得到的压力测试值，用于验证补偿累计不漂移，不是真实场景的增高次数。隔离测试 3 个场景各 3 遍，每遍实测 36 次增高，逐次最大位移、累计最大位移、带符号逐次位移和均为 0px。

**会改**：`components/reader/Reader.tsx`、`components/reader/GlossPanel.tsx`、`components/reader/Reader.test.ts`、`scripts/dev-slow-gloss.mjs`、`scripts/verify-g40.mjs`
**回滚**：git revert

---

### G-13 · 书架
**P0** ｜ 依赖 G-10 ｜ 分支 `feat/g13-shelf` ｜ 完成：2026-09-21 ｜ 合并 commit：`4b561b586e6b3c0bfcf7bacdada49c6b43f44282`

**可验证增量**：首页显示已读文档的书脊，点击进入并回到上次位置；空态显示导入入口。本卡原验收中的「空态示例书」移交 G-16。

**验收**
- [x] **顶部「继续读」入口**：显示上次读的书名 + 停留位置，一次点击直接进正文原位置，不经过详情卡
- [x] 书脊横向排列，宽窄错落；宽度不编码任何信息
- [x] 书脊过多时横向滚动，不换行、不缩小字号
- [x] 选中的书凸出为封面，页面背景色跟随该书
- [x] 书脊颜色默认随机，右键可自定义
- [x] 不显示阅读进度
- [x] 空态：导入入口居中（示例书明确标记的验收移交 G-16，本卡不实现）
- [x] 「添加新书」入口放在书架上，不在阅读界面单加按钮
- [x] 点击最近文档跳转至上次阅读位置
- [x] 所有功能性文案为白话中文；装饰性英文仅作视觉纹理，不作为任何可点击元素的唯一标签
- [x] 页面上不存在 MVP 之外的入口（无「索引」「校勘」「导出」等）

**Clara 亲验 / 浏览器实测（2026-09-21）**：实际宽度居中与溢出、右缘渐隐、书架板随滚动延展、书脊错落与间距、hover/点击封面、封面 280×440、封面与书脊间隙 16px、整块封面可点击、详情卡 hover、右键菜单、换色持久化、删除隔离清除、导入弹窗、空书架与计数隐藏、点击另一书脊关闭菜单。

**Codex 自测**：单元测试覆盖索引迁移只增不改、幽灵书过滤、删除关联数据与自动白话缓存隔离；`npm test`、`tsc`、对比度测试通过。浏览器未亲自验证的项目不在此处宣称已验。

**会改**：`components/shelf/{Shelf,Spine,EmptyShelf}.tsx`、`app/page.tsx`、`lib/storage.ts`、`styles/tokens.css`、`styles/shelf.css`、`components/reader/Reader.tsx`、`styles/reader.css`
**不改**：`KANBAN.md`（原分支开发阶段）以外的产品文档；本卡合并后仅更新本记录
**回滚**：`git revert 4b561b5`

### G-11 · 功能二（每句可用与置灰态）
**P0** ｜ 依赖 G-08 ｜ 分支 `feat/g11-explain`

**可验证增量**：每一句的操作行都有「听不懂」，不以有没有标记为条件；点击出整句理解，用过后按钮置灰，收起再打开结果仍在。

> 2026-09-18 决定：标记稀少是已知事实，入口不能因此限制整句理解的可达性；入口本身不调用模型，点击才调用。
> 本决定替代 PRD D14 的条件显示规则；每句一次、不可追问的约束不变。PRD 本轮不修改。
>
> **2026-09-20 Clara 内容决定**：整句理解不能把目标句逐句换一种说法；必须在结构逻辑、隐含意图、语境中至少落到一项，仍以这一句为落点，不写整段或整章概要。请求带读者已看过的白话（去掉术语定界符），让模型不重复字面意思；上下文扩为上一段、当前段、下一段及全书结构摘要。判断结构和意图必须有输入依据；背景只写公认且必要的常识，不编造细节。不足以判断更深含义时，如实说明该句在上下文里的作用；只有连作用也无法判断时才拒答。提示词从 `explain-v3` 起执行；本卡最多再调整两轮 prompt，之后无论结果如何均记录并转新卡。

**实现决定记录（2026-09-20）**
1. 逐句出现：服务端缓冲到完整句（。！？）并通过检查后，以 NDJSON sentence 事件发给前端；第一句就超过 250 字、没有完整句或尾部是半句的情况都不保存、不算用过。
2. 超过 250 字或命中延伸句式时，从该句起丢弃，并中止上游；已发出至少一句时算成功。分别记录 `explain_truncated` / `explain_extension_cut` 日志，只记字数和句数。
3. 生成中收起不中止请求，请求状态放在 Reader 层；结果正常结束后照常保存并置灰。离页中止归 G-29。
4. 「听不懂」跟随操作行：白话写完后出现，未就绪时用 `visibility: hidden` 占位；功能一最终失败时也可以用。
5. 已用的判定：流正常结束、通过检查并成功写入 `gloss:explain:${docId}` 才算用过；身份为 docId + paraIndex + start + 句子 hash。
6. 逐句追加时，只有整块面板都在视口上方时才做字符锚定补偿；视口顶边切过面板时，面板向下生长。
7. 输入：上一段 + 当前段 + 下一段，合计不超过 2000 字；当前段最多占 1200 字，围绕目标句裁剪；剩余预算按两侧实际需要分配。请求携带读者已读的白话（去掉 ⟦⟧），没有白话时明确告知模型。
8. 模型输出先做 Markdown 清洗，再识别拒答标记【无法作答】；拒答和 WAF 限流不给「重试」。
9. 提示词版本：explain-v3 通过 Clara 亲验；v1、v2 存档。亲验观察：马克思文本密度高，几乎没有纯过渡句，v3 对疑似过渡句也给出了实质理解，Clara 认可。

**验收**
- [x] **每一句操作行都显示「听不懂」，不以有没有标记为条件**；仅展示入口不产生模型调用——Clara 亲验 2026-09-20
- [x] 按钮标签固定为「听不懂」三字，无术语名、无箭头——Clara 亲验 2026-09-20
- [x] 输出 ≤250 字，**不含延伸句式**，**界面中不存在任何输入框**——Clara 亲验 2026-09-20
- [x] **每句只能用一次**；用后置灰禁用——Clara 亲验 2026-09-20
- [x] **结果常驻**；收起再打开仍在；刷新后仍在；**永不重新生成**——Clara 亲验 2026-09-20
- [x] 点击已置灰按钮无反应、不发请求——Clara 亲验 2026-09-20；『记录埋点』部分移交 G-15（本卡已派发 gloss:analytics 事件 deep_explain_blocked，G-15 负责接入正式采集）
- [x] 用强模型，与功能一分开计费（模型 deepseek-v4-pro，路由 /api/explain，与 /api/gloss 分开）——Clara 亲验 2026-09-20

**会改**：`app/api/explain/route.ts`、`lib/prompts/explain.ts`、`components/reader/ActionRow.tsx`、`lib/storage.ts`
**不改**：`app/api/gloss/route.ts`、`lib/prompts/gloss.ts`
**回滚**：`git revert`

---

### G-10c · 保存白话（阅读与复习模式）
**P0** ｜ 依赖 G-10b ｜ 分支 `feat/g10c-reading-review`

**可验证增量**：保存三条白话，刷新页面后仍在，左栏「本文沉淀」显示 3 条。

**验收**
- [x] 左栏「本文沉淀」条目摘要两行 `line-clamp-2`（从 G-04 移来：那时无真实数据，验不了）——Clara 亲验 2026-09-20
- [x] 阅读模式：折叠为行末微标记；复习模式：全部展开——Clara 亲验 2026-09-20
- [x] 两种模式是显式切换，不是自动——Clara 亲验 2026-09-20
- [x] 本卡只新增阅读／复习模式切换；其余设置项归属各自的卡：白话显示形态 G-10b、导出形态 G-14、纸面底色 G-19、呈现模式 G-21——Clara 亲验 2026-09-20
- [x] 左栏摘要不得新增随滚动变化的 state 或 effect——Clara 亲验 2026-09-20
- [x] 点常驻白话的框本身（不只是原句）也能显示／隐藏操作行，规则同 G-10b 决定 2（来源：Clara 2026-09-20 G-10b 手测反馈）——Clara 亲验 2026-09-20
- [x] 操作行按钮字号与白话统一为 14px（来源：Clara 2026-09-20 G-10b 手测反馈）——Clara 亲验 2026-09-20
- [x] 按钮文案「保存 / 已保存」改为「留下 / 已留下」，让读者看出白话会留在页面上；PRD F7 本轮不修改（来源：Clara 2026-09-20 G-10b 手测反馈）——Clara 亲验 2026-09-20

**决定记录（2026-09-20）**
1. 阅读／复习模式为全局设置，键 `gloss:settings:reading-mode`，值 `"reading" | "review"`，默认 `"reading"`；与 `gloss:settings:gloss-shape` 相互独立。
2. 行末微标记为「白」字按钮，14px，颜色 `--saved-inline`，只挂在句子最后一个片段之后。
3. 阅读模式下最多展开一条（`expandedSavedIndex`），切换模式时重置。
4. 左栏「本文沉淀」条目点击后跳转并展开该条。
5. 阅读模式下刚点「留下」的那条保持展开，点其他微标记或切换模式时折叠。

**会改**：`components/reader/**`、`components/settings/SettingsPanel.tsx`、`styles/reader.css`
**不改**：`lib/cache.ts`、`app/api/**`、保存区身份与写入协议
**回滚**：`git revert`

---

### G-10b · 保存白话（常驻呈现）
**P0** ｜ 依赖 G-10a ｜ 分支 `feat/g10b-persistent`

**验收**
- [x] 保存后转为常驻；形态可在设置中选（气泡 / inline 小字，默认后者）——Clara 亲验 2026-09-20
- [x] 一页 10+ 保存句常驻时，正文连贯性不被破坏，位移验收为 0——Clara 亲验 2026-09-20
- [x] 同一段有多个撑开区时，所有拆分点必须基于原始排版一次算出，不得在已拆分的 DOM 上逐个计算——Clara 亲验 2026-09-20
- [x] 两句在同一行结束时，两个撑开区按句子顺序上下相邻插入，不合并——Clara 亲验 2026-09-20

**会改**：`components/reader/**`、`components/settings/SettingsPanel.tsx`、`styles/reader.css`、`styles/tokens.css`、`lib/theme-contrast.test.ts`；多展开区底层与 G-18 共用
**不改**：`lib/cache.ts`、`app/api/**`、保存区身份与写入协议

**性能实测**：Clara 实测，本地 production，450 段样本约 30 条常驻白话，开书拆分测量 7 次：中位数 4.5ms，最大 7.5ms。

**Clara 决定（2026-09-20）**：
1. 气泡常驻进入文档流，和 inline 同样插在句末所在行的下一行行首；它是带框白话块，不做浮层。
2. 点已常驻句只显示／隐藏该常驻区下方操作行；它占用唯一非锁定态，点别处、Esc、滚出视口、再点同句只隐藏操作行，点另一句同事务先隐藏它。
3. 取消保存后原地退回普通临时撑开区，保留文本；异步写入完成时重新抓阅读锚补偿。
4. inline 使用 14px `--saved-inline`：羊皮纸 `#666057`、月白 `#626269`、护眼绿 `#58675A`；左竖线与普通撑开区相同。
5. 气泡同为 14px `--saved-inline`，仅有 1px `var(--rule)` 边框、6px 圆角、`fit-content`／`max-width: 100%` 与约 `0.3em 0.75em` 内边距；无左竖线、底色、阴影。
6. 白话显示形态默认 inline；键为 `gloss:settings:gloss-shape`，值为 `"inline" | "bubble"`，缺失或非法值回退 inline；SettingsPanel 只让这一项可切换。
7. 点「保存」写入成功后，常驻区操作行继续显示「已保存」；之后按 A1 的点别处、Esc、滚出视口、再点同句规则隐藏。
8. 白话统一 14px（临时撑开区与常驻区同字号，仅颜色区分）；PRD 3.10「白话与正文同字号」本轮不修改，以本决定为准。
9. 白话显示形态作用于所有白话：inline 都是左竖线小字，bubble 都是带框小字；已保存与未保存只以 `--saved-inline`／`--gloss` 颜色和「已保存」／「保存」操作行区分。切换同一锚定事务补偿、不得重量拆分点。PRD F7「保存形态」本轮不修改，以本决定为准。

**PRD 本轮不修改**。
**回滚**：`git revert`

> W1 完成记录更正：W1 在 F2 的 AI 断句未实现的情况下宣告完成，遗漏已补记为 G-28；已完成 issue 的勾选保持不变。

### G-10a · 保存白话（数据层）
**P0** ｜ 依赖 G-08 ｜ 分支 `feat/g10a-save-data`

**可验证增量**：保存一句白话、清除自动缓存、关闭标签页重开同一本书后，点该句仍显示当时保存的同一版本；取消保存后刷新再点，重新生成。

**验收**
- [x] 保存版优先于自动缓存与现场生成；刷新后恢复（D5）——Clara 亲验（本地 slow-gloss）
- [x] 保存记录按 `docId + paraIndex + 段内起始下标 + 句子 hash` 识别；不一致时不显示、不删除——Codex 自测（单元测试）
- [x] 保存的是当时显示的原样白话（含术语定界符），自动缓存更新不改变保存版；类型字段预留编辑版——Codex 自测（单元测试）
- [x] 保存入口只在完整白话放出后可用；写入成功后才显示「已保存」，再点取消保存——Clara 亲验（本地，失败态按钮不可点）＋ Codex 自测
- [x] E1/E2：存储满或被禁用时提示，功能不阻断；导出入口依赖 G-14，本卡不实现——Codex 自测（单元测试）
- [x] 设置面板只有“清除自动缓存”：两步确认、只清 IndexedDB 自动缓存，不删除已保存白话、编辑版本、阅读位置、原文或功能二结果；当前内存预载 Map 不变，下次开书生效——Clara 亲验（本地 slow-gloss）

**会改**：`lib/segment.ts`、`lib/storage.ts`、`lib/cache.ts`（仅新增清除全部自动缓存导出）、`components/reader/{Reader,GlossPanel,ActionRow}.tsx`、`components/reader/Reader.test.ts`、`components/settings/SettingsPanel.tsx`、`styles/reader.css`
**不改**：G-09 的缓存键、查找、写入、预载与过期清理；`app/api/**`、提示词、导出、编辑 UI
**身份说明**：保存白话的身份将 PRD 3.8「句 hash」细化为 `docId＋paraIndex＋段内起始下标＋句子 hash`；PRD 本轮不修改。
**合并记录**：PR #3 的 `b9e84b5 Merge pull request #3 from clarachan1123/feat/g10a-save-data`。
**遗留**：
- 无效 key 时服务端日志的 `detail` 是否为 `"HTTP 401"` 未核实（上游 401 在 `lib/deepseek.ts` 统一转为 502，401 只出现在 detail 字段）；下次本地起服务时由 Clara 在自己的 PowerShell 日志中确认。
- 本地 slow-gloss 必须用 [http://localhost:3430](http://localhost:3430) 访问；用 127.0.0.1 时 Next 16 拦截开发资源，页面无法交互。
- 仓库根目录的 `tmp-*.log` 测试日志未被 `.gitignore` 覆盖，记入 G-26。
**回滚**：`git revert`

### G-09 · 跨会话缓存
**P0** ｜ 依赖 G-06 ｜ 分支 `feat/g09-cache`

**完成（Clara 线上复查通过，2026-09-19）**：当前读者浏览器 IndexedDB 自动缓存；不同读者不共享。

**合并记录**：PR #1 的 `104ff6d Merge pull request #1 from clarachan1123/feat/g09-cache`；R2 修订的 `72808d1 Merge pull request #2 from clarachan1123/fix/g09-lookup`。

**R2 修订（2026-09-19）**：优先带摘要条目，没有就回退无摘要条目，避免重开书时因结构摘要已持久化而二次生成。

**验收**
- [x] IndexedDB 数据库 `gloss-auto-cache` 的 object store `ai-gloss-auto-cache`；键含版本、docId、上下文 SHA-256、模型参数与是否带结构摘要，键和值均不存原文明文
- [x] 命中时完整白话直接显示，Network 面板不出现新的 `/api/gloss`
- [x] 同一句在同一会话内白话固定，不重新生成
- [x] 命中 / 未命中状态留给 G-15；不新建客户端埋点通道
- [x] 用户编辑版本不写入自动缓存；写入只用 add，先到结果保留
- [x] 只缓存收到 `[DONE]` 的完整成功输出；存储失败静默按未命中降级
- [x] 预载写 ref，不触发段落重渲染；摘要就绪不覆盖已展示或会话生成结果

**遗留项**：命中与未命中的计时未验，移交 G-36；重渲染数由 React DevTools 高亮检查加代码审查验收。

**已知风险（2026-09-19，仅记录，不开卡）**：上传与阅读器文档仍依赖 localStorage。`lib/storage.ts` 的 `getStorage()` 读取 `globalThis.localStorage`；不可用时抛 E2，上传页显示“当前浏览器禁止本地存储，暂时无法打开阅读器。”，不会进入阅读器。按 5 MiB = 5 × 1024 × 1024 = 5,242,880 bytes、UTF-16 localStorage 约每字符 2 bytes，且暂不计 JSON key / 元数据：3037 字序言约 6,074 bytes，第 864 本触发 5 MiB；50,000 字书约 100,000 bytes，第 53 本触发 5 MiB。

**回滚**：`git revert`。缓存可直接清空，无数据迁移风险

### G-34 · 短句生成超时的线上诊断
**P1 · W2** ｜ 依赖 G-07、G-15 ｜ 分支待定

> **线上实测证据（Clara 亲验，2026-09-19，Vercel Runtime Logs，Production / main）**：04:58:12.58（GMT+8）的一次 POST /api/gloss 返回 504；Execution Duration 15.01s / Maximum 5m，Response finished in 15.1s。日志事件为 gloss_error，模型 deepseek-flash；External APIs 显示 POST api.deepseek.com/chat/completions，请求确实已发出。同一时段前后的 04:57:00、04:57:22、04:58:54 请求均为 200 成功。

> **结论**：这是我方 15 秒首字阈值触发的主动放弃，不是 Vercel 平台超时；上游偶发变慢，不是代码缺陷。生产函数已启用 Fluid Compute，实测 Maximum 为 5 分钟。

**已定方案（2026-09-19）**
- 选择候选 2：首字前超时后显式中止第一次上游请求，等 1 秒，再自动重试一次；每次点击最多 2 次上游调用。第二次仍失败才显示“这次没能生成，点「重试」通常就好”。
- 只覆盖首字前。首块已经发出后的流中断仍按既有 D2 路径保留文字并给重试按钮，不自动重试。
- 前端在发出请求后 10 秒仍未收到首字时，显示“这句有点慢，再等一下…”。服务端首字阈值为 15 秒；**前端 10 秒必须小于服务端 15 秒**，任一侧改动时都要核对另一侧，否则慢提示会失去意义。
- 浏览器始终只发一次 /api/gloss，服务端内部重试不额外占 WAF 的 30 次 / 10 分钟额度。
- gloss_retry 记录 retryOutcome（success / failure / aborted），供 G-15 统计重试救回比例；不记录原文或白话。

> 本轮不改流协议、Reader.tsx、150 字截断或既有 abort / D2 语义。

**候选做法与代价（记录；已选择第 2 项）**
1. 保持 15 秒阈值，只改文案，让读者知道这次是上游变慢、重试通常有效。代价：仍会在偶发慢请求时放弃一次生成。
2. 首字前超时自动重试一次，再失败才提示。首字前失败没有产出，重试实现风险较小。代价：会多发一次调用，增加等待时间与费用。
3. 调高阈值。代价：读者等待更久，且与 PRD 首字 P90 ≤2.5s 的目标背道而驰。
4. 保持现状，只在 G-15 埋点里统计发生频率，发生率足够低就不动。代价：在统计完成前，读者仍会偶发看到当前超时提示。

**验收**
- [x] 取得并核对该请求的生产 gloss_error 日志，确认首字前 15 秒主动超时而非平台超时（Clara 亲验（线上），2026-09-19）
- [x] 首字前超时后仅重试一次，间隔 1 秒；两次失败才返回 timeout（Codex 自测：假计时器）
- [x] 第一次上游 fetch 的 AbortSignal 在第二次请求建立前已中止；读者在 1 秒等待期间中止时不发第二次请求（Codex 自测：假计时器）
- [x] 首块已经发出后的流中超时不自动重试，仍走既有 streaming / D2 路径（Codex 自测：假计时器）
- [x] gloss_retry 记录最终 retryOutcome，成功、失败和中止均可区分（Codex 自测：假计时器）
- [x] 前端 10 秒慢提示位于既有 3 行预留内；首字到达时立即消失；提示出现、消失时撑开区上方可见内容位移均为 0（Clara 亲验（本地复现），2026-09-19）
- [ ] 最终失败时只显示失败提示，且替换时可见内容位移为 0（还差浏览器模拟两次首字超时的亲验）
- [x] development 下模拟地址只接受 localhost / 127.0.0.1；外部、带凭据及无效地址均回退官方地址（Codex 自测：lib/deepseek.test.ts）

**会改**：app/api/gloss/route.ts、components/reader/GlossPanel.tsx、lib/deepseek.test.ts；后经批准追加：lib/deepseek.ts（仅开发环境、仅本机的模拟上游地址限制）、package.json、scripts/dev-slow-gloss.mjs、AGENTS.md
**不改**：components/reader/Reader.tsx、lib/gloss-client.ts、流协议、150 字截断、既有 abort / D2 语义
**完成**：2026-09-19 ｜ 实现 commits：feb2532、60c8f44
**回滚**：git revert


---

### G-08 · 三类输出与术语标记
**P0** ｜ 依赖 G-07 ｜ 分支 `feat/g08-output-types`

**可验证增量**：马克思那句的白话里，「市民社会」带灰蓝标记且不折行。

> **灰蓝标记的含义（2026-09-18，Clara 定）**：**这个词没给你改写**——白话里原样保留了原句里的词，没有换成大白话。
> 不是「这是本书的承重概念」。承重与否是主观判断，模型三轮都判不准（召回率 < 8%）；
> 「是不是照抄原句的词」是可核对的事实。「承重与否」仍然决定**哪些词不改写**（见下方最后一条），但不再决定标不标。

**标记密度定案与止损线（2026-09-18）**：Clara 亲验（8 句标记对照页，数量据本轮确认），判断“没问题”，当前密度可接受。
v5→v9 经四次版本递进，约束越细、输出越保守；v7 标 9 个、v9 标 2 个（沿用 2026-09-18 历史实测，按出现次数计，本轮未重跑）。
原因不明，记为已知限制，不作因果结论；**不再用多轮调 prompt 提高标记数量**。
第二批评测已完成；验收 c 由 Clara 亲验通过。密度低但选词准确，按止损线不再调 prompt。

**第二批评测事实（2026-09-19，实测）**：12 条中 3 条出现标记，共 4 个词：权力意志、市民社会、类存在物、自为存在。选词没有标错，数量偏少；Clara 已据此完成亲验，结论见验收 c。

**验收**
- [x] prompt 能区分句法简化 / 指代还原 / 术语标注三类（Claude Code 自测）：三类是对同一句白话的三种要求，不是三选一；
      只有术语有视觉标记，句法简化与指代还原体现在白话怎么写，界面上不加任何标签 / 图标 / 分区（2026-09-18 决定）
- [x] 术语保留原词 + 灰蓝标记，**不输出「为什么不翻」的解释文字**（Claude Code 自测）：
      模型用 ⟦⟧ 圈承重概念，前端解析成 `.gloss-term`（只染 `--term` 色，不加底色、不加下划线）
- [x] 定界符 ⟦⟧ 选型有据（Claude Code 自测）：在评测集、5 份 PDF 样本与序言 docx 共 23 份文本、60895 字符里出现 0 次。
      `「」` 出现过、`【】` 已被用户消息的分节标题占用，都排除
- [x] 配对失败降级为纯文本，定界符不出现在读者眼前（Claude Code 自测）：只有左半边 / 只有右半边 / 跨数据块被截断三种
      都有单元测试；浏览器实测三种情况面板里均无定界符
- [x] 定界符不计入 150 字上限（Claude Code 自测）：`CharLimiter` 跳过这两个字符，单元测试锁住
- [x] 术语 `white-space: nowrap`，行末整词移行（G11）（Claude Code 自测）：1440×900 下用 14 个偏移逐个试，
      术语永远只占 1 行，且每次都从行首开始（放不下就整词下移）
- [x] 术语色在三套主题下均达 WCAG AA（Claude Code 自测）：术语 vs 中栏底色 —— 羊皮纸 8.29:1、月白 8.23:1、护眼绿 7.52:1，
      均 ≥ 4.5:1（正文标准，不用大字号的 3:1）。`lib/theme-contrast.test.ts` 直接读 tokens.css，颜色一改就失败
- ~~20 句评测集上术语识别准确率 ≥85%~~ **作废（2026-09-18），连同 53 个的分母。**
      理由：评测集的「术语」字段来源无法确认——会话记录里没有任何一次写入这个文件，
      文件由 Clara 在 2026-09-16 04:51（本地时间）放进来，她本人不记得是手标还是用工具生成。
      拿来源不明的数据当标准答案，等于机器给机器打分。字段已改名为「术语_来源不明_非标准答案」（值未动）。
- [x] **a. 显示出来的每个标记，必须逐字出现在原句里**：前端本地校验，对不上的按纯文本显示。机械可测，要有测试
      （Claude Code 自测）：`lib/output.ts` 的 `limitTerms()`，比对时忽略空白；流式中途未配对的前半截也校验；
      降级后与两边文字连成一段。2026-09-18 经批准改 `Reader.tsx` 一处 props，把被点击的原句传给 `GlossPanel`，线上生效。
      浏览器实测（假的流，未调模型）：原句里有的「资本」「土地所有制」标出；原句里没有的「剩余价值」、
      第二次出现的「资本」、11 字的长短语都按普通文字显示；无定界符
- [x] **b. 一句白话里最多标 3 个不同的词**：超过时保留前 3 个，其余按纯文本显示。标记的价值在稀少，满屏灰蓝等于没标
      （Claude Code 自测）：`MAX_TERMS_PER_GLOSS = 3`，`GlossPanel` 渲染时生效；没通过前面几道检查的不占名额；
      相邻的两个术语（⟦实体⟧⟦样式⟧）分开计数。单元测试锁住（2026-09-18 起按「不同的词」计，重复出现的不占名额）
- [x] **d. 同一段白话里，同一个词只标第一次出现**（2026-09-18 Clara 定）（Claude Code 自测）：第二次及以后按普通文字显示，
      比对时忽略空白。单元测试锁住。已知代价：流式中途，重复词还没写完的一两个字会先按术语显示、写完后才判成重复，
      文字每 45ms 放出 4 个字，至多闪一次
- [x] **e. 标记长度 ≤ 6 字**（2026-09-18 Clara 定）（Claude Code 自测）：`MAX_TERM_CHARS = 6`，超过的按普通文字显示且不占名额；
      「以另一个样式为条件的样式」这类整段短语被拦下；恰好 6 字（斯宾诺莎主义）与 1 字（力）照标。单元测试锁住。
      **「必须是名词性的概念词」本地判断不了**，只能靠提示词（gloss-v9 已写入）
- [x] **f. 标记的视觉只有颜色**（2026-09-18 Clara 定）（Claude Code 自测）：只标在白话里，原文一侧不动；不要底色、下划线、边框、图标，
      保留 `white-space: nowrap`。`lib/theme-contrast.test.ts` 读 reader.css，断言 `.gloss-term` 只有 `color` 与
      `white-space` 两条声明、全文件只有一处给它定样式；多一条就失败
- [x] **提示词示例不出自评测集与 Clara 在读的书**（2026-09-18）（Claude Code 自测）：gloss-v9 定稿。
      正例换成 市民社会、生产关系、交换价值、物化、拜物教；「日常词也是概念」的例子 劳动、意识 → 劳动、商品。
      7 个词在评测集（原句 + 前后文）与 pdf-real.pdf 正文里实测均为 0 次。改成「类别描述 + 正反例」：
      类别＝哲学、理论体系里的抽象概念名词和专名；反例＝泛词（东西、关系、问题、方面、情况）、短语、已改写的词。
      核对：评测集「术语」字段的 21 个词里，只有「概念」出现在生效提示词中——是描述类别用的措辞（「抽象概念名词」），
      不是示例，没法去掉。附带发现：「意识」自 v5 起就在提示词里，评测集 5 次、pdf-real 35 次，之前一直在泄题
- [x] **c. 「标得够不够、有没有标错」由 Clara 在第二批难句上判断**（Clara 亲验，2026-09-19）：12 条中 3 条有标记，共 4 个词——权力意志、市民社会、类存在物、自为存在；全部标对，没有该标而未标的词。密度低但选词准确；按止损线不再调 prompt。
- [x] **术语色已调**（Clara 亲验（线上），2026-09-19；候选页由 Clara 于 2026-09-18 挑定）：原色三套主题下「完全看不出来」（术语 vs 白话色 1.23–1.31:1，ΔE 8.6–13.2）。
      候选页 `Claude outputs/g08-术语色候选.html`。挑定并写入 `styles/tokens.css`：

      | 主题 | 选定 | 新值 | vs 底色 | vs 白话色 | ΔE |
      |---|---|---|---|---|---|
      | 羊皮纸 | A 深·低饱和 | `#233448` | 11.73 | 1.78 | 20.0 |
      | 月白 | A 深·低饱和 | `#1F3341` | 11.99 | 1.91 | 16.4（月白的白话色偏冷灰，灰蓝难拉开） |
      | 护眼绿 | B 中·中饱和 | `#326095` | 5.70 | 1.08 | 26.8 |

      对比度测试（`lib/theme-contrast.test.ts`）按新值通过。**Clara 亲验（线上，2026-09-19）**：gloss-jet.vercel.app 的 `--term` 已为选定值，术语标记显示为灰蓝。
- [x] **原句悬停色拆成独立变量 `--hover`，Clara 选定 C 档**（Clara 亲验（线上），2026-09-19；C 档于 2026-09-18 选定）：`--term` 原本被 4 处共用，
      只拆悬停一处——Clara 判断原来的悬停「太不显眼」，要稍微显眼一点点，但只是提示这句能点，不能抢注意力。
      候选页 `Claude outputs/g08-悬停色候选.html`，三套主题都选 **C 档**（最显眼的一档）：

      | 主题 | `--hover` | vs 底色 | 与正文墨色 ΔE | 与术语色 ΔE |
      |---|---|---|---|---|
      | 羊皮纸 | `#1F4E75` | 8.08 | 21.6 | 10.4 |
      | 月白 | `#1F4E75` | 8.02 | 21.6 | 11.8 |
      | 护眼绿 | `#14446B` | 8.94 | 23.3 | 10.1 |

      `lib/theme-contrast.test.ts`：悬停色 vs 底色 ≥ 4.5；与术语色 ΔE ≥ 10（悬停的原句不能像被标了术语）；
      与正文墨色的色差大于换色前的悬停色。
      **回退预案**：上线后 Clara 实际连读一段时，如果觉得扫过去太跳，降到 **B 档**，只改一个色值——
      羊皮纸 `#2E5270` / 月白 `#2C506D` / 护眼绿 `#204665`（三者都满足上面三条测试）。
      **另外两处不拆，跟着 `--term` 变深**：白话面板的「重试」链接、上传页**阻断型**提示框的左边线（横幅型提示框没有这条线）。
      **Clara 亲验（线上，2026-09-19）**：原句悬停色生效。
- [x] **原文含混、有多种读法的地方，保持含混，不要替读者选一个**（Clara 亲验，2026-09-19）：诗与文学 3 条的白话是字面直译，没有替读者选定一个读法。
      来自 G-06 评测第 4 句：原文里「欲望（Appetitum）」与「内在的努力」是并列还是蕴含，
      原文本身就含混，三个版本各给了一个答案——模型在替读者消解原文的含混。G-06 只记不改，本 issue 处理
- [x] **术语按承重与否分别处理**（Clara 亲验，2026-09-19；从 G-06 移交，PRD 3.7）：本批实际输出中，承重概念保留原词并被标出，其余词白话化。体系性概念与哲学史专名保留原词，
      文言词与普通学术词白话化。G-06 的 v2、v3 实现过这条但随版本废弃（原文在 `lib/prompts/gloss.ts` 存档里），
      Codex 只读核对（2026-09-18）：**保持含混**与**按承重与否决定保留还是白话化**均已在 gloss-v9 生效提示词中。
      前者要求不替读者选定原文的多种读法；后者要求理论概念保留、文言词与普通学术词白话化。
      这与标记“这个词没给你改写”是两件事。写入提示词不等于验收通过，两项保持未勾选，等第二批 10 句到齐后判定；本轮未改 prompt。

**完成**：2026-09-19 ｜ 实现 commits：080404e、45b626f、690c436、54adbb6、8f43f58、d35e8ab ｜ 评测与记录：1cd1cd7

**两批 v9 评测费用**：20 条首批约 ¥0.0125（日志 token × 当时价格计算）；12 条第二批已知 usage 费用 ¥0.00454028（日志 token × 空闲时段价格计算），累计可核算 ¥0.01704028。第二批第 6 条因 150 字截断中止，usage 为 null；中止前已处理 token 无法从日志还原，真实账户扣款不确定且可能更高。

**会改**：`lib/prompts/gloss.ts`、`components/reader/TermMark.tsx`（新建）、`styles/reader.css`、
`components/reader/GlossPanel.tsx`、`lib/output.ts`、`lib/output.test.ts`（复述时经批准补入）；
开工后追加：`lib/theme-contrast.test.ts`（新建，对比度自动校验没有现成的落点）；
2026-09-18 经批准追加：`components/reader/Reader.tsx`（只加一处 props：把原句传给白话面板）、`styles/tokens.css`（按 Clara 挑定的值换 `--term`）
**不改**：`components/reader/Sentence.tsx`、`lib/segment.ts`、`lib/parse/**`、`app/api/**`、`lib/deepseek.ts`、`scripts/eval-gloss.mjs`
**回滚**：`git revert`

**术语识别实测**（2026-09-18，20 句评测集，真 key，三轮共 60 次调用、合计约 ¥0.035）

| 版本 | 改了什么 | 模型标出 | 命中 | 召回率 | 精确率 |
|---|---|---|---|---|---|
| gloss-v5 | 术语一条分成承重 / 不承重，承重的圈起来 | 2 | 2 | 3.9% | 100% |
| gloss-v6 | 把「圈起来」移到【格式】写成硬要求 | 6 | 2 | 3.9% | 33.3% |
| gloss-v7（生效） | 不再判断「承重」，改成机械规则：原样搬进白话的理论概念一律圈 | 7 | 4 | 7.8% | 57.1% |

分母 51–53 取自评测集的「术语」字段（20 句里 17 句有值）——**该字段来源不明，这组数字已作废，只保留作记录**。三轮都没有出现定界符未配对。

**关键发现**：v5 那一轮里，53 个标注术语有 **45 个出现在白话里但没被圈**，只有 4 个被圈——
模型保留了原词，却不认为它们够格。它自己圈的是「欲望」「终极因」「能动的自然」这类。
所以卡点不是指令写在哪，而是**模型对「什么算术语」的门槛比标注严得多**。
v7 把判断改成机械规则后翻了一倍，仍然只有 7.8%。**这可能是口径分歧而不是模型能力问题**。
（后续：查实该字段来源不明，验收口径已作废重写，见上方 a / b / c。）

**gloss-v9 实测**（2026-09-18 19:20，20 句评测集，真 key，20 次调用，约 ¥0.0125；只记数字，是否达标由 Clara 看真实输出判断）

| 版本 | 模型标出（按出现次数） | 本地检查后显示 | 平均每句显示 | 每句显示 0 / 1 / 2 / 3 个的句数 | 本地检查拦掉 |
|---|---|---|---|---|---|
| gloss-v7 | 9 | 6 | 0.30 | 15 / 4 / 1 / 0 | 重复 2、超过 6 字 1 |
| gloss-v9 | 2 | 2 | 0.10 | 18 / 2 / 0 / 0 | 0（不逐字 0、超长 0、重复 0） |

v9 显示的两个都是「样式」（id 158、166）。v7 之前记的「标出 7 个」是按每句去重数的，这里统一按每次出现数，是 9 个。
v9 与 v7 的白话平均长度相同（都是 77 字），原始输出里只有 2 对定界符，没有未配对的。
对照页：`Claude outputs/g08-v9-标记对照-8句.html`（本轮没有一句标满 3 个，所以页上没有这一类）。

**离线对齐实验**（2026-09-18，用 v7 那一轮的白话重算，未发请求）：把白话和原句做连续公共子串对齐（≥ 2 字，去掉功能词和泛词），
本地对齐平均每句标出 **10.6 个**（限 2 字时 17.1 个），模型三轮只标出 2 / 6 / 7 个——两个极端。
本地对齐的失败模式：① 跨词边界的碎片（「把终极因」「的体系」「只是因为」，中文没有空格）；
② 普通词也亮（「行动」「开端」「最大」）；③ 停用词表永远不全。
方向（待 Clara 看完对照页再定）：**模型提名 + 本地校验 + 每句上限 3 处**。

**性能复测**（与 G-07 同脚本、同规格样本：450 段 / 1830 句，本地 production 构建，每轮 20 次点击）

| 轮次 | 中位数 | p90 | 最长 |
|---|---|---|---|
| main 对照 第 1 轮 | 7.5ms | 9.1ms | 12.0ms |
| main 对照 第 2 轮 | 7.3ms | 9.2ms | 12.7ms |
| 本分支 第 1 轮 | 7.4ms | 8.9ms | 10.1ms |
| 本分支 第 2 轮 | 7.1ms | 8.9ms | 9.4ms |

没有回归（每次渲染多跑一次 `splitTerms`，开销在噪声以内）。

**接线后复测**（2026-09-18，`Reader.tsx` 把原句传给白话面板之后；同脚本同样本，每轮 20 次点击）

| 轮次 | 中位数 | p90 | 最长 |
|---|---|---|---|
| main 对照 第 1 / 2 轮 | 7.2 / 7.2ms | 8.5 / 9.1ms | 10.8 / 9.6ms |
| 本分支 第 1 轮（冷）/ 第 2 轮 | 7.7 / 7.3ms | 11.5 / 9.1ms | 14.6 / 9.5ms |

没有回归。重渲染：3 个位置各点一次，451 个段落元素里每次只有被点的 1 段换新（原句按字符串传入，按值比较，不破坏 memo）。
重渲染：抽 3 个位置点击，451 个段落元素里每次只有被点的 1 段属性对象换新。


---


### G-25 · 阅读界面左栏：解析统计常驻 + 无标题时的呈现
**P1** ｜ 依赖 G-04 ｜ 分支 `feat/g25-side-info`

> 来源：Clara 在 Preview 上的实际使用反馈（2026-09-18）。

**验收**
- [x] 「N 字 · N 段 · N 页」常驻阅读界面左栏（Claude Code 自测）：左栏新增「这份文档」块，
      第一行文件名、第二行「字数 · 段数 ·（PDF 才有的）页数 · 句数」、第三行跳过信息。
      数字全部来自已存的记录（`meta` + 重算的句数），**刷新后仍在**；解析完照旧自动进入阅读器，没有加确认步骤
- [x] 把「跳过了什么」带进阅读页（Claude Code 自测）：跳过信息作为**可选字段** `meta.skipped` 随文档存进 localStorage，
      **不升 SCHEMA_VERSION**；docx 记表格处数 / 字数 / 有无公式，PDF 记扫描页页码。
      实测：`docx-table-formula.docx`「已跳过：表格 1 处（102 字）、公式」；`pdf-mixed.pdf`「已跳过：第 2 页（扫描页）」
- [x] 老记录照常打开（硬要求）（Claude Code 自测）：手工写入一条没有 `meta.skipped` 的旧记录，正常打开、正文正常、
      不显示跳过行、不报「找不到这份文档」；`storage.test.ts` 有对应用例
- [x] 无标题的文档：目录块留在原位，文案改为「这份文档没有标题层级」（Claude Code 自测）：
      Clara 定的口径——目录时有时无会让读者困惑，块保持在原位，状态自己说明自己。
      带标题的 docx 仍正常显示目录（序言样本显示「序言」）
- [x] 不做任何随滚动变化的内容（Claude Code 自测）：左栏全部静态，不新增 state / effect
- [x] 性能不回归（Claude Code 自测）：见下方复测数字
- [x] Clara 亲验（Preview 部署）：6 条全部通过——常驻并在刷新后仍在、换书数字正确（docx 无「页」）、
      跳过信息正确（表格 / 公式 / 扫描页）、干净文档不出「已跳过」、老文档照常打开、目录块留在原位且点句不位移

**会改**：`components/reader/Reader.tsx`、`styles/reader.css`、`lib/parse/validate.ts`（`SkippedContent` 类型、
`assembleDocument` 增加可选参数、`skippedSummary()`）、`lib/parse/docx.ts`、`lib/parse/pdf.ts`（填 `meta.skipped`）、
`lib/storage.test.ts`、`KANBAN.md`；开工后追加：`lib/parse/validate.test.ts`（`skippedSummary` 的文案用例）。
`lib/storage.ts` 与 `components/Upload.tsx` 经核对**无需改动**（`meta` 原样存取，上传页提示沿用 G-24 的路径）——
与复述清单的两处出入（这两个文件未改、`validate.test.ts` 为追加）已由 Clara 确认接受
**不改**：`components/reader/{GlossPanel,Sentence}.tsx`、`app/api/**`、`lib/{segment,gloss-client,deepseek}.ts`、`lib/prompts/**`、`package.json`
**回滚**：`git revert`
**完成**：2026-09-18 ｜ commit `3e010f4` ｜ Clara 在 Preview 部署上亲验通过

**性能复测**（与 G-07 同一脚本、同一规格样本：450 段 / 1830 句，本地 production 构建，40 次点击）

| 轮次 | 中位数 | p90 | 最长 |
|---|---|---|---|
| G-07 基线（2026-09-17 记录） | 6.5ms | 10.6ms | 17.2ms |
| main 对照 第 1 轮（冷） | 7.8ms | 11.6ms | 13.5ms |
| main 对照 第 2 轮 | 6.9ms | 8.3ms | 9.1ms |
| 本分支 第 1 轮（冷） | 8.5ms | 13.7ms | 17.4ms |
| 本分支 第 2 轮 | 6.7ms | 8.1ms | 10.4ms |
| 本分支 第 3 轮 | 6.6ms | 7.8ms | 8.0ms |

每轮第一次测量都偏高（热身），稳定后本分支 6.6–6.7ms、main 6.9ms，**没有回归**。
重渲染：抽 5 个位置点击，451 个段落元素里每次只有被点的 1 段属性对象换新。
位移：被点击行 0px，段内其余字 0px。

---

### G-24 · docx 剔除表格时给出提示（A10）
**P1** ｜ 依赖 G-02 ｜ 分支 `fix/g24-table-notice`

> 来源：G-02B 查字数差异时发现。docx 解析按 A10 剔除表格，但**没有任何用户可见提示**，
> 违反 PRD 第 761 行「A1–A10 … 每条均有用户可见提示，**无任何静默失败**」。
> G-02 验收时漏掉了：G-02 的验收项只写了「A1–A3、A7、A8 各有提示」，没有覆盖 A10。
> 现象：《政治经济学批判》序言 docx 文末的署名、落款、出处说明共 62 字是用表格排版的，
> 上传后这 62 字直接消失，用户不知道。位置：`lib/parse/docx.ts` 里 `body.querySelectorAll("table")` 那一步。

**样本**：`test-fixtures/docx-table-formula.docx`（正文 + 1 处表格 3 行 6 单元格 102 字 + 1 个 oMathPara 公式）。

**验收**
- [x] docx 含表格时出横幅提示（不阻断），说明表格内容已跳过（Claude Code 自测）：序言「1 处表格，其中的 62 字已跳过」；
      新样本「1 处表格（102 字）和公式，都已跳过」；两者都停在上传页 + 「开始阅读」，与 A6 / A9 一致
- [x] 含公式时同样提示，**不报个数**（Claude Code 自测）：mammoth 不转换 OMML，直接丢弃，但 messages 里留下
      `An unrecognised element was ignored: {…/2006/math}oMath(Para)`。行内 `m:oMath` 与独立成段的
      `m:oMathPara` 都带这个命名空间；消息按元素类型去重，两个公式也只有一条，所以只能知道有、数不出几个
- [x] 表格计数口径（Claude Code 自测）：只数最外层 `<table>`（嵌套表格的字数算进外层，不另计）；
      只有一个单元格的排版表格照样计入（序言那 62 字就是这么丢的）；不区分「排版用」与「放数据」——XML 上无可靠差异
- [x] 不含表格和公式的文档不出提示（Claude Code 自测）：txt 直接进阅读器，无横幅；构造数据用例覆盖
- [x] 反证：同一本书的 PDF 路径不出此提示，且表格文字读得进正文（Claude Code 自测）：
      `pdf-text.pdf` 无横幅，阅读器正文里搜得到「原文是德文」；序言 docx 搜不到
- [x] Clara 亲验（Preview 部署）：横幅里的字数与 Word 的**「字符数（不计空格）」**一致
      （不是「字数」——「字数」把 1859 这类数字整体算 1 个，对不上）：
      新样本「1 处表格（102 字）和公式」＝ 102；序言「1 处表格，其中的 62 字」＝ 62；
      两份文档进阅读器后分别搜「意识流型」「原文是德文」都搜不到；
      反证：`pdf-text.pdf` 不出此横幅且搜得到「原文是德文」，无表格无公式的文档不出横幅、直接进阅读器

**会改**：`lib/parse/docx.ts`、`lib/parse/validate.ts`、`components/Upload.tsx`、`lib/parse/docx.test.ts`（新建）、`KANBAN.md`；
开工后追加：`lib/parse/pdf.test.ts`、`lib/segment.test.ts` —— `parseDocx` 改为返回 `{ doc, warnings, detail }`，
这两个测试各有一行取 `doc` 的写法要跟着改
**不改**：`lib/parse/{txt,pdf}.ts`、`components/reader/**`、`components/Notice.tsx`、`app/api/**`、`styles/**`、`package.json`
**回滚**：`git revert`
**完成**：2026-09-18 ｜ commit `f32c89c` ｜ Clara 在 Preview 部署上亲验通过

---

### G-02B · PDF 文字层解析
**P0** ｜ 依赖 G-02 ｜ 分支 `feat/g02b-pdf`

> 补漏：PRD F1 的支持格式含「文字层 PDF」（P0 / W1），原看板漏排此 issue。
> **执行顺序：排在 G-07 之后。** PDF 导入不参与「上传→切句→点击→出白话」这条
> 端到端链路，不该占用 W1 关键路径。W1 的死线是「有一个能发给别人的链接」。

**可验证增量**：上传一份文字层 PDF，直接进入阅读器并显示正文（有提示时停留在上传页，页面显示「已解析「文件名」：N 字 · N 段 · N 页」）；上传一份扫描件 PDF，页面出现含「扫描件」三字的提示，且**不显示任何乱码**。控制台不再输出正文。

**验收**
- [x] pdf.js 在客户端解析文字层，**worker 本地托管，不走 CDN**（Claude Code 自测）：本地 dev 与 production 构建下 worker 为本站打包文件，cMap 只从本站 `/pdfjs/6.3.289/cmaps/` 加载，无第三方请求，`quickjs-eval.wasm` 等 wasm 从未被请求
- [x] A4 加密 PDF：提示需先解除密码（Claude Code 自测）
- [x] A5 扫描件 PDF：提示**明确含「扫描件」三字** + 原因 + v2 计划；**不可显示乱码**（Claude Code 自测）
- [x] 扫描件判定有明确阈值（文字层为空，或字符数/页数低于阈值），阈值写进注释（Claude Code 自测）：字数 < 20 且最大图片面积 ≥ 30%；无扫描页但平均每页 < 20 字 → A5
- [x] 解析全部在客户端完成，**原文不出浏览器**（Claude Code 自测）：网络请求只有本站静态资源和 cMap；控制台不输出正文
- [x] 与 docx/txt 走同一套异常提示组件，不新造一套（Claude Code 自测）
- [x] 与 docx 对照（序言样本，`pdf.test.ts` 自动检查）（Claude Code 自测）：
      ① PDF 提取文字去空白后，与 docx **全部可见文字（含表格）**去空白后逐字一致；
      ② PDF 字数 = docx 解析字数 + docx 表格字数（3099 = 3037 + 62）；
      ③ PDF 前 15 段与 docx 的 15 段逐段一致
- [x] 页眉页脚：`pdf-header.pdf`（奇偶页不同页眉 + 页码）去除后与 `pdf-text.pdf` 去空白后逐字一致（Claude Code 自测）：逐字、逐段均一致
- [x] 解析后归一化：合并汉字与汉字之间的半角空白（空格、制表符）；全角空格 U+3000 不合并（2026-09-17 决定），
      不影响中英文之间和数字之间的空格。需要先在样本上统计误伤率（Claude Code 自测）：序言 docx 0 处，`pdf-text.pdf` 10 处（标题字间空格），评测文本 169 处，抽查未见误伤
      （G-06 评测时发现：docx 提取留下「性 状」「倾 向」这类词中空格，模型收到的是断开的词，任何一本书都会受影响）
- [x] `Claude outputs/` 加进 `.gitignore`。它目前既没被跟踪、也没被忽略，一旦有人用 `git add -A` 就会被提交进去
      （G-07 收尾时发现，记在这里，不在 G-07 做）（Claude Code 自测）：`git check-ignore -v` 命中 `.gitignore:17`
- [x] 线上构建：Vercel 构建日志里有 `> gloss@0.1.0 prebuild`，复制了 169 个 cMap 文件
      （Clara 亲验（Preview 部署））
- [x] 线上 cMap：Network 里 `GBK-EUC-H.bcmap`（12.5 kB）与 `Adobe-GB1-UCS2.bcmap`（30.0 kB）均为 200 且来自本站；
      无 cdn / jsdelivr / unpkg（Clara 亲验（Preview 部署））
- [x] 线上文字层 PDF：`pdf-real.pdf` 页面闪现「8091 字 · 69 段 · 14 页」后进入阅读器，数字与自测一致
      （Clara 亲验（Preview 部署））
- [x] 线上控制台：没有原文（Clara 亲验（Preview 部署））
- [x] 线上三种提示：扫描件、加密、混合型均正确，混合型显示「234 字 · 1 段 · 2 页」
      （Clara 亲验（Preview 部署））
- [x] 线上 docx 回归：3037 字 · 15 段（Clara 亲验（Preview 部署））
- [x] 生产环境同样通过（Clara 亲验（线上 gloss-jet.vercel.app））：上传 `pdf-real.pdf` 进入阅读器；
      Network 的 Domain 一列全是 `gloss-jet.vercel.app`，无外部域名，也没有 Preview 才有的 vercel feedback / validate；
      两个 `.bcmap` 均为 200；上传 `pdf-text.pdf` 点句子，白话正常生成（真实调用）

> Preview 上另见两条与本卡无关的请求，已确认不是本项目代码：
> ① `vercel.com` 的 feedback / validate 是 Vercel 给 Preview 部署注入的评论工具条
>   （仓库里没有任何相关代码；未登录取到的 Preview HTML 里也没有这些脚本）；
> ② `favicon.ico` 404 —— 项目还没有网站图标，Preview 与生产都是 404，与本卡无关，见 G-26。

**会改**：`lib/parse/pdf.ts`、`lib/parse/validate.ts`、`components/Upload.tsx`、`components/Notice.tsx`（实际未改）、`.gitignore`；
经批准追加：`lib/storage.ts`（日志只记错误类型）、`lib/parse/pdf.test.ts`、`lib/parse/validate.test.ts`、
`scripts/copy-pdfjs-assets.mjs`、`package.json` / `package-lock.json`（`pdfjs-dist` 锁定 6.3.289，`predev` / `prebuild`）、`KANBAN.md`
**不改**：`lib/parse/{docx,txt}.ts`、`components/reader/**`、`app/api/**`、`styles/**`
**回滚**：`git revert`
**完成**：2026-09-18 ｜ commit `6d18967`（实现）、`5ffed98`（Preview 验收记录）｜ gloss-jet.vercel.app 已上线，Clara 线上亲验通过

**样本逐页数据**（2026-09-17，配本地 cMap；阈值依据，`lib/parse/pdf.ts` 注释引用这里）

| 样本 | 页 | 每页字数 | 最大图片面积 | 判定 |
|---|---|---|---|---|
| `pdf-text.pdf` | 6 | 433 / 684 / 682 / 683 / 571 / 46 | 0% | 文字页 |
| `pdf-encrypted.pdf`（打开密码 123456） | 6 | 与 `pdf-text.pdf` 逐页相同 | 0% | 不带密码 → A4 |
| `pdf-scan.pdf` | 1 | 0 | 62.7% | 扫描页 → A5 |
| `pdf-mixed.pdf` | 2 | 234 / 0 | 0% / 44.5% | 第 2 页扫描页 → A6 |
| `pdf-real.pdf`（扫描图 + GBK 文字层） | 14 | 420–672 | 每页 96.9% | 文字页；**不配 cMap 时每页 0 字** |
| `pdf-header.pdf`（奇数页「政治经济学批判」、偶数页「序言」、底部页码） | 6 | 441 / 687 / 690 / 686 / 579 / 49 | 0% | 6 个页眉、6 个页码全部去掉，结果与 `pdf-text.pdf` 逐字、逐段一致 |

乱码比例全部样本为 0%。旧版 `pdf-encrypted.pdf`（未加密，3011 字）已被覆盖。

**已知限制**（知道做不到，本卡不处理）
- 页底的脚注混进正文，会打断跨页段落。
- PDF 分不出表格和文本框：docx 路径按 A10 剔除的表格文字，PDF 路径会读进来
  （序言样本里是文末署名和出处说明共 62 字，docx 用表格排版）。
- 英文长单词换行会让行末提前，被误判为段末。
- 页眉页脚只看每页最上、最下各一行；页眉下紧跟的第二行即使也重复，也不去掉。

---

### G-02 · 文档解析（docx / txt / 粘贴）
**P0** ｜ 依赖 G-01 ｜ 分支 `feat/g02-parse`

**可验证增量**：上传《政治经济学批判》序言 docx，控制台打印出纯文本，字数与原文一致。

**验收**
- [x] docx / txt / 粘贴三种入口可用，**解析全部在客户端完成**
- [x] 表格与公式块被剔除；脚注单独归类不参与切句
- [x] 异常 A1–A3、A7、A8 各有用户可见提示，**无静默失败**（PDF 此阶段落入 A1）
- [x] A9 非中文文本：允许上传 + 横幅提示，**不阻断**

**会改**：`lib/parse/{docx,txt,validate}.ts`、`components/{Upload,Notice}.tsx`、`components/{Upload,Notice}.module.css`、`app/page.tsx`（临时挂载点）、`package.json`、`package-lock.json`、`.gitignore`、`KANBAN.md`
**不改**：`components/reader/**`、`app/api/**`、`styles/**`、`lib/parse/pdf.ts`、`lib/storage.ts`
**回滚**：`git revert`
**完成**：2026-09-15 ｜ commit `d36d547` ｜ 3037 字 · 15 段，Clara 本地实测

---

### G-04 · 阅读器渲染（三栏 + 排版）
**P0** ｜ 依赖 G-03 ｜ 分支 `feat/g04-reader`

**可验证增量**：整篇序言以定稿排版渲染在中栏，左右栏有内容骨架。截图与视觉定稿比对，字号行距一致。

**验收**
- [x] 正文 16.5px / 行高 2.05 / 字距 .025em
- [x] `text-align: justify`；`text-justify` 写两行——先 `inter-ideograph`（旧引擎）
      后 `inter-character`（现行规范值）。**`inter-ideograph` 是 IE 遗留值，不在现行规范里**
- [x] 中栏 ≤750px，约 38 字/行
- [x] 左栏：目录（来自 `headings`）+ 已保存列表的**空态**
- [x] 右栏：设置面板骨架（静态列出 PRD M1 六项，无控件）
- [x] **左栏最小宽度能容纳「复习态」等标签单行显示**
- [x] **阅读位置记忆存句序号**（PRD 3.8：doc_id → 句序号），不存像素位置。
      刷新后回到原句。字号/行距可调之后像素位置必然错位，句序号不会
      ⚠️ **已知且接受的行为**：顶端停在长句中间时，刷新会回到该句句首，
      即向上回退 1–3 行（最长句 157 字 ≈ 4 行）。这是句序号方案的必然结果，
      **方向正确**——向后漂会跳过没读的内容，向前回退只是重读半句。不修。
- [x] 段落按 `paragraphs[]` 渲染，共 15 段；**所有非标题段统一 `text-indent: 2em`**
- [x] 原文的段首全角空格在渲染时去掉，缩进由 CSS 承担
      （否则那两个空格会变成第一句可点击区域的一部分，G-05 会很难看）
- [x] 清理：删除 `styles/reader.css` 中已失效的 `.col-main-empty`（G-02 遗留死样式）

> ⚠️ **本 issue 必须新建 `lib/storage.ts`**：G-02 只解析不存储，而「阅读位置记忆」
> 和 `/read/[docId]` 路由都需要把文档取出来。范围限定为**文档与阅读位置**
> （paragraphs、headings、footnotes、meta、滚动位置），**不做白话存储**——那是 G-10。
>
> **两个预先定案**：
> ① `docId` = 文档内容 hash 的前 8 位（hex）。同一份文档重复上传不产生重复条目，
> G-13 书架上不会出现两本一样的书。
> ② **只存 `paragraphs`，不存 `text` 和 `sentences`**，后两者每次加载时重算。
> 切句是确定性的，重算结果必然一致，而三份重复数据会在 5 万字的书上逼近
> localStorage 上限（PRD 异常 E1）。

**会改**：`components/reader/{Reader,Sentence}.tsx`、`components/Upload.tsx`（上传后跳转）、`styles/reader.css`、`app/read/[docId]/page.tsx`、`app/page.tsx`、`lib/storage.ts`、`lib/storage.test.ts`、`KANBAN.md`
**不改**：`lib/segment.ts`、`lib/parse/**`、`app/api/**`、`components/settings/**`、`styles/tokens.css`
**回滚**：`git revert`
**完成**：2026-09-15 ｜ commit `0fb3ceb` ｜ 排版数值实测通过，1440/1024 两宽度各 38/37 字每行

---

### G-05 · 点击撑开与视口锚定 ⚠️
**P0** ｜ 依赖 G-04 ｜ 分支 `feat/g05-expand`

> **这是 W1 最关键的一个 issue，也是整个交互模型的地基。做不对，后面全部白搭。**

**可验证增量**：点击任意句子，下方撑开一块占位区（内容为固定占位文本，暂不接 AI）。**点击行的视口位置纹丝不动**，下方内容平滑下移。

**验收**
- [x] **点击后，被点击行在视口中的位置不变**（录屏逐帧核对）
- [x] 句子部分超出视口时，点击自动微调滚动，整句 + 撑开区完整可见
- [x] **原句的底色、边框、字重、位置在点击前后完全一致**（D13）
- [x] 撑开动画 ≥50fps，无跳动
- [x] 再次点击 / 点别处 / 该句滚出视口 → 收起
- [x] **自动收起同样要做视口锚定**：撑开区在视口上方被移除时，
      可见内容位移必须为 0。实测发现不补偿会「往上弹」
- [x] 点击另一句，前一句收起（同时只允许一个非锁定撑开态）
- [x] **用户已选中文本时，松开鼠标不触发撑开**（G5）
- [x] hover 时句子轻微变色（PRD 3.7）
- [x] **≥50fps 必须在 9 万字长文档上逐帧测量**，样本（61 句）测不出重排开销

> **DOM 结构定案（附实测证据）**：**行尾拆分** + 上半段 `text-align-last: justify`。
> 点击时量出被点句最后一字所在行，在该行行尾把段落拆成上半段 / 撑开区 / 下半段。
> 1440 宽、45 次段中拆分的逐字坐标比对：加 `text-align-last` 偏移 0px，
> 不加则 45/45 全部移位、最大 33.7px。
>
> **`text-wrap: pretty` 从正文段移除**，原因写进 `reader.css` 注释：
> 它按「段落最后几行」重新平衡断行，段落一拆，「最后几行」的定义就变了，
> 与行尾拆分原理冲突。D13 是硬约束，孤字是观感优化，冲突时前者优先。
>
> **hover 轻微变色要做**（PRD 3.7）。它是「这句能点」的唯一视觉提示。
> 逐像素比对时点完把鼠标移开再截图——移开鼠标不会收起撑开态。
>
> **切换句子时前一个直接收起、不播动画**，锚定以新点的那一行为准。
> 两个动画同时播会让锚定基准不唯一。

**会改**：`components/reader/{Sentence,GlossPanel,Reader}.tsx`、`styles/reader.css`、`KANBAN.md`
**不改**：`lib/**`、`app/api/**`、`components/settings/**`
**回滚**：`git revert`
**完成**：2026-09-15 ｜ commit `18ba1f2` ｜ 9 万字文档 100+fps，四条路径锚定位移 0，三种滚法实测无弹跳。**本 issue 独立成 commit，便于单独回退**


### G-06 · 功能一 API 与流式返回
**P0** ｜ 依赖 G-03 ｜ 分支 `feat/g06-gloss-api`

**可验证增量**：`curl` 调 `/api/gloss` 传入马克思那句原句，返回 73 字左右的白话，流式。
（「73 字左右」是举例，不是阈值；验收阈值只有 ≤150 字。v4 实测：125 字，首字节 0.84s）

**验收**
- [x] 上下文窗口 = 目标句 + 前后各 1–2 句 + 结构摘要，**不传整章**
      窗口实验（前 2 句 / 前 5 句 / 前约 1000 字）判定不稳，按事先定好的规则保持前 2 句
- [x] 流式返回，服务端按 **3–5 字块**切出（**传输层合并属正常**，不得加人为延迟去凑）
      「视觉上 3–5 字的节奏」由前端保证，已移交 G-07
- [x] 输出 **≤150 字**；超长由服务端截断并埋点 `gloss_overlength`
      ⚠️ **比例规则已废除**（PRD 2026-09-15 修订）。原「≤原句 80%」会毙掉 Clara
      自己一多半的批注（评测集实测：批注/原句中位数 111%、最高 220%）。
      难源于密度不是长度，短而密的句子只能展开。防啰嗦靠 prompt 措辞 + 人工检查
- [x] 异常 C1–C6 各有明确的错误类型返回（`lib/deepseek.test.ts` 用模拟 fetch 逐条跑过）
- [x] 返回内容清洗掉 markdown 与格式符号
- [x] **用 20 句评测集跑一遍，Clara 本人逐条读**。这是本 issue 真正的验收：
      代码决定它跑不跑得起来，prompt 决定产品好不好用，而只有读得懂原句的人
      才能判断白话说没说清
- [x] 语气：**口语但不用俚语**——像耐心的师兄在讲，不像评论区留言
- [ ] ~~**重述而非改写**：必须重组句子骨架，不是换词。禁止保留原句的
      破折号、分号、多层从句、双重否定；指代词必须指名道姓~~
      **撤销**：v2 按此执行，盲测证明是退步（本来就好的句子被过度改写），已废弃。
      指代问题改由 v4 的正向指令处理
- [ ] ~~**术语按承重与否分别处理**（PRD 3.7 已细化）：体系性概念保留原词，
      文言词与普通学术词白话化~~
      **移交 G-08**：v2、v3 实现过，随两版一起废弃；生效的 v4 只有「术语原词照用」，
      「文言词与普通学术词白话化」这一半尚未实现。PRD 3.7 的要求仍然有效，由 G-08 承接
- [ ] ~~**第二轮评测目标：A 类（难点没解开）≤ 4 / 20**。~~
      第一轮实测 B=0、C=0、A=10——约束与语气已达标，缺的是重述动作本身
      **终止**：噪声实验显示同配置两遍的逐句判定分歧率约 64%，人工判定分辨不了这个量级的差别，全量评测到此为止
- [x] **评测代号**（每轮人工阅读按此归类，一句可以同时属于多类）：
      **D 偏离原意**（没加内容，但改变了意思）——最严重，优先级高于 A、B、C
      **A 难点没解开**（读完白话，卡住的地方仍然卡住）
      **B 加了原文没有的信息**（评论、背景、引申）
      **C 语气不对**（像论文摘要、像讲解、像评论区）
      ⚠️ D 在单版评测里是隐形的：第一轮单版阅读时没发现，三版并排后 20 句里暴露出 7 句。
      **以后每轮评测都用多版盲测对照**（`node scripts/eval-gloss.mjs --compare …`），不得退回单版
- [x] 机械检查（破折号 / 分号 / 双重否定 / 指代词）是**定位工具，不是目标**。
      指代词一项永远会有命中，不得为了压低它而让白话变成机械重复名词。
      判定以人工阅读为准
- [x] `/api/structure` 每份文档只算一次并缓存，**不得每次点句都调**
      （结构上保证：`/api/gloss` 只接收调用方传入的摘要，从不调用 `/api/structure`；前端按 docId 缓存属于 G-07）
- [x] 模型标识符去 DeepSeek 官方文档核对，**不要凭记忆写**；
      功能一用 `deepseek-flash`（DeepSeek-V4.1-Flash），功能二（G-11）用 `deepseek-v4-pro`（DeepSeek-V4-Pro）。
      旧名 `deepseek-v4-flash` 已退役。标识符与高峰 / 空闲、缓存命中 / 未命中价格见 `lib/deepseek.ts` 注释（2026-09-16 核对）

> **提示词结论（2026-09-16）**：**v2、v3 经盲测均劣于 v1，已废弃**。随后的噪声实验（v1 同配置跑两遍 / v1 降温到 0.2 /
> v3 换 pro）四版盲测 20 句里 19 句无明确赢家，同配置两遍的逐句判定分歧率约 64%：
> 提示词之间的差异小于人工判定的分辨率，**全量评测到此为止**。温度由 0.7 改为 0.2。
>
> **窗口实验（2026-09-16）**：6 句 × 前 2 句 / 前 5 句 / 前约 1000 字，逐列判「说清了 / 没说清」，
> 结果 4/2、3/3、2/4。输入相同的三对判定全部不一致（这三对的差异里也包含模型两次生成本身的差异），判定不稳；
> 唯一只有宽窗口拿得到所指的第 11 句，三种窗口都判为说清了。按规则回到前 2 句。
> 实测成本供参考：前约 1000 字每次 ¥0.0022（高峰价，前文不命中缓存），前 2 句 ¥0.0009。
>
> **生效提示词 gloss-v4** = v1，只把「指代」一条改为正向指令：从前文里找出它指的是什么，把名字写进白话；
> 找不到就保留原词。**v4 未经人工评测**，按事先定好的收尾规则直接采用。v1、v2、v3 原文留在
> `lib/prompts/gloss.ts` 的存档里，只供开发环境对照实验。

**会改**：`app/api/gloss/route.ts`、`app/api/structure/route.ts`、`lib/prompts/{gloss,structure}.ts`、`lib/context.ts`、`lib/context.test.ts`、`lib/deepseek.ts`、`lib/deepseek.test.ts`、`lib/output.ts`、`lib/output.test.ts`、`scripts/eval-gloss.mjs`、`vitest.config.mts`（`@/` 别名，route 测试需要；用 .mts 是因为 package.json 未声明 ESM）、`KANBAN.md`、`PRD.md`（长度规则与术语规则修订）、`docs/`（产品决策记录）
**不改**：`components/**`、`styles/**`、`lib/segment.ts`、`lib/storage.ts`、`lib/parse/**`、`package.json`
**回滚**：`git revert`
**完成**：2026-09-16 ｜ 142 条测试通过 ｜ 生效：gloss-v4、温度 0.2、前后文各 2 句 ｜ 人工评测 6 轮（单版 2 + 盲测 4），API 花费合计约 ¥0.23

---

### G-07 · 端到端串联
**P0** ｜ 依赖 G-05 + G-06 ｜ 分支 `feat/g07-e2e`

**可验证增量**：**上传 docx → 读 → 点句 → 真实白话流式出现在撑开区。链接可以发给别人。**

> **打勾只表示已验证通过。** 标注分两种：**Clara 亲验（线上）** = Clara 在 gloss-jet.vercel.app 上亲手验过；
> **Claude Code 自测** = 本地开发环境或本地生产构建里用脚本测的。
> 未验、或只验了一部分的不打勾，后面写明还差什么；属于其他 issue 的验证工作，注明挪到哪里。

**验收**
- [x] 全链路跑通，无控制台报错
      —— **Claude Code 自测**（本地：上传 docx → 后台算结构摘要 → 点句 → 流式白话，控制台无报错）
- [ ] **Safari 上重跑 G-05 的逐字坐标比对**：行尾拆分方案只在 Chrome 152 验证过，
      Safari 的断行是否同样「从前往后、互不影响」未知。这是已知盲区，不是已知缺陷
      **挂起，不阻塞 G-07**：没有 Mac，现在验不了。见「🔍 已知盲区」
- [ ] 首字延迟 P90 ≤2.5s（G-06 已实测 P90 682ms）
      —— **Claude Code 自测**（本地实测首字约 0.7–0.8s）
      —— **还差**：线上的首字延迟 P90，**挪到 G-15**
- [x] **前端按 3–5 字节奏渲染**（从 G-06 移来：服务端切块正确，传输层会合并，
      视觉节奏只能由前端保证）
      —— **Claude Code 自测**（逐次记录显示字数：14 次各 4 字 + 末尾 1 次 3 字）
- [x] **流式生成时正在读的内容不动**（产品不变量的直接推论）：
      展开那一刻允许一次可见性微调（把撑开区带进视口），仅此一次。
      之后撑开区每长高一行，**撑开区本身及其上方的可见内容位移必须为 0**，下方内容被向下推开；
      长过视口底部也不自动追，由用户自己滚。判据同 G-05：测量，改动发生时可见内容位移 = 0
      **撑开期间高度只增不减**（决议 B）：生成结束、白话不足 3 行、换成失败提示，都不缩回，收起时才缩回
      —— **Clara 亲验（线上）**：A 生成过程中上方可见内容位移为 0；A2 生成结束时面板下方内容不上移
      —— **Claude Code 自测**：撑开区及上方 126 字位移 0；成功（短 / 长）、断流、403、429、502 六种情况，
      生成结束时下方第一行字位移均为 0px
- → 移交 G-40（2026-09-20）
- [ ] 生成中收起 / 离开页面 → 中止请求、不缓存（D3/D4）；中止后本地不再发出新请求，服务端日志记录 abort 事件
      —— **Claude Code 自测**：生成中按 Esc，服务端日志记为 `gloss_abort`（首字前）；中止结果不进会话缓存
      —— **还差**：离开页面时的中止（依赖浏览器在卸载时取消请求，没测过），行为拆卡方案待确认。
      —— **计费口径修订（2026-09-18）**：不承诺零费用，上游在中止前已处理的 token 可能仍计费；
      G-15 用 `gloss_abort` 事件与 DeepSeek 用量核对对账。原有自测记录保留，不据此补勾未验项目。
- [x] 断网时点击句子有明确提示，非无限加载
      —— **Claude Code 自测**（模拟 `navigator.onLine = false`：显示「网络已断开，连上网络后再试。」+ 重试）
- [x] 中途断流（D2）：已收到的文字保留在撑开区，一行灰字注明没写完（不写「出错了」这类把责任推给用户的话），
      给「重试」——从头重新生成，不做断点续传（接不上原来的语气）
      —— **Claude Code 自测**（模拟断流：保留已收到的字 +「网络中断，这段白话没有写完。」；重试后整段替换）
- [x] **被限流与服务端出错分成两种界面**（决议 #8）：WAF 返回 403 时显示「点得太快了，过几分钟再试」，
      不给马上能点、点了还会失败的「重试」；只按状态码判断。我们自己的路由从不返回 403。
      403 发生在流开始之前，与中途断流（D2）是两条路径；上游 429（C3）保持原样。
      不做冷却：每次点击都真实请求，403 结果不进会话缓存。结构摘要被 403 拦下时不提示、本次会话不重试
      —— **Clara 亲验（线上）**：B 被 WAF 拦下时显示限流文案、不显示重试；窗口过去后收起再点，能重新请求并正常生成
      —— **Claude Code 自测**：模拟 403 / 429 / 502 三种界面分开；`app/api` 下无 403
      —— **Claude Code 自测**（2026-09-17，本地生产构建，前置本地代理模拟 WAF：`/api/structure` 一律 403，
      `/api/gloss` 返回模拟流、不转发；预检确认服务端模型调用 0 次）：用从未缓存过摘要的 docId 开书——
      ①页面上没有任何提示；②本次会话 `/api/structure` 只发了 1 次，点两句后仍是 1 次，摘要未写入缓存；
      ③点句照常出白话，两次请求的 `structure` 均为 null。整个过程服务端模型调用 0 次
- [x] 同一句在本次会话内重复点击，不重复发请求（跨会话缓存是 G-09）
      —— **Claude Code 自测**（收起再点同一句：直接整段显示，新请求 0 个）
- [x] 结构摘要：开书时后台计算一次，按 docId 缓存；算好之前的点击不带摘要照常发，不让第一次点击等待
      —— **Claude Code 自测**（开书发 1 次 `/api/structure`，摘要 253 字按 docId 存入 localStorage；之后点句日志 `hasStructure=true`）
- [x] **部署到 Vercel，链接可访问**（需关闭 Deployment Protection，否则对方要登录 Vercel）
      —— **Clara 亲验（线上）**：在无痕窗口打开 gloss-jet.vercel.app，不需要登录就能打开，点句子能出白话
      —— **Claude Code 自测**：main `b7bfe89` 的 Production 部署状态为 success；
      未登录 Vercel 的 `curl` 能从 gloss-jet.vercel.app 取回页面，前端脚本里有本次新加的文案
- [ ] **实测并记录单次调用成本**，用于校准 PRD 3.8 的额度阈值
      —— **Claude Code 自测（本地日志）**：前后各 2 句 + 结构摘要，每次约 ¥0.0009（高峰价，固定前缀命中缓存），
      全部不命中缓存时约 ¥0.0021；空闲时段减半（G-06 窗口实验实测）
      —— **还差**：线上的单次调用成本，**挪到 G-15**
- [x] ⚠️ **公开链接的滥用防护**：PRD 3.8 的每日额度存在 localStorage，清一下就绕过。
      链接一旦公开，任何人都能烧你的 API 额度。**合并进 main 前必须有**：
      服务端 IP 级速率限制 + 供应商侧的消费硬上限。两者缺一不可——速率限制防刷，消费上限兜底。
      消费上限：DeepSeek 是预付费，**余额本身就是硬上限**，余额耗尽返回 402（G-06 实际遇到过）；
      账户里常态保持 ≤¥20
      —— **Clara 亲验（线上）**：WAF 限流（见下一项）；Production 配 key 在 WAF 生效之后
      （Clara 确认：先用 35 次请求验证拦截生效，之后才添加 key）
      —— **Clara 亲验（线上）**：DeepSeek 账户余额 ¥17.07（2026-09-17 查看），≤¥20
- [x] 🖐 **需人工操作（Clara 在 Vercel 控制台完成，不写代码）**：Firewall → 新建 WAF 限流规则——
      路径前缀 `/api/`，按 IP，10 分钟 30 次，超出返回 **403**（WAF 在边缘拦截，请求到不了我们的路由；Hobby 套餐每个项目限 1 条）。
      已配置并验证（2026-09-16，gloss-jet.vercel.app）：POST 连发 35 次，前 30 次 400，之后 403。
      用 WAF 而不用应用层计数：应用层每个实例各记各的，挡不住。
      **规则生效之前，不得在 Production 配置 `DEEPSEEK_API_KEY`**（安全底线第 6 条）
      —— **Clara 亲验（线上）**；Production 已配 key，Preview 与 Development 未配

> 前端埋点（`lib/analytics.ts`）不在本 issue 做，推给 G-15：服务端日志已有延迟、错误类型、中止与 token 数，
> W1 要看的指标都在里面。PRD 3.8 的每日 100 次配额：防滥用由上面的 WAF 规则负责；
> 给用户看的「今天还剩多少次」是产品功能，推给 G-15。
>
> **性能回归（Claude Code 自测，本地生产构建，perf30x：450 段 / 1830 句，40 个样本，与 G-05 同一脚本）**：
> 点击 → DOM 更新中位数 6.5ms（G-05 为 6.4ms）、p90 10.6ms、最长 17.2ms；从收起状态点开一句，
> 450 个段落组件里只有被点的 1 段重新渲染（10/10）。测试时拦截曾失效一次，真实调用 11 次、约 ¥0.009（见执行纪律第 6 条）。

**会改**：`components/reader/{GlossPanel,Reader}.tsx`、`lib/gloss-client.ts`（新建）、`lib/storage.ts`、`lib/storage.test.ts`、`styles/reader.css`、`KANBAN.md`、`PRD.md`（3.8 变更记录）
**不改**：`app/api/**`、`lib/deepseek.ts`、`lib/prompts/**`、`lib/parse/**`、`lib/segment.ts`、`components/Upload.tsx`、`package.json`
**回滚**：`git revert`
**完成**：2026-09-17 ｜ commit `b7bfe89` ｜ Safari 挂起为已知盲区；gloss-jet.vercel.app 已上线

---

### G-03 · 规则切句
**P0** ｜ 依赖 G-02 ｜ 分支 `feat/g03-segment`

**可验证增量**：同一份 docx 切出 **61 句**，平均 49.8 字，最长 157 字，最短 2 字。这是本 issue 的硬验收。

> ⚠️ **基准两次修订：56 → 62 → 61。**
> ① PRD 1.2 的 56 句是在「软回车不分段」前提下测出的；G-02 定案软回车按分段处理，
> 段落 4 → 15，句数变为 62。
> ② 62 的算法有缺陷：段 14 末尾的孤立后引号 `”` 被切成了单独一句（最短 1 字）。
> 定案「句末标点后紧跟的闭合标点并入前一句」，句数落到 61。
> 61 是双方独立复算一致的结果。实现若得出别的数字，**逐项说明差异来源，不得调算法去凑**。
> PRD 1.2 的数字待 Clara 决定是否同步修订。

**逐段句数分布**（定位差异用）：
段 1–3 各 1 ｜ 段 4 = 6 ｜ 段 5 = 3 ｜ 段 6 = 6 ｜ 段 7 = 23 ｜ 段 8 = 7 ｜
段 9 = 6 ｜ 段 10 = 2 ｜ 段 11–15 各 1

**验收**
- [x] 序言 docx 切出 61 句，平均 49.8 字，最长 157 字，最短 2 字
- [x] B3 引号 / 书名号内部不切分
- [x] B4 分号视为句末；省略号、破折号不是
- [x] B5 空白段落归一化，不产生空句
- [x] B2 超长句（>250 字）在句中标点处切分，切完仍超长则继续切
- [x] **句末标点后紧跟的闭合标点（`”’』」》）］"'．`）并入前一句**，不产生孤立残片
- [x] **全角 `（）［］` 与引号、书名号同等保护**，内部不切（PRD B3 未写，属填空白）
- [x] 句末标点集合：`。！？；．` + 半角 `! ? ;`；**半角 `.` 不算**（`1.5`、`Mr.`）
- [x] 产出含**全文顺序号 `index`**，G-06 取上下文、G-09 做缓存 key 都依赖它
- [x] **B2 切分只在成对标点之外下刀**；整句都在一个长引号/括号内时不切，
      但必须留下可观测痕迹（`console.warn` 或埋点），不得静默存在
- [x] **段末栈未清空（存在未配对的前引号）→ 该段放弃成对标点保护，重切一遍**。
      防止一个错误引号让整段变成一个不可切分的巨句（直接推高 G-06 的调用成本）
- [x] **降级要可观测**：产出中带 `degradedParagraphs: number[]`，
      并有断言覆盖「样本的段 11、段 13 降级，其余段不降级」。
      否则降级逻辑在真实样本上被触发却零断言，将来一改就静默失效
- [x] 有一组回归测试用例，含上述边界 + B2 合成用例 + `《甲；乙》。` 应为 1 句
      + 未配对前引号的段落能正常切分

**会改**：`lib/segment.ts`、`lib/segment.test.ts`、`package.json`、`package-lock.json`、`KANBAN.md`
**不改**：`lib/parse/**`、`components/**`
**回滚**：`git revert`
**完成**：2026-09-15 ｜ commit `049383e` ｜ 49 条测试通过，含降级逻辑反证


### G-01 · 脚手架与 token 系统
**P0** ｜ 依赖 无 ｜ 分支 `feat/g01-scaffold`

**可验证增量**：`npm run dev` 起得来，页面渲染出三栏骨架（空内容），三套主题可在控制台手动切换并看到底色变化。

**验收**
- [x] Next.js 项目可本地启动、可部署到 Vercel
- [x] `styles/tokens.css` 含三套完整主题（羊皮纸 / 月白 / 护眼绿），每套 6 个变量
- [x] `--paper-side` 比 `--paper-main` 明显更深，中栏最亮
- [x] Cinzel 在 wordmark 上实际渲染（控制台实测 `getComputedStyle(document.querySelector('.wordmark')).fontFamily` 含 Cinzel）
- [x] JetBrains Mono 已声明但 `preload: false`，Network 面板无其字体请求
- [x] 中文正文使用本机字体栈 `--font-serif-cjk`，无任何中文网络字体请求
- [x] **项目中不存在 `cdn.tailwindcss.com` 的引用**：`git grep -n "cdn\.tailwindcss\.com" -- ':!*.md'` 返回为空即通过（排除文档，只查代码与配置）

**会改**：`package.json`、`package-lock.json`、`tsconfig.json`、`next.config.ts`、`.gitignore`、`app/layout.tsx`、`app/page.tsx`、`styles/tokens.css`、`styles/reader.css`、`README.md`、`KANBAN.md`、`PRD.md`、`AGENTS.md`、`CLAUDE.md`
**不改**：无（初始 commit）
**回滚**：删分支
**完成**：2026-09-15 ｜ commit `488c7c1` ｜ Vercel main 构建通过


---

## 待定事项

- 白话读完率的判定规则未定，由 Clara 与项目负责人共同决定，G-15 开工前解决；Codex 不自行定义。
- 撑开区预留 3 行（Clara 觉得预留空白不好看）：G-10b 合并后单开小卡，用 slow-gloss 对照三种做法——① 维持 3 行；② 预留 1 行后随内容逐行长高；③ 3 行高度改为浅灰占位线。仅记录，本轮不做。

---

## 🚧 阻塞中

| Issue | 阻塞原因 | 需要谁 |
|---|---|---|
| G-16 | **示例文本与版权路径未定** | Clara |
